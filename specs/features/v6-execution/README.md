# V6 execution repairs (W01–W03)

Base: `03fab6b9d6a04ce9fdaeb48636383757213f7242`. Status: implementation in progress; results belong to `verification/v6/RESULTS.json`.

Outcome: an accepted research run survives the API/queue crash gap, only one attempt owns a live lease, and unsafe publication fails closed. Requirements: V6-F01/F09/F10/F11/F14/F15/F19, A01–A23 and A43–A48 as applicable. Existing acceptance requirements remain.

Impact checklist: no new framework/service; W04 adds a pinned offline Python extraction dependency (Trafilatura/lxml), exact artifact lock and isolated process per ADR-009; additive PostgreSQL migrations 006–008 for durable dispatch, run request digest, logical provider attempts, bounded evidence byte artifacts and extraction receipts; queue and run modules retain ownership; no public response schema change; no new model route/prompt or spending authorization. Dispatch payload contains only run identity, never query/private bytes. Existing nonterminal runs need idempotent outbox backfill. Migration must not rewrite earlier migration files. Local test database only; hosted migration remains gated.

Verification: production publication regressions with controls, actual multiple-connection PostgreSQL lease/admission tests, dispatcher interruption/replay tests and existing nonbillable suites. Tests must not hold a transaction during queue/network calls. Record failures rather than broadening accepted errors.

Rollback: disable issuance/dispatch or new strategy; retain accepted outbox records, budget reservations, rejection gates and deletion tombstones. Restore execution only after reconciliation; never blindly retry unknown provider attempts. Do not drop durable safety data to roll back application code.

Open: all-worker mutation fences/heartbeat, complete scoped provider accounting, atomic owner/consent validation, full semantic report coverage, and migration rehearsal. File existence is not completion.

Report checkpoint impact: additive migrations 009–010 retain immutable checked claim revisions, actual checker identity, digest-bound support and deterministic report derivations. No new provider or public report representation. Existing report claim IDs now point to persisted server-generated UUIDs. Privacy deletion removes new support/derivation records. Legacy raw-file and derived-store deletion remains open. Regression scope includes renamed entities, false entailment, hidden extra prose, forged derivation counts, and real PostgreSQL publication. Rollback may disable publication or compilation, never restore overlap-as-proof or unmapped material claims.

W03 deletion impact (supersedes the prior open raw-file note): migration 011 adds transaction-owned attachment bytes, a durable legacy-file deletion outbox and versioned purge replay. No new dependency or service. Account deletion owns content purge; storage cleanup owns only generated flat keys under configured storage. Public deletion response adds `fileCleanupPending`; it does not claim disk cleanup succeeded during an outage. Tests inspect all current research stores, actual filesystem failures/retries and real PostgreSQL contention with uploads and four late API mutation paths. Rollback disables new writes if necessary while keeping deletion processing enabled. Production identity, mobile caches and hosted backup/retention verification are still separate open requirements.

W03 identity impact: migration 012 owns the one-way issuer/subject-to-account digest mapping; existing development users are not merged by email or client metadata. Config pins Supabase origin/publishable key; no dependency, framework, service provisioning or spend added. New public read endpoint `/v1/session` exposes only local account identity/mode. Remote Auth verification runs outside database transactions, then account creation/lookup serializes by identity. No production fallback to local tokens. New identities receive zero allowance; deletion keeps a minimal denial mapping. Hosted Auth, native login/refresh and provider-side identity deletion remain activation gates. Positive claim-target tests now use actual persisted claim IDs; new negative cases reject foreign, unpublished, missing and block-alias IDs. No security assertion was removed. Rollback disables production authentication instead of restoring development-token acceptance.


W03/F17 mobile impact: exact MIT dependency expo-secure-store 15.0.8, no runtime dependencies or model assets, behind the mobile credential adapter. License, package integrity, baseline/current audit and bundle hashes are in `verification/v6/mobile/`. No server schema, provider route, network/spend capability or hosted migration added. Request generations and persistence have separate responsibilities; tests use injected stores/transport and explicitly do not prove native keychain behavior. Existing unscoped credentials are discarded, forcing sign-in. Content retention ends on logout/expiry/deletion; Android backup disabled in app configuration. Seven existing Expo-chain security advisories remain open with identical audit IDs before/after. Rollback disables session restoration/authenticated use; never restore AsyncStorage tokens or unguarded stale writes. Native secure-store failures and production login/refresh require device/hosted evidence before activation.


W04/F07 impact: additive migration 013 adds attachment extraction JSON and marks legacy pasted PDFs unsupported. New binary upload and owned status endpoints; source responses add immutable extraction/page/geometry fields. Existing JSON upload remains for explicitly pasted text/Markdown and rejects PDF MIME. Selected offline Docling Parse dependency has measured negation-preservation benefit over smaller text baselines; exact versions, transitive/license hashes, limited advisory lookup, runtime costs and rejected alternatives are in verification/v6/documents/. Pypdf checks metadata/encryption only; no model assets, new hosted service, spend route or mobile/server dependency reversal. Extraction occurs outside transactions and result storage revalidates run/account/fence. Full Docling/PDFium remain experiment-only. Old PDF positive test now transfers original binary bytes and retains unread-content assertions; malformed and encrypted negative assertions remain strict. No test was removed or skipped. Rollback disables binary processing; retain migration/bytes/ownership/deletion and treat missing runtime as unavailable. General synthesis/coverage and native file picker remain unimplemented.


W02/F15 executable argument impact: canonical Zod contracts now bound and strictly enumerate every action argument; no dependency, service, migration or paid route added. Model-proposed SQL, account/budget fields, arbitrary calculation expressions and claimed challenge outcomes reject. Challenge conversion projects only authorized arguments and revalidates the executable search, including its cost and privacy gates. Existing deterministic producers now supply explicit targets. Nine newly failing negative regressions were repaired; valid controls retained. Rollback disables affected actions instead of restoring broad record arguments. Typed result execution and general model gateway remain separate open work.


W02 account/run accounting checkpoint: `reserveLiveAttempt` requires exactly one active, owned run allowance and atomically counts actual provider receipts plus all unresolved reservations against the smaller of the admitted run budget and allowance. Existing account-serialized allowance admission bounds concurrent runs. Project cap remains independently locked. For controlled research, settlement uses actual OpenRouter receipts and retains the entire run allowance while any receipt is unknown; retry after reconciliation settles once. Actual overruns remain visible, not clamped to estimates. No schema/dependency/service change. Three reproduced failures plus concurrent receipt settlement/overrun/owner controls pass locally. The original project race now uses a 60,000 cap and 40,000 per-call reserve within its unchanged 100,000 run allowance, preserving the project contention test rather than increasing authorization. Rollback disables paid issuance/settlement, never releases unknown reserves. Provider-key buckets, automatic reconciliation, cost-view separation and stale historical settlement repair remain open. Default 200,000 live-call reserve exceeds the 100,000 run allowance and now correctly blocks; do not raise either actual allowance without authorization.

The original provider-before-network positive control now has an explicitly funded test-only 200,000 allowance; production defaults remain 100,000. A separate default-budget control reproduced swallowed admission errors falsely emitting searched/published. Worker errors now stop with search_blocked/search_unresolved and preserve unknown reservations; the negative case requires zero provider calls, zero issued intents and no searched/published events. No positive assertion was removed.

W02 provider-key impact: additive migration 014 stores an opaque SHA-256 credential identity on new provider intents, never the credential. A separate server-only LIVE_KEY_SPEND_CAP_MICRO defaults to zero; admission locks its bucket across accounts/project scopes. Unattributed historical OpenRouter intents conservatively count against every key, pending explicit evidence-based reconciliation. No new service/dependency or allowance. Tests use fabricated nonbillable credentials and local PostgreSQL contention. Rollback disables issuance while retaining key identities/unknown holds; dropping the column or restoring project-only admission is unsafe.

W02 late-receipt/cost-view impact: keep receipt application and terminal allowance settlement in one account/run-locked transaction, without replaying external requests or touching evidence. Active run cost becomes the confirmed provider sum; cancellation/deletion do not erase genuine charges. Public cost response adds explicit confirmed/held/unknown provider fields and labels fixture simulation separately. No migration/dependency/service/paid request. Historical already-settled rows require separate repair, not automatic retroactive charging. Rollback disables receipt application and retains unknown holds rather than releasing them.

Additive migration015 records the settlement amount and basis for new settlements. Historical rows retain null basis, remain explicitly unreconciled and are not silently recharged. Controlled-run cost responses exclude old fixture simulation rows; provider spend excludes unmeasured local worker/storage costs. Read-only repeatable-read snapshots prevent mixed ledger views. Receipt application after deletion may record incurred financial cost but cannot restore private content/session authority. No new public mutation endpoint or provider polling loop is added.

W05 structured gateway impact (in progress): one backend model transport/coordinator, six strict versioned operation outputs, backend-owned IDs/basis/budgets and persisted typed outcomes. Use generated JSON Schema from canonical Zod definitions; proposed exact zod-to-json-schema3.25.1 is ISC, no runtime dependencies beyond existing compatible Zod peer. No SDK/service/model assets; public contracts remain pure. Prompt/config/version changes are recorded. Provider is pinned with no fallback and capped prices; selected metadata is a capability observation, not an authorized paid probe or semantic quality proof. No fixture output or old template substitutes for failed model output. New result persistence must join deletion/fence tests before activation. Rollback disables the strategy while retaining publication/privacy/accounting gates.

W05 result-store/consent impact: migration016 adds account/run-owned model results keyed to immutable provider intent and versions, included in deletion. Consent policy2026-09-17 explicitly names OpenAI; controlled admission and gateway require it. The former duplicate request-default literal was removed after it caused11 initial consent failures. Transport/coordinator have no fixture fallback. One corrupted-cache regression failed before stronger validation; no assertion was weakened. Route remains opt-in and general-worker integration remains open.


W02 search-outcome impact: a narrow worker repair, no dependencies, services, public schema, migration, prompt or model-route changes. Nonthrowing failed/unknown search receipts and prior issued attempts without output now terminate unresolved instead of emitting searched/bumping evidence. Financial receipt/state are one transaction; unknown allowances stay held. A positive control now includes an explicit zero-cost provider receipt, replacing its contradictory success expectation on missing cost; no negative assertion removed. Five new PostgreSQL worker cases cover HTTP failure, timeout, missing cost and unknown/confirmed prior-attempt replay. Rollback disables controlled search, retaining reservation/receipt safety rather than restoring fabricated success. Durable search-output reuse and malformed-response validation remain open.


W05/F04 task identity impact: additive migration017 research_tasks owns a versioned criterion/question specification and server-generated IDs, unique per run/brief revision. The worker preparation service calls the existing gateway or adopts its already stored result without another paid request; evidence revision changes preserve task identity. Strict provenance, receipt and version checks reject corrupted/stale data. Material ambiguity is needs_clarification; ready means planning-ready only. No new dependency/service/model route/prompt/public API. Domain services depend on declared ports; worker composition supplies adapter policy/prompt pins. Account deletion explicitly purges rows with cascade defense. Ten new real PostgreSQL service regressions supplement the existing11 gateway tests. Exact original-question mutation test initially changed only a legacy column; its corrected setup changes the canonical payload too. Rollback disables structured preparation and preserves tables/consent/budgets/deletion/publication. End-to-end controller, semantic interpretation quality, typed corrections and criterion-linked support remain unverified/unimplemented.


W05/F02-F04 extraction impact: the worker assertion-extraction service composes the owned task, explicitly selected whole passages and existing structured gateway. No fixture names/catalog, new model prompt, processor, dependency, service or public API. Migration018 adds nullable input_manifest to model results; old rows stay NULL and fail cached reuse/adoption instead of fabricated backfill. Manifest records all selected passage/version/digest/access even when model output omits them; source-text hashes are recomputed. Selection order is canonical; repeated identical input reuses its logical operation. Eight added PostgreSQL tests use randomized unfamiliar entity names and fabricated model responses to prove execution/persistence boundaries, not semantic model quality. Assertions are unverified until substantive support execution and publication. Raw/derived deletion removes manifests with results. Rollout retains structured activation off until general loop/support/publication integration. Rollback disables structured operations and preserves manifest checks, financial holds and deletion.


W05/F01-F04 support impact: implements a pure scoped-support resolver, one worker coordinator, owned extraction-context restoration and additive migration019 for assertion/revision mappings and actual support results. The existing evidence-context loader is shared by extraction/support with unchanged ownership and whole-passage bounds; it is not a general utility sink. No dependency/service/public API/model prompt/route change. Stable canonical claims/revisions remain unverified for publication until the separate final gate. Checker results preserve model status, independent rule outcomes, uncertainty and omitted counterevidence; exact model schema/prompt/policy follows the linked operation. Tests preserve all existing publication negatives/controls and add paraphrase, negation, qualification, numeric/unit/scope/access, missing-binding and omitted-counterevidence controls. Real PostgreSQL tests prove stable replay, changed-unit veto, uncertainty, bindings, owner/basis isolation, corruption and deletion. Model responses are fabricated; no semantic benchmark/human/live claim. Initial typecheck failed on an inferred empty numeric array; an explicit string-array return type fixed it without weakening assertions. Rollback disables structured support; retain new data/deletion/financial/publication protections. Native/general controller/report integration and held-out semantic evaluation remain open.


W01/W05 publication impact: wire stored scoped assertions into the real report gate, rechecking exact canonical text/passages, current basis, policy and immutable check records. Managed assertions cannot fall back to literal approval when scoped checking is non-supporting/missing/stale. Pure citation validation retains all mapping/ownership checks; supported canonical IDs are preserved rather than copied to new revisions at publication. No public schema, dependency, service, migration, prompt or route change. Shared backend policy constants moved unchanged to the declared port to avoid domain-to-adapter imports. Added real PostgreSQL publication/reopen cases for semantic paraphrase, partial literal support, stale basis, changed/unmapped prose, forged caller approvals and corrupted stored checks. Existing supplied negative probes/valid controls stay enabled. All semantic judgments in tests are fabricated transport inputs; this is boundary evidence, not live entailment quality. Rollback disables the structured writer/strategy while retaining managed-claim rejection, privacy, receipts and existing report reads. General writer and controller integration remain required.


W05/F05 generic writer impact: adds immutable draft lineage (migration020), pure writer-statement projection/compiler and a worker write→check-final-text→publish service. Source premises must be revalidated supported assertions; generated headings/paragraphs/limitations are separate inference proposals assessed against original source text. Unsupported material becomes localized unresolved text, without granting approval from premise citations. Canonical final claims retain premise revision IDs and partial dependency completeness. Self/cyclic draft origins and oversized expansion reject; repeated drafts reuse durable output. New scoped-support.v2 currency-token guard requires a checker-version component in the support-result primary key; older records remain separate. Existing claim scope and model prompt/contracts/routes remain unchanged. No dependency/service/public API/model assets. All new records are included in account deletion. Local migration/table tests are not hosted migration evidence; primary-key change requires a hosted lock/volume rehearsal before deployment. Rollback disables structured writing, retains new lineage/versioned results and all publication/privacy/financial gates. Real-model semantic quality, criterion closure, general processRun/retrieval, corrections, mobile and evaluation remain open.


W05/F04 coverage execution impact: adds a pure criterion-linked coverage resolver, fenced review coordinator and additive migration021 for immutable review outcomes. The existing structured review operation and prompt stay unchanged. The review uses original question, versioned task and exact selected evidence/assertions, with independently restored support checks. Supported coverage requires each cited assertion to have one supported check and each requested criterion to have relevant scope-matching evidence; model waivers, missing criterion bindings, ambiguity and omitted original requirements cannot produce complete coverage. This is answer coverage, never proof of positive candidate eligibility. Semantic relevance remains fallible and requires held-out model evaluation. Typed outcomes preserve review reasons and failed deterministic checks, canonical claim revision IDs and support/coverage checker versions. Replay revalidates all stored inputs and results; account deletion purges outcomes. No new dependency, service, public contract, prompt, processor or spending authority. Rollback disables coverage execution and retains ownership, versioned support, deletion and financial holds. Main worker and final report completion integration remain open.


W05 final-report coverage integration: the generic writer executes review_coverage after final-wording support, then reloads the typed resolved review inside the publication transaction. Completion requires complete question coverage and no rejected final statements. The canonical publication gate independently revalidates stored coverage and recompiles the exact draft; dropped/changed blocks, changed claim membership, missing/corrupt review and caller completion flags cannot grant completion. Incomplete coverage retains completed_with_limitations. Runs with no structured task retain the historical publication contract; this is not general controller integration. No dependency, migration, public schema, prompt, processor or model-route change. One additional bounded structured review call uses existing admission/receipt rules; failures block publication rather than claim completion. Rollback disables structured writing; retain completion gate, financial holds and private evidence deletion. Local fabricated semantic responses prove boundaries only; real research quality and worker stopping integration remain open.


W05 production worker integration: with structured activation enabled, processRun now prepares the versioned task, ingests owned attachments, extracts selected whole passages, executes assertion support and question coverage, and writes/rechecks/publishes a generic report. This path never invokes the fixture proposer or template composer. No readable evidence, invalid/unknown operations or no supported assertions end explicitly unresolved/failed. Discovery and larger evidence selection remain unfinished; the flag stays off by default. Existing historical routes remain separate. This is a first evidence-backed worker path, not the complete unfamiliar search/correction journey.
Impact: new worker composition module only; existing contracts, gateway, persisted results, source extraction and publication gates reused. No dependency, migration, public schema, prompt, processor or monetary authority change. Source preparation precedes extraction, and all network/model work remains outside transactions. Typed support/coverage executions back the evidence_checked event; an event alone grants no support. Resumption uses saved logical operation outputs; terminal unresolved paths settle actual known costs and preserve unknown holds. No silent passage truncation or fixture fallback. Rollback disables STRUCTURED_MODEL_ENABLED, preserving current identity, privacy, financial and publication safeguards. Main discovery/action scheduling, effective corrections, mobile and real evaluation remain open.
