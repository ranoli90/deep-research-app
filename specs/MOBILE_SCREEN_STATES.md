# Mobile Screen and State Specification

These are implementation requirements. They are not screenshots or claims that native validation has occurred.

## Navigation and global behavior

Research is the sole primary destination. There is no bottom tab bar. Library opens full-screen from a left **Menu** control and from **Profile → Library** (ADR064). Profile opens Settings as a full-screen calm account surface. New research is always available from Research or Library chrome without traversing an agent dashboard; it clears local focus and dismisses Library while leaving server jobs and history intact. Keep a research conversation's route stable while its job runs. A notification/deep link opens the exact conversation and verifies the signed-in owner before fetching it.

Use a tablet/foldable master-detail / left drawer for Library only when space supports it; do not force a phone into desktop density. On narrow devices, Library and Profile are dedicated full-screen destinations with Done/Android back returning to Research. Respect back-navigation conventions on both systems. Android back closes a sheet/keyboard before navigating away. Navigation must never silently cancel a server job.

Preserve drafts, report scroll anchors, collapsed sections, and local display preferences across ordinary navigation. Clear account-specific cached state on logout/account switch. A draft containing potentially sensitive content must follow an explicit local retention policy rather than live forever in unprotected storage.

## 1. First launch and authentication

Screen states: fresh install, signed-out return, expired session, signing in, sign-in failed, email verification/pending, authenticated, and unavailable network. Do not present a paid live research button that will fail because authentication is missing.

Offer a labeled example report without requiring payment. A user can draft before sign-in; make the preservation behavior explicit and test it. Disclose AI processing before the first actual transfer. Declining consent leaves the user able to view the sample and settings, without secretly initiating requests.

Email sign-in/deep-link flows must validate redirects and avoid displaying raw auth tokens. Social sign-in is optional until its complete native and store requirements are implemented. Do not show providers with broken credentials. Recovery, resending, expiry, and account switching need defined behavior.

## 2. Composer

Normal content: empty home shows a restrained “Ask anything.” plus stacked example lines (not a chip grid). Composer is a **docked hairline field** (not a floating pill): ~52pt visual height, 16pt gutter, 24pt outer radius, 16pt input, max four lines, 18pt plus and 36pt send inside 44pt invisible hits (`accessibilityLabel="Start research"`). Placeholders: empty `Ask anything…`; researching `Ask or refine research…`; completed `Ask a follow-up…`. The + sheet offers Files, Paste note, Add URL (public http(s) only, attached as a text note), and Source preferences disclosure. Quiet send until there is text, then filled teal with an up-chevron; researching morphs send into stop. Files are optional. Do not require advanced configuration on first use. Example prompts must be specific useful tasks, not a grid of generic AI slogans. Show few or none after the user has history.

**Continue-thread after a finished report:** keep the same dock composer for the thread (do not leave a leftover new-run “Research”/“Update” pill). Placeholder becomes “Ask a follow-up…”; send stays the rounded-square up-chevron (`accessibilityLabel="Send follow-up"`), not an Update pill. Header New research is a compose-square control (`accessibilityLabel="New research"`), not a text link. Corrections and follow-up details still submit through this composer.

**Dark / light chrome:** composer dock, thinking rail, question chip, and stop square consume `packages/design` chrome tokens (`color.light|dark.composer|thinking|userBubble|stop`). Keep the cream/ink/teal palette with calm contrast — not DeepSeek `#0F0F0F` black pills, ChatGPT green, or a Grok black-circle stop clone. Dark composer is a warm paper field (`#1F1C19`) with teal send (`#7EC4BC`); stop is a quiet rounded square. Visual identity: `verification/v8/research-beta/VISUAL_IDENTITY.md`.

Handle empty/whitespace input, long pasted text, emoji and Unicode, multiline text, pasted URLs, hardware keyboard, autocorrect, selection, dictation supplied by the operating system, and right-to-left text within a primarily English interface. Enter behavior must not surprise mobile keyboard users; explicit send remains available.

Use a growing composer with a maximum visible height and internal scroll, so a long question does not cover the entire screen. The send button has a stable hit target and prevents accidental duplicate submissions with server-side idempotency, not just a disabled animation. On Android edge-to-edge, pad the composer dock by the IME frame height plus a 48dp suggestion-strip inset. That inset is unit-tested only; physical Gboard coverage is unproven, so FP-085 is not closed. This is a functional keyboard inset, not a visual restyle of the composer.

Attach controls show permissible formats, remaining count, and size limits before upload. A removed file is detached from the brief and queued for appropriate deletion; stale upload completion callbacks must not attach it again.

## 3. Clarification and assumptions

When the backend brief has assumed constraints or unconfirmed consequential assumptions during queued/preparing, show a compact “Researching this…” card with the objective and explicit assumptions. The user may continue without editing. Do not force this card when the run is already researching without material flags. Present at most a compact group of material questions. Clarification is blocking only when lifecycle is `awaiting_input` or the brief marks `materialClarification`. Each question should be answerable without knowing research jargon. Keep the user's original question visible or recoverable. Suggested options must not silently choose a consequential personal fact.

Provide an “Use these assumptions” or equivalent start path when safe, showing the assumptions briefly. When missing information truly prevents safe execution, explain the specific missing input. Do not create an infinite clarify loop; record previous answers as hard context.

Editing the question before starting invalidates stale clarification suggestions. Submitting repeated answers is idempotent. A lost network request must recover server state rather than start a duplicate run.

## 4. Active research

Primary content: question, short current stage, safe activity summary derived only from the typed sanitized `activity` DTO on persisted events, elapsed time measured from event timestamps when present, cancel, and an optional details expansion. After completion the trail collapses to a truthful summary such as source count and elapsed time. Use indeterminate progress only when no semantic event exists yet. Distinguish queued from actively researching. Never map private chain-of-thought, raw prompts, or legacy `type`/`publicSummary` fields to progress. When `activity` is null, show nothing from that event.

Detailed activity is an ordered set of real events: searched a topic, opened a source, found conflicting figures, checked an assumption, drafted a report. Do not fabricate animated team dialogue. Explain a pivot in one sentence anchored to findings, not a stream of private reasoning.

**Live source appearance (during search).** Prefer Grok-style search/read activity lines over a Perplexity-style favicon strip of domains “arriving,” ChatGPT places/maps chrome, or DeepSeek “Found N results” theater. Do **not** invent hosts, titles, or favicons while thinking/searching. Domain pills may appear only from `activity.sourceDomain` on a `source_reading` event. Pill copy is the hostname; favicons render only from owned icon bytes already on device—never a third-party favicon CDN (query leakage and generic/fake icons). If no domain is present, show no source pills. Source counts distinguish found/read/cited only when those facts exist on events.

Show useful partial findings only after they exist and label them provisional. Source counts explicitly distinguish found/read/cited where shown. In-app progress can be more detailed than a privacy-safe push notification.

When the user scrolls up, new events must not pull them down. Offer a subtle new-content control. Screen readers should receive bounded meaningful status changes rather than every streamed token.

Cancel first enters “Stopping new work”; it must not falsely claim an already-issued opaque provider request is stopped. Explain retained partial results and allowance outcome once known. Do not force a second confirmation for a harmless routine cancellation, but do not confuse cancel with deletion.

## 5. Connectivity, app lifecycle, and notifications

Offline: retain the readable report/draft that policy allows, show a single status line (“You're offline.”) with Retry, keep the composer visible, disable network-dependent send, and recover automatically. Do not use a chip banner or status card for offline. Do not enqueue a paid submission invisibly while offline. On network restoration, ask/confirm according to the original submission state and idempotency record rather than guessing.

Background/terminated: server research continues. On reopen, request a state snapshot plus missed events. Notifications are optional and delivery is not guaranteed. A denied permission leaves the core app functional. An invalid push token is retired; retry notification failures without resending the research job.

A research completion notification defaults to nonsensitive copy such as “Your research is ready.” Account switching unregisters the old account/device binding. Notification navigation must recheck ownership and show a safe unavailable state for deleted reports.

## 6. Report reader

Use stable structured blocks with strong reading hierarchy. Put the actual answer first. Long reports receive a compact outline, section anchors, and source access. Tables scroll horizontally within their boundary or become accessible stacked comparisons; do not shrink text to illegibility.

Support text selection, copy section/report, code block horizontal scroll, external links, citation navigation, and native sharing. Do not render raw generated HTML. Long unbroken URLs or code cannot expand the entire screen width.

The report states its research date and important scope limitations. It distinguishes external evidence, inference, user-supplied facts, and calculation as needed without badges on every sentence. Do not display a numerical truth/confidence score without a validated calibrated meaning.

Writing/streaming states must not repeatedly reflow the whole document. Final publication should be atomic enough that a report cannot look complete while lacking its final citations. Keep the composer accessible below the report without covering content.

## 7. Source sheet and after-search citations

**After-search citation chips.** On the published report, cite with first-appearance numbered chips (`1`, `2`, …)—never raw passage UUIDs. Optional `N · domain` only when that domain is known from owned source metadata (not guessed from titles). Chips sit with the answer/blocks they support; tapping opens the source sheet for that passage.

**Source sheet (quote-first).** Lead with the exact supporting passage, then title, domain/publisher, uncertainty/access/coverage, “Cited in” when a related claim exists, locator, extraction limits, and open-original when a safe public link exists. Keep the most useful evidence above low-level metadata. Do not show a nonexistent downloaded document, page highlight, or favicon invented for the host.

Buttons: open original and close/back; optionally expand evidence context. A deleted or inaccessible source reference returns a truthful error without blanking the report.

Entering/leaving the sheet preserves report scroll position and keyboard focus. VoiceOver/TalkBack receives a descriptive citation label (number, not UUID), sheet heading, and appropriate close action.

## 8. Follow-up and report versions

Provide optional relevant actions plus free text, not a wall of chips. “Go deeper” scopes to selected content when selected. “Check this claim” identifies the claim and supporting evidence. “Find contrary evidence” does not manufacture disagreement when the evidence is consistent.

**Suggested next asks (consumer chips):** place at most three chips in the composer dock **above the field**, not under the answer body. Grok-style dock placement stays tappable while the keyboard is open; Perplexity/ChatGPT under-answer chips are rejected here because the keyboard would hide them. Copy is short **questions** derived from unresolved items, caveats, and named limitations — not truncated caveat statements. Chip labels stay ≤42 characters; tapping fills a ≤160-character question into the composer (with `Also:` appended after the original question on `replace_question` routes). Never dump multi-kilobyte block text into the draft. Chips stay visible when the keyboard opens; tabs may hide.

Before a paid continuation, show its allowance effect. The new run references its parent and uses an updated brief. A change summary explains altered findings rather than just saying “updated.” Preserve version history and its source references. Do not overwrite an earlier report while it is being exported.

## 9. Library

States: no reports, sample-only, loading, results, search-empty, offline, error, and deleted item. Use readable titles, brief metadata, and discreet run status. Rename should not change the underlying research content. Archive is reversible; delete is clearly destructive and follows the retention policy.

Paginate long histories. Search must respect ownership and include only permitted content. Avoid creating separate confusing tabs for chats, runs, reports, sources, and projects; those are data relationships, not necessarily navigation destinations.

## 10. Settings, billing, and help

Group Account, Appearance, Research preferences, Source preferences, Privacy & data, Purchases, Help, and About. Hide developer/provider diagnostics from normal users. Settings rows are ~56pt. Help explains question → progress → answer → sources. About is the product name and version only.

Usage shows allowance, pending reservations, settled usage, and any relevant reset/expiry policy with actual time zones. Do not invent time-to-complete or remaining run counts when tasks have variable cost. Restore purchase is available and handles restore-to-wrong-account safely.

Privacy shows actual consent controls and data flows. Delete account explains effects on active research, saved reports, device sessions, data retention exceptions, and any separate subscription cancellation steps. Cancellation of a store subscription and deletion of an app account must not be falsely described as the same operation.

Provide in-app AI-output reporting with category, optional explanation, clear submission state, and permission to include relevant content. Help/errors should offer an actual next action, not a dead “contact support” link.

## Interaction completion checklist

Every visible control needs a defined normal, pressed, disabled, loading, success, error, offline, accessibility, and restoration behavior where applicable. Keep irreversible actions distinct from reversible navigation. Confirm destructive deletion; do not make everyday research require repeated confirmations.

Release checks must use real native screens on both platforms, multiple text sizes, compact/large phones, both themes, and long real/fixture content. Browser screenshots alone cannot satisfy this checklist.


## Revision 2: decision, correction and reading continuity
These requirements refine the earlier states in this same canonical specification. They are not verified UI behavior.

**The first result.** Put the task answer first with its key constraint and any decision-blocking unknown. This is a view of the canonical claim set, not an independently hallucinated summary. Show enough detail to distinguish a feasible option from an unverified one. Exploratory questions need synthesis rather than forced rankings. Preserve long-form reading on demand.

**A natural correction.** The ordinary composer accepts “Actually, the budget is different” or “That document is outdated.” After server acceptance, show a compact acknowledgement of the changed constraint and mark affected sections as being checked. Unaffected sections remain readable. A correction that broadens eligibility reopens discovery, not only reorders old results. Ask before additional spend only when the action materially exceeds the already accepted allowance policy.

**Current versus previous result.** Latest report and last verified report are distinct when an update is running. Do not present an old conclusion with a fresh timestamp. Version comparison highlights changed evidence, changed outcome, newly included/excluded options and remaining uncertainty. Presentation-only changes are labeled as such. Source deletion can remove/redact material in previous versions; immutable history does not override privacy.

**Source inspection.** One tap opens the exact passage, page/table cell when available, date/access state and the claim it supports. A second action opens the original. A snippet, abstract or failed page is visibly labeled. Model-checked does not mean proven true. Preserve reading position when returning.

**Persistent reading anchor.** Store reportVersion, blockId and a local offset. Restore on close/reopen. Map to a new version only when the block identity remains valid; otherwise explain that the section changed. Do not silently reset to the first paragraph. Test with large text, rotated/folded layouts, source sheets, long tables and offline cached reports. Authorization and deletion rules still apply to local caches.

**Failures and partial answers.** State what happened, what is preserved and the next viable action. Distinguish service capacity, expired session, no accessible evidence, partial extraction, unknown provider outcome, user cancellation and exhausted allowance. Do not call a task “failed” simply because a client stream disconnected while the server report exists. Do not promise the user will not incur cost when provider accounting is unknown.

**Error / offline / empty-progress chrome.** Competitors use one line plus Retry and keep the composer. Do not present these as banners, Sample-answer chips, or cards:
- Offline → `You're offline.` + Retry
- Failed research → `Research failed.` + Retry
- Cancelled → `Research cancelled.` (no Retry)
- Waiting for server (no events yet) → `Waiting for the server.` + Retry (Cancel remains available)
Keep the composer mounted. Sample labeling belongs on a demo report (`Sample` kicker), not a global chip banner. Active trails with real events may still use the collapsed activity card.

**Input acquisition.** Launch supports explicit share/import of links and supported files through native user action. Never read the clipboard or share-sheet content silently. Unsupported data formats need an understandable rejection. File removal during processing must invalidate late completion and derived report access.

**Plans and controls.** Show a lightweight editable plan when it helps; do not require confirmation for every clear request. No agent/model/token dashboard. Only expose pause if the actual executor can pause safely; cancel is required. Progress is event-based, not fabricated. Notifications are optional, private by default, and never replace reopen synchronization.

## Revision 3 notification/lifecycle clarification
P0 needs usable, labeled, keyboard-safe native composer/reader controls and close/reopen synchronization, not the complete P3 navigation/settings/purchase polish. Basic accessibility is built into those controls; broader device/accessibility validation remains explicit per platform.

Use the logical completion identity and account/device-binding epoch contract in `ENGINE_CONTRACTS.md` §10. Suppress duplicate in-app completions and reauthorize every incoming report link. Clear prior-account local cache and controllable displayed/pending alerts on logout. An already-issued generic remote push may still arrive; it must contain no private question/title/report data and must not open another account's report. Do not claim that app-side deduplication guarantees exactly one OS alert. Live push remains optional and does not block P0 research/reopen proof.


## V6 local session boundary (W03/F17, implemented; broader native proof pending)
Credentials use Expo SecureStore 15.0.8 with no plaintext fallback. Account/backend-bound content snapshots and request journals use a separate SecureStore service with bounded chunks and atomic slot metadata (ADR029); ordinary AsyncStorage retains installation/denial metadata, not newly saved private content. Legacy unscoped caches/tokens are discarded and require sign-in; migration of recognized legacy content must finish before restoration, with no plaintext fallback on failure. Logout, expiry and deletion clear private drafts, reports, source panels and queued persistence. Account/run/source generations discard obsolete responses even if transport cancellation is ignored. A durable denial marker prevents failed credential deletion from silently restoring the old session; failures remain visible. A fresh installation discards surviving keychain credentials. Android backup is disabled in configuration; both-platform native verification remains required. Production sign-in/refresh remains an activation gate. These rules supersede historical draft-preserving logout observations.


W06 comparison display: report-changes.v1 summaries show the server’s measured assertion/criterion/version note. Do not append “earlier conclusion kept” to exact inventory equality: the child independently recomputed its claims. No new native execution claim; existing historical diagnostic summaries retain their previous format.


W06/W07 replacement-question UI: the owned run snapshot advertises correctionMode and correctionReserveMicro. Production correction controls require replace_question capability, a displayed brief revision and valid allowance metadata; absent/disabled capability is not silently treated as the legacy route. The complete revised question is sent as the accepted patch, with explicit reuse_snapshot/refresh policy. Edit-current-question assists full replacement; helper text explains snapshot age and uploaded-file byte retention. The previous report remains available. In-flight submission disables editing/repeat taps and checks the captured account/view generation before selecting the child. Errors retain the current input. The protected snapshot stores the correction question, policy and exact run/revision (ADR030). Selecting another run hides the draft; returning restores it. A newer brief requires explicit review/rebase before submission, and account clearing removes it. Acknowledged correction persistence has a bounded native cold-restart control; abrupt death before acknowledgement can lose the latest input tail.
The API advertises only configured structured/live/key/cap availability; authoritative account, revision, allowance, consent and route gates still run at admission. This is not a paid availability probe or a promise of completed research. Demo correction admission is denied when fixture routing is disabled. Shared strict patch schema is reused on mobile before network dispatch. Unknown fields cannot inject run/budget authority.
Metro resolves NodeNext .js relative imports within the public contracts source package through its normal extension probing. Other package/native resolution remains unchanged, with no dependency added. The initial failed Android JS export and successful repair are recorded; bundling is not native execution. Bounded Expo Go correction cold-restart/recovered refresh evidence is recorded in verification/v6/correction-draft; broader keyboard/radio/large-text/back, authenticated lifecycle and standalone/iOS checks remain open. Rollback disables correction controls while retaining server ownership/route gates and current account/request-generation protections.

W04/W07 native binary selection (implemented; bounded Expo Go evidence, broader native gates open): the system picker accepts a single PDF, .txt or .md document, bounded to8MiB and the existing three-attachment limit. The mobile adapter reads real bytes from the picker cache copy; it never labels pasted notes PDF. Bytes remain in transient account state and are sent as application/octet-stream to the existing authenticated binary API on submission. Raw document bytes and picker URIs remain transient. Before submission, the protected admission journal records filename, MIME, exact-content SHA256, upload key and any confirmed attachment ID so retries preserve identity; these metadata are private content, not ordinary AsyncStorage entries. The picker copy is removed after success/failure, and its dedicated cache directory is cleared on next launch after a crash. Original user files are untouched. An account generation change prevents late adoption; selection blocks duplicate picker calls and Send until complete. Selected files can be removed before submission. Closing the app requires selecting them again, as explained in the UI. Account-scoped upload idempotency and protected admission recovery retry the same complete payload, with digest-checked reselection after restart; byte-offset streaming resume is not implemented. Source-specific server deletion and dependent local invalidation are implemented separately under ADR036, with native deletion acceptance still open. Rollback removes the picker control while retaining byte API ownership/validation/deletion.
Impact checklist: pinned expo-document-picker14.0.8 and explicit expo-file-system19.0.24 dependency (already present through Expo54), both MIT with no new JavaScript production transitive dependencies/model assets. Reviewed exact installed package manifests and picker native cache-copy paths, matched local Expo bundledNativeModules. No new service, schema, migration, provider route or paid call. The current installed standalone APK is not upgraded; Expo Go verification is separate. The original three-moderate/four-high dependency receipt remains historical. After scoped PostCSS and UUID remediation and the reviewed Vitest upgrade, the integrated audit remains exit 1 with two high image-size advisories and no moderate advisories (verification/v6/dependency-uuid-integrated). Expo Metro remains 0.83.3; the isolated Metro upgrade is unadopted while its native runtime check is blocked. These JavaScript dependency checks do not establish standalone Gradle resolution or release security.

Source inspection now validates metadata, preserves exact passage text, ordered table-cell headers/spans/scopes, actual page/block and recorded coordinates, publisher/access/coverage/extraction warnings and immutable source version. Missing dates are explicitly unavailable; coordinates are not a rendered PDF/highlight. Only HTTP(S) original links without credentials open on user action. Reading anchors use the invoking block and a signed offset relative to the report card; drag/momentum end records the visible block. Removed-block fallback clears stale offset. Abrupt death mid-drag and screen-reader focus return remain device checks; no native claim follows from unit tests or JS export. Rollback may disable presentation changes but preserves request/account fencing, exact evidence and honest coverage.

W03 protected snapshots (ADR029): credentials and content use distinct SecureStore services. Draft/report snapshots use bounded UTF-8 chunks and atomic active-slot metadata; ordinary AsyncStorage contains no newly saved content. Legacy plaintext migration completes before restoration, or raises a visible storage error without plaintext fallback. Account clearing writes durable content-denial flags and physically deletes allocated slots; failures remain actionable, not successful deletion. Native saves coalesce obsolete snapshots, skip unchanged view-only state, use150ms quiet time and flush queued data on background. Process death before save acknowledgment is not guaranteed to preserve the latest input tail. Research and Library are the only bottom tabs; Settings is profile-accessible. Unpaced bulk native input stress and broader lifecycle acceptance remain separate from storage controls. The synthetic offline-reader cold-restart control below is bounded evidence, not authenticated lifecycle acceptance.

W06 correction recovery (ADR030): the protected account snapshot stores one pending correction with its question, snapshot/refresh choice and exact run/revision. A different run hides it; returning restores it. A newer brief leaves saved text visible but blocks submission until the user explicitly reviews and rebases. Successful correction admission clears the draft with the child transition. Invalid local draft metadata is discarded without discarding a valid report. Native Expo Go cold-restart and recovered refresh submission are recorded in verification/v6/correction-draft; process death before acknowledged persistence remains unguaranteed.

W07 admission recovery (ADR031): preserve an immutable protected request journal before upload/admission, with per-file keys and content fingerprints. Retry reconnects and resumes confirmed uploads; unconfirmed files require original bytes after restart. Pending question/route stay fixed. Check or withdraw atomically opens an existing run or withdraws an unused key before editing. Invalid journal prevents ordinary sign-in until explicit cleanup. No upload bytes in saved state. Recovery notices belong in the scrollable conversation, never the horizontal composer. Evidence: verification/v6/admission-retry/.

Reader restoration uses the immutable report ID and protected account envelope. Each mounted reader waits for its card, target block, viewport and content size; it restores once within the legal scroll range. User drag cancels delayed restoration. Returning from a source sheet remounts the report reader; concise-hidden and removed sections explain fallback separately. Stale layout callbacks from another account/report/mount are ignored. Source-sheet internal scroll position is not persisted. ADR034 and reading-position regressions own this behavior. Actual Expo Go synthetic offline-reader evidence retained section 7 and its signed offset through a cold restart (verification/v6/reading-position); initial failed launch/inspection attempts are preserved. This does not prove authenticated API, rotation, screen-reader, standalone or iOS behavior.

Source deletion is an explicit two-step source/version-bound action. Confirming clears cached report/source/run/correction/reading content and selected documents through acknowledged protected storage before DELETE; the composer draft remains. A pending source-ID handle survives restart and blocks report reopening, sharing and new research until confirmation; retry never invents success after a lost response. Picker and attachment entry are blocked during deletion. Invalid pending handles fail closed. File cleanup pending is disclosed separately. ADR036 owns privacy-action persistence and rollback. Native source-deletion confirmation and cleanup of its synthetic protected cache remain unconfirmed because device keyguard prevented access; no unit/storage pass substitutes for that device journey (verification/v6/mobile-source-deletion and native-dependency-compat).

Requested claim verification (ADR037): a current controlled report can recheck its first answer claim against the inspected evidence, with snapshot reuse or selected-source refresh. Feedback is saved privately but is not itself assessed or used as a public query. Persist the exact parent/report/version/claim/policy/note/idempotency identity in protected storage before POST. Unknown admission blocks new research, source deletion and report switching; retry uses the same identity. Check or withdraw supplies the bound original request, then durably adopts an authenticated child GET snapshot before clearing the journal, or confirms withdrawal. Superseded accounts cannot adopt late responses; expired sessions clear private state through the existing session handler. Diagnostic reports do not offer this as verification. Unit/storage/type/export evidence is distinct from the still-unverified native journey.

Mutating follow-up, assumption confirm/replace, and text correction persist a SHA-256-bound journal (`prepared`, `sent`, `accepted`, `adopted`, `rejected`, `withdrawn`) before POST. An unresolved journal blocks a different mutation and survives lost responses, 401, empty mutating bodies, and superseded views; 409 persists `rejected`. Read-only explanations append to the conversation and do not clear a pending mutation. Hermes uses `newId()` rather than `crypto.randomUUID`. Device double-tap remains separately unproven.

RES-04 durable parsing: every extant follow-up, assumption and correction journal is strictly parsed before its phase can authorize retry, adoption, terminal replacement or a new request identity. This includes `adopted`, `rejected` and `withdrawn` records. The record must carry a valid immutable request UUID, exact lowercase SHA-256 payload digest, matching deterministic key, known phase and only the accepted fields allowed for that phase. Confirmation is same-run and its accepted identity must equal the parent; replacement and correction accepted identities remain child-bound and pass RES-01 ownership adoption. A valid terminal record may allow a deliberate new request only after validation; damaged, ambiguous or unsupported legacy state remains byte-preserved and held for explicit cleanup without minting, saving, POSTing, adopting or silently clearing. State admission, New Research, routed follow-up and Android Back gates parse all three non-null journal records before testing any journal phase; a damaged sibling outranks another unresolved record and returns a blocking cleanup state without clearing the raw in-memory record or current run. Valid accepted records resume adoption without a second POST.

RES-01 accepted-child handoff: after `accepted` is durably stored, a child-returning mutation may replace the parent view lease only with a lease captured for the exact authenticated child returned for that saved request. Same-run steering retains the parent lease. The app must obtain and durably store an owned child snapshot before `adopted` can be stored or the journal cleared; a swallowed/lost child GET is not successful adoption. Unrelated navigation, cancellation, account/session change, source invalidation/deletion, or local-write failure still supersedes the operation and leaves the last durable phase recoverable. Saved follow-up, assumption and correction notices retry their bound parent/message or values/evidence policy/key rather than current composer text. Successful adoption preserves a newer unrelated draft and allows the next ordinary request. Local controls are not native acceptance.

Profile is a full-screen destination opened from the header Profile control (no bottom tabs anywhere). Done and Android back return to Research. ProfilePanel owns presentation; App retains session authority and asynchronous account, consent, restore and deletion handlers. Calm sections are avatar/account, Appearance (system/light/dark, installation-local), Privacy (consent switch, processor/data-flow disclosures, deletion), Library → Saved reports, Preferences with Demo mode as a quiet switch retaining fixture/controlled-research identities, and Sign out. Purchases/push remain explicitly unavailable; destructive account deletion stays behind confirmation. No notification capability is implied. Rollback can inline this presentation while preserving handlers, confirmations and privacy wording. Native accessibility/lifecycle acceptance remains separate from rendered callback controls.

W02/W03/W07 configured-route preflight (ADR047): a fresh authenticated settings read must confirm the selected route flag before a new admission journal/upload/admission. Failures preserve existing recovery identities; Check/Withdraw remains available for already accepted requests. This is configuration checking, not readiness or budget assurance. Account/view generations reject stale responses. Root148 mobile controls/types and648module2.18MB JS export pass; actual local API probe passes with zero provider intents. Incorrect initial session-endpoint mock proof is explicitly rejected and preserved. No native journey or live semantic claim. Evidence: verification/v6/admission-preflight/ and admission-preflight-integrated/.

W06/W07 document correction (ADR048): a controlled report can add PDF/TXT/MD files without changing its question, within the existing three-document total. Show that prior evidence is retained and the answer is reassessed. Require fresh boolean route and append capability before upload. Persist exact parent/revision/upload identities and confirmed IDs in a separate protected journal; never persist picker bytes. The saved action offers original-file reselection/retry and Check/Withdraw. Recover accepted actions through the exact bound resolver and actual child GET before clearing the journal. A withdrawn action does not pretend its uploaded copies were deleted. Block conflicting new research/source deletion/verification until recovery; account/logout/deletion clears private state. Transient files are bound to account and parent immediately, not only through a delayed effect. Current local controls/JS export are not native accessibility or complete device acceptance.

W03/W07 remote source invalidation (ADR050): an authoritative owned snapshot with `contentInvalidated:true` hides current/previous report, source, correction text, events, selected documents and reading anchor before ancillary requests. Preserve independent composer and exact pending journals. Protected account/run cleanup markers survive interrupted storage writes; current UI remains hidden and disk cleanup stays explicitly unconfirmed on failure. Successful cleanup removes the marker, retains a redacted tombstoned snapshot and permits offline reopening without a false cleanup hold. Late coalesced/required writes cannot restore content in the owning session. Account/view/run guards and explicit opening state handle stale responses and responses before React commits. Invalidated children cannot be adopted as successful correction/verification recovery. Current unit/storage and JS export receipts are not native erasure or complete lifecycle proof.

W03/W07 native regression follow-up (2026-09-18): bodyless authenticated DELETE/GET requests do not advertise an empty JSON document; JSON posts and binary upload content types remain explicit. Report activity is derived from authoritative run lifecycle, never only cached screen status. Invalidated research has an unavailable notice and no spinner/Cancel, including offline protected restart; ordinary terminal outcomes without reports also show ended/cancelled/failed notices. Pending deletion and storage-cleanup holds remain fail-closed. Android14 debug emulator evidence covers synthetic actual-PDF report/source/correction/append/share-preview and deletion/account cleanup, with fabricated model judgments; broader physical/iOS/accessibility and hosted identity remain unpassed.

W03/W07 expiry and reconnect recovery (ADR053): successful expired-session startup cleanup permits a fresh sign-in without requiring a second manual logout. Sign-in remains blocked during cleanup and after any storage failure; a superseded cleanup cannot change a newer account's readiness or error. Premature sign-in attempts show the existing restoration/cleanup explanation in Settings. After an owned invalidated-run refresh and acknowledged local redaction, clear a stale offline banner while retaining the deleted-source notice and unavailable report state. Do not fetch a deleted report to prove connectivity. Link `apps/mobile/test/auth-recovery.test.ts` for pending/success/failure/supersession, visible denials and reconnect regressions. Physical defects were reproduced; post-fix native evidence is separately recorded in `verification/v6/physical-unlocked/`, and neither local controls nor Expo Go establish hosted identity or standalone/iOS acceptance.

W07 source-reader focus: opening a citation moves accessibility focus to the source heading after mount; closing restores the invoking citation after the report remounts. Account/report/view generations invalidate stale callbacks. Android Back consumes a handled source/profile transition synchronously, independent of deferred React state updates. Local component/state tests do not establish VoiceOver/TalkBack device behavior.

## Norrow guest-first mobile handoff (ADR079; local implementation checkpoint)

The first consented text/public-URL question uses a server-issued guest proof held separately in native secure storage. A protected first-request ID resolves uncertain admission before any resend. Guest reading, cited source inspection, progress and cancellation use only the narrow proof-route allowlist; guest content and proof are discarded on a fresh installation before keychain restoration. Server consent revocation with a newer control version disables a further send without erasing the reader or draft.

On a second Send—including a routed follow-up or exact pending clarification—the client saves the draft, first reader position, payload digest, submission ID and guest context before registering the action. The sheet opens only after the server confirms that registration. Apple, Google and email-code controls are available only with configured Clerk and server capability/legal-URL readback; the current client does not claim a provider is configured. Each provider/email challenge uses one durable auth-attempt ID: confirmed server begin precedes SDK work, code resend/verify retains the ID, and close/cancel must confirm server end before a new attempt. On cold restart with an `authenticating` journal, the app first resolves that exact server attempt: a verified Clerk session may finish the original action once, while an unsigned restart confirms server cancellation before opening another provider attempt for the same submission. Unknown resolution/end remains held and dismissible; a late reply after unmount cannot claim. An uncertain end or lost claim/continuation reply remains held for original-ID resolution, never an invented resend. Closing the sheet preserves the exact reader and draft, clears any one-time code, and returns focus to the composer. The resend control respects its cooldown clock. Visible provider marks use supported native vector icons, not Unicode approximations.

Verified Clerk identity is checked by the backend before local adoption. Claim requires the same auth-attempt ID plus guest proof and member bearer; after claim, resolution and continuation use member bearer only. A server-confirmed child is saved under the member account before the terminal guest journal is recorded and the guest proof is cleared. On restoration under another verified member, a guest journal already bound to the first member stays encrypted and untouched; its reader, draft and proof are not mounted under the other account. A pending second action blocks New Research, ordinary third Send, correction, verification and clarification paths. An edited draft is not silently submitted as the saved action: explicit Send first obtains the exact server cancel (preclaim) or abandon (postclaim) receipt, durably retains the cancelled old journal, and only then creates a fresh submission. A failed or unknown abandonment keeps the old ID held. If member snapshot cleanup fails after a confirmed abandon, the cancelled journal still blocks ordinary Send until that cleanup is retried. If the claimed action was ordinary research or a follow-up, edited text starts ordinary member research in a new conversation with a fresh request ID. If it was a clarification, the replacement remains a clarification: a new journal ID and digest retain the exact pending-input ID, field and brief revision, then member-only registration and current authority/consent/allowance readback precede one continuation of the saved answer. Unknown registration retries the same ID and digest; unknown continuation resolves its original ID without a blind repost. Guest consent does not become member consent on sign-in or cold restoration; a missing member consent or allowance retains the claimed action for explicit consent/funding and fresh claim readback. The next ordinary message after a dispatched second action uses the returned member conversation ID and a fresh request ID. Dismissal during an in-flight claim disables automatic continuation; a late receipt rereads the current journal and cannot restore auto-resume. Cold-start Clerk restoration owns a fresh request-scope lease after activating the verified member session. The client gets a current Clerk token before new member requests; a same-account refresh rotates only the credential, preserving reader and journal scope. On an expired-token 401 it forces one refresh but does not replay a potentially accepted mutation; a genuinely rejected current credential still expires the local session.

Local TypeScript, synthetic transport/journal, mounted App and rendered sheet tests are not native, configured-provider, hosted-identity or paid-research evidence. Guest deletion uses the exact proof-only `DELETE /v1/guest` route; member deletion never receives guest proof. Rollback disables guest bootstrap/second-send UI while retaining saved journals, server ownership/consent/allowance/deletion gates and resolve-only recovery identities; it must not enable development-session sign-in.

AUTH-06/CLAIM-14 credential continuity: each member multi-step action binds its internal account and principal epoch, then takes the current verified Clerk bearer at each API and protected-storage boundary. This includes admission preflight/save/upload/adopt, claim/edited clarification/resume, source deletion, correction and verification journals, and consent revocation. The protected store still rejects an old token; a routine same-account rotation does not change the saved action ID, reader or view lease. A different Clerk subject or Clerk sign-out while a member reader is mounted immediately hides that reader and invalidates callbacks before device cleanup; it never mounts the old reader under the new subject or adopts the new subject without backend verification. A lost or uncertain mutation remains resolve-only. Mounted synthetic tests cover mid-preflight, claim-time and edited-clarification rotations, external subject switch/sign-out, and stale callback suppression. These are not native or real-provider results. Rollback must keep the exact storage-token and principal/view fences even if the refreshed-token handoff is disabled.

Build activation remains separate: `App` reads `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` at Expo bundle time and otherwise mounts `auth=null` with providers unavailable. A `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` in the repository-root local environment does not supply that Expo variable, and the guarded device EAS wrapper intentionally strips arbitrary process variables. The approved EAS project/profile environment must explicitly provide this project's Expo-prefixed publishable key, together with a reachable API and confirmed legal/provider capability readback, before a native sign-in claim is possible. Do not embed the key in source or silently substitute another project's key; no EAS/provider build result is implied here.

PROVIDER-08 synthetic host behavior: the same server-confirmed email auth-attempt ID is retained on resend and code verification. A structured Clerk rate-limit failure holds a resend at an unknown retry time and forbids another code dispatch; an expired code confirms server cancellation before allowing a fresh attempt ID and clears the old code. Incorrect codes display a safe retry message without claiming; raw provider details are not rendered. Mounted App tests exercise resend limit, expiry, provider cancellation, repeat open/close, exact journal retention and draft preservation; reducer/sheet tests exercise countdown and input reset. These do not prove a live provider, native keyboard or screen-reader behavior.

Google native configuration is a separate activation gate. The installed `@clerk/expo-google-signin` plugin returns the Expo config unchanged when `EXPO_PUBLIC_CLERK_GOOGLE_IOS_URL_SCHEME` is absent, while the installed Android hook/module require `EXPO_PUBLIC_CLERK_GOOGLE_WEB_CLIENT_ID` at runtime (and iOS requires its iOS client ID). None of those variables is declared in the current EAS profiles. This observation does not establish a prebuild failure or a configured live Google sign-in; fail closed until the approved Expo build environment supplies the matching nonsecret client identifiers.
