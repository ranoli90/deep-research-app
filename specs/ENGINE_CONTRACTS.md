# Research engine contracts — revision 3
Owner role: research/backend lead. Status: normative design to implement; no runtime schemas or application tests executed. Reviewed: 2026-09-16.

Implementation checkpoint 2026-09-17: the repository now has an application; historical design status above is not a test result. W02 adds durable dispatch, atomic admission/reservation and worker attempt fences; W04 replaces the controlled fetch prefix with offline extraction. `source_versions`, `extraction_receipts`, `evidence_artifacts` and immutable block locators retain transport/digest/extractor provenance. HTTP failure text cannot become passages. HTML reading is partial, not full-document comprehension. `apps/backend/extraction/README.md` records the selected runtime and remaining layout/OCR/coverage limitations. Exact evidence and failures are in `verification/v6/RESULTS.json`; the entire W01–W09 milestone remains incomplete.

## 1. Scope and trust
These contracts refine the original evidence-first design. In a real repository, validated executable schemas become the source of truth and documentation is generated or checked against them. Do not maintain two independent schema definitions. Version external contracts and inspect installed clients before changing them.

Every persisted entity has an ID, an explicit tenant/ownership path, timestamps and an applicable schema version. IDs are opaque but are not authorization. Store UTC instants, preserve user timezone/date-only precision, and never invent a publication time from a date. Use exact decimals/integer micro-units with currency for money; preserve scientific units and significant assumptions.

Separate model suggestions from controller authorization. Sources, attachments, retrieved instructions and model outputs cannot change tools, permissions, privacy, spend or completion gates. Actions are read-only except internal state persistence and authorized report/account operations.

## 2. Task and decision representation
`ResearchBrief`: id, conversationId, originalQuestion, desiredOutcome, audience, language, geography, timeRange, freshnessRequirements, outputPreferences, attachmentIds, sourceRestrictions, nonGoals, constraints[], assumptions[], budgetPolicyId, consentPolicyVersion, revision.

`Constraint`: id, field, operator, value, units, origin (`explicit|confirmed|assumed`), importance (`hard|preference`), explanation and appliesTo. Hard constraints are never silently relaxed. `Assumption`: id, value, reversibility, impact and userConfirmationState. A safe assumption is visible and editable; uncertainty that could materially change authorized processing or the decision is clarified.

`DecisionFrame` for comparisons: candidateScope, criteria, hardConstraints, allowedTradeoffs, candidateDiscoveryStatus (`open|bounded-complete|incomplete`), and disqualifyingConditions. Do not impose this schema on every open-ended question; general synthesis uses coverage without recommendations.

`Candidate`: id, identity/version/region, discoveredFrom, currentScope and source evidence. `FeasibilityCheck`: candidateId, constraintId, status (`satisfies|violates|unknown|not-applicable`), evidenceIds, validAsOf and checker method. A missing fact is unknown, not satisfies. Empty search results do not establish no feasible option exists.

## 3. Coverage, questions and gaps
`CoverageItem`: id, question, taskRequirementIds, importance, requiredEvidenceDescription, dependencies, status (`unstarted|investigating|supported|qualified|blocked|excluded-by-user`), supportingIds and limitation. A `qualified` item must state what was established and why uncertainty remains. It is not a synonym for “not attempted.”

`QuestionNode`: id, parentId, coverageIds, originTrigger, scope, attemptedActions, lastUsefulEvidenceAt, currentHypotheses, unresolvedContradictions and nextActionCandidate. Avoid duplicate semantic questions, but retain different dates/populations/versions.

`EvidenceGap`: id, questionId, missingFact, whyItCouldChangeAnswer, affectedCandidateIds/claimIds, importance (`blocking|material|background`), sourceTypeNeeded, accessibleRoutesTried, latestOutcome and stopReason. The model’s question plan is useful but not complete truth. Preserve a bounded discovery/counterexample pass for missed alternatives. Evaluation uses independently identified decisive evidence, not only model-authored coverage.

Clarification is justified when an unresolved field is blocking and cannot be handled safely with a reversible assumption. Record the field, expected benefit, options and default. An exhaustive interview is not the default. A very large requested artifact is admitted as a bounded useful scope with disclosed limits, not promised and then abandoned.

## 4. Action contract and selection policy
Allowed actions: `clarify`, `search`, `fetch`, `extract_text`, `extract_table`, `inspect_visual`, `compare`, `calculate`, `verify`, `challenge`, `replan`, `synthesize`, `stop`. `extract_table` and `inspect_visual` are unavailable until their real adapters/tests exist; explicit capability flags reject use rather than fabricate results. `challenge` is the disconfirmation action (search for evidence that would make a material conclusion wrong). The versioned projection is `research-controller.v1`. Model proposes; `admitProposedAction` decides. The live default selector is `selectAdaptiveAction`; `selectBaselineAction` remains the bounded comparison arm.

Every action contains actionId, runId, briefRevision, coverageIds, gapId if applicable, validated arguments, short observable rationale, estimated maximum resource demand where enforceable, source access constraints and dedupeKey. The controller authorizes it with the current revision basis and atomically reserved budget. It rejects privileged fields proposed by the model.

Policy is a lexicographic decision rule, not a mystical confidence score:
1. Reject unsafe, unauthorized, out-of-scope, duplicate or over-budget actions.
2. Resolve a blocking ambiguity or missing eligibility fact before cosmetic report completeness.
3. Inspect an already-known likely primary source before issuing another generic query.
4. If extraction is incomplete, change extraction method or disclose access limits; do not ask another model to guess the missing table.
5. For a consequential contradiction, align dates, definitions, denominators, populations and versions before treating it as disagreement.
6. If the current route repeats the same source families without changing coverage, switch source type/terminology or stop that branch.
7. Permit bounded disconfirming evidence and candidate discovery when it can change a material conclusion.
8. Reserve verification/writing resources; synthesize when further authorized action has low expected marginal value.

Initial saturation heuristic: after two materially different queries produce no new relevant source family and no coverage progress, re-evaluate rather than blindly repeat. This is a configurable engineering starting rule to test, not an empirically optimal threshold. “New source” does not itself mean useful progress.

Source escalation examples: comparison page -> vendor compatibility matrix; technical blog -> underlying paper or repository issue; aggregate number -> methodology/table; current page -> authorized archive/version history; public assertion -> provided contract/manual. Specialist databases may require credentials/licensing; represent that barrier, do not route around access control.

`ActionOutcome`: success/partial/failed/blocked, receiptIds, accessedSourceIds, evidenceAdded, gapsResolved/created, coverageDelta, costState, nextStepReason. A pivot needs trigger evidence, previous approach, new source route, affected scope and a bounded action. No endless self-dialogue. Expose concise decisions and observable actions, never private chain-of-thought transcripts.

### V6 report/support implementation checkpoint (2026-09-17)

`literal-scope-v3` never uses word overlap as positive entailment. Complete literal statements and a narrow controlled passive paraphrase can pass; other wording remains unsupported/context-only until a substantive semantic assessment exists. Claim bindings cover block text exactly. Unknown/duplicate claims, unmapped factual headings/caveats and evidence-free type-label claims are rejected. A closed set of non-assertive section labels and fixed abstentions can omit claims.

Publication reloads owned passages and atomically stores claim revisions, text/evidence/scope digests, checker version and actual decisions. Pure deterministic report derivations are recomputed from the owned brief/evidence and recorded with input/output digests; they are not model verification receipts. The draft compiler localizes unsupported sections rather than leave unsupported prose or bypass publication. Candidate extraction has no fixture-name catalogue, but remains bounded literal grammar, not arbitrary semantic extraction. Missing evidence stays unknown; price variants retain sentence scope. Source-presence coverage and several event-only action handlers remain unrepaired and cannot establish research completion.

## 5. Evidence and source independence
`Source`: id, tenantScope, canonicalLocator, originalLocator, publisher, author when known, title, sourceType, language, publicationDate/precision, eventDate/precision, originCluster and rights/retentionClass.

`SourceVersion`: id, sourceId, retrievedAt, finalLocator, redirect metadata, contentHash when available, MIME, extractionVersion, accessLevel (`discovered|snippet|abstract|partial-text|full-text|visual-inspected|blocked|failed`), qualityWarnings, textCoverage and authorized artifact pointer. These states describe inspected material, not truthfulness. No blanket “fully read” from successful HTTP alone.

`Passage`: id, sourceVersionId, exact limited text or structured table cell values, locator, extractionMethod, qualityWarnings, scope qualifiers and optional labeled translation with original. Locators include page/section/cell and normalized text offsets or anchors. Hashes establish content identity, not factual correctness.

`SourceFamily`: id, originalWork/source relationship, relation (`same-document|syndicated|quotes|derived-from|independent-unknown`), evidenceOfRelationship. Several sites repeating a press release do not create independent support. Independent-unknown remains unknown; do not infer independence only from domain names. Title/snippet clustering may assign a cluster when `originCluster` is only a URL origin.

Public query terms are provenance-classified as `user-public`, `safe-application-derived`, `public-evidence-derived`, or `private-document-derived` (`query-provenance.v1`). Safe expansions (synonyms, abbreviations, canonical names, source-type qualifiers) are application-derived from a bounded lexicon after the model proposal has been bound to the original question span. Private-document-derived terms never enter a public search adapter without an explicit `query_authorizations.kind='approved'` row. Source-text bait cannot authorize a query. Mixed-document tasks without that approval remain `document_search_requires_public_query_approval`.

Source-type planning (`source-type-plan.v1`) selects a preferred evidence class per criterion (first-party pricing, vendor docs/release notes/repository tests, statute/regulator, primary literature, filings, docs/source/benchmarks, independent review). Adaptive breadth (`evidence-value-breadth.v1`) continues only while an unresolved consequential criterion can change the answer, a distinct strategy remains, and the existing discovery ceiling (`MAX_DISCOVERY_QUERIES=3`) and budget hold. Empty search results are recorded as coverage/`unresolvedAbsence`; they do not mean nonexistence.

Whole-passage-selection.v1 remains the stored proof. Neighbor bundles also include parent headings, nearby tables, footnotes, exceptions and date/version context when locators encode those kinds. Selection ranks relevance, not truth; omitted inventory stays unassessed. Criterion-specific freshness (`criterion-freshness.v1`) distinguishes current price (hours/days), current effective law, applicable software version, contemporaneous historical evidence, and dated scientific method. Document/web reconciliation (`document-web-reconciliation.v1`) returns `confirmed|partially_confirmed|contradicted|outdated|unverifiable|blocked_by_access` with source scope and a permission requirement for private-only terms. It does not summarize an uploaded document as the answer.

`Claim`: id, text, type (`external-fact|user-provided|calculation|inference|conditional-conclusion|limitation`), materiality, scope/date/units, supportStatus (`direct|inference|disputed|unverified`), evidenceLinks and validation records. `ClaimEvidence`: claimId, passageId, relation (`supports|contradicts|qualifies|context-only`), checkerVersion, decision and concise support explanation. Context-only cannot support the displayed assertion.

Citation integrity has distinct checks: referenced ID exists; user owns access; locator resolves; actual passage supports scope of statement; consequential uncited claims are detected; inference does not overreach. A working link is not verification, and a supporting source can itself be wrong. Model agreement is not independent confirmation.

V6 W01 implementation: the validator preserves missing-claim, unmapped-block, contradiction, qualification, ownership and source-version failures through the report wrapper. Publication reloads passages through their run/account/source ownership chain and rejects altered caller text/version. This is a deterministic safety layer, not a calibrated semantic verifier. Revision-scoped assessments, complete factual-clause coverage and legacy composer bindings remain open; passing the isolated regressions does not close those requirements. Rollback must keep these rejections and disable unsafe generation rather than restore false acceptance.

## 6. Calculations and contradictions
`Calculation`: id, named formula/version, inputs with source/claim IDs, exact units/currency/time basis, transformation/rounding, output, error bounds when justified, executionReceipt. Use deterministic bounded arithmetic; no arbitrary model-generated shell/code in production. Missing inputs stay unknown. A sensitivity range may be more useful than invented precision.

`Contradiction`: id, proposition, claimIds, alignedScope, mismatchDimensions, competingExplanations, evidenceNeeded and resolution (`scope-difference|evidence-favors-one|unresolved|withdrawn`). Source count does not settle a dispute. Preserve a supported alternative when evidence remains genuinely ambiguous, without pretending all alternatives have equal support.

## 7. Dependency-aware changes
Persist typed relational edges: constraint -> candidate selection/feasibility; sourceVersion -> passage; passage -> claim; claim/input -> calculation; claim/calculation -> conclusion; conclusion/claim -> report block. Reject invalid owner-crossing edges and dependency cycles where the relation requires an acyclic graph. This is a bounded provenance/impact graph implemented in PostgreSQL, not a knowledge-graph platform.

A correction creates a new brief/evidence revision and an `ImpactSet`: changed inputs, invalidated objects, reopened discovery scopes, preserved objects, reason and dependencyCompleteness (`known|partial|unknown`). If dependencies are incomplete, conservatively rerun the affected branch or full bounded task. Do not sell incremental efficiency at the cost of silently preserving wrong findings.

| Change | Required response |
|---|---|
| A cited number/date is corrected | Recompute dependent calculations and conclusions, then affected blocks; preserve unrelated evidence if freshness still fits. |
| Hard constraint is relaxed/broadened | Reopen candidate discovery, including options previously excluded. Re-ranking only old candidates is insufficient. |
| Constraint is tightened | Recheck all displayed feasibility statuses and conditionally remove choices; new discovery may still be useful if all old options fail. |
| New document is supplied | Parse under consent, identify relevant support/conflicts; do not blindly replace all sources or assume new is correct. |
| A source becomes unavailable | Preserve permitted historical evidence with last-access date; mark inability to refresh. Disappearance alone does not falsify an earlier observation. |
| Private source is deleted or consent revoked | Stop new processing, tombstone dependents, purge/redact prohibited content including historical report copies/caches; retain only allowed non-content audit metadata. |
| User asks only for a different format | Render the same claim set without pretending new research occurred; do not incur an expensive full search by default. |

`ChangeSummary` is semantic: evidence updated, conclusion changed, newly feasible/infeasible option, uncertainty increased/decreased, unchanged material. It references affected block/claim IDs. Do not regenerate an unrelated narrative pretending every sentence changed scientifically.

## 8. Durable lifecycle and phases
Internally distinguish lifecycle (`queued|running|awaiting_input|cancelling|terminal`), phase (`preparing|researching|verifying|writing`) and terminalOutcome (`completed|completed_with_limitations|cancelled|failed`). This avoids conflating a loop phase with run finality. If original external labels exist, map them through a versioned contract: queued, preparing, researching, verifying, writing, awaiting_input, cancelling, completed, completed_with_limitations, cancelled, failed.

Every nonterminal run, including writing and awaiting_input, can be cancelled. Awaiting input is persisted, not an open HTTP request. Completed output can be inspected but a late cancel cannot retroactively double-refund or destroy it. A deletion request is a separate privacy operation and can override retained content.

V6 local deletion implementation: the account lock serializes deletion with admission, attachment storage, correction/follow-up/clarification writes, challenges and worker content transactions. Purge includes original briefs, events/checkpoints/controller state, source metadata/locators, passages, support/derivation records, candidates/gaps/calculations/disconfirmations, challenge excerpts, all report versions and raw database attachment bytes. Minimal tombstones and amount/state/opaque provider receipt identifiers remain for already-issued accounting; unknown spend reservations are not released as if confirmed zero. New attachment bytes commit with their metadata, avoiding the prior file-before-row orphan window. Legacy flat-key files are claimed through `file_deletion_outbox`, unlinked outside transactions, and retried after storage failure or lost acknowledgement. Only the exact generated owned key under configured storage is authorized. API returns `fileCleanupPending` when storage cleanup remains outstanding. Worker startup/periodic replay upgrades prior deletion tombstones via `deletion_cleanup_version`; hosted backup restoration and external copies remain separate evidence gates. Rollback must retain these fences, tombstones and cleanup processing.

Allowed loop: preparation -> investigation -> verification -> writing. Verification may return to investigation for a named material gap with remaining budget; no infinite writer/critic ping-pong. Completion-with-limitations is a useful outcome with explicit blockers, distinct from failed delivery.

## 9. Fences, idempotency and recovery
A `RevisionBasis` contains briefRevision, evidenceRevision, consentEpoch, cancellationEpoch and workerLeaseFence. Load it at action planning, recheck at authorization, and compare atomically before publication. A lease fence is monotonic; an expired worker cannot overwrite the new owner’s result even if its network call finishes later.

Admission validates identity/ownership/consent and atomically records run, user allowance reservation and enqueue/outbox. A duplicate idempotency key with the same canonical request returns the accepted run; the same key with different payload is a conflict. Workers checkpoint evidence and task intent independently of report generation.

V6 identity boundary: production uses the configured Supabase Auth `/auth/v1/user` verification endpoint, not the development session table or client-decoded claims. HTTPS origin and publishable key are required at startup; redirects, oversized/malformed responses, unconfirmed/anonymous/non-user identities and outages fail closed. Provider outages return temporary unavailability rather than invalidating a local session. No bearer token, email or editable user metadata is persisted by identity mapping. The verified issuer/subject digest maps atomically to one server-generated owner; new identities receive zero automatic allowance. The digest survives local deletion solely to deny account resurrection. `/v1/session` returns the resolved local account ID. Follow-up/challenge claim IDs must belong to the owned current/selected report, not merely exist in a claim table. Production login/refresh UI, provider-side identity deletion and hosted Auth verification are still required before real-user activation; current tests establish the adapter/database boundary using transport doubles, not provider cryptography.

Paid calls persist intent before issuance: correlation ID, route/capability version, request digest, scope, reserved maximum, state (`planned|issued|confirmed|failed|outcome-unknown`). A timeout after issuance is not proof of no charge or no work. Query provider status when supported; otherwise keep uncertainty visible and apply a conservative documented reconciliation/refund policy. Do not blindly retry and debit the user twice.

Use bounded retry/jitter for genuinely transient failures, permanent errors for unsupported access/arguments, and explicit manual redrive with lineage. Every redrive rechecks consent, deletion, scope and remaining budget. Dead-letter storage is not an exception to content retention.

Publish report blocks, citation map, run outcome and completion-notification outbox in one transaction guarded by the revision basis. If stale, do not publish. Reconcile late receipts/costs separately if permitted without resurrecting private content. This promises idempotent application effects where tested, not exactly-once external computation (S40).

## 10. Mobile synchronization and events
Events persist internally: id, runId, sequence, createdAt, schemaVersion, type, publicSummary, phase, payload, relatedSource/Claim/Report IDs and snapshotRevision. Persist only observed milestones. Discovered/read/cited counts have separate definitions. No invented completion percentage or fictional agent conversation.

Consumer `GET /v1/runs/:id/events` returns versioned `public-activity.v1` sanitized events: id, runId, sequence, createdAt, schemaVersion, phase, and `activity` (typed public DTO or null). Raw `type`, `publicSummary`, payloads, chain-of-thought, and URL paths are not returned. Envelope `phase` is one of preparing|researching|verifying|writing; unknown or oversize stored phases become canned `researching` and never echo the raw value. `activity.sourceDomain` uses the same public-host rules as `publicSourceUrl` (no RFC1918, `.internal`, localhost, or credentialed URLs). Mobile progress UI may consume only `activity`.

Initial transport: authenticated snapshot plus cursor polling with backoff and foreground wake refresh. Add streaming only after native runtime/reconnect tests prove benefit; streaming is disposable. Cursor gaps return a current snapshot and a gap marker. The client deduplicates events by ID and reconciles against server revisions.

Optional push says only that a report is ready, without query/title/source text. Notification permission is not required for research. A push is not proof the client downloaded the report; opening authenticates the active account and fetches authoritative state.

**Completion identity.** Allocate monotonic per-run `completionEpoch` atomically when a new report version is successfully published. Duplicate publication attempts reuse the same existing publication identity; retries must never increment it. A later genuine report publication receives a new epoch. Unique logical notification key is the typed tuple `(runId, completionEpoch)` within its tenant/run ownership constraint, not ambiguous string concatenation. Create that logical outbox row in the publication transaction.

**Delivery identity.** Fan-out, if enabled, uses a unique row `(notificationId, accountId, deviceBindingId, bindingEpoch, channel)`. The binding identifies a particular authorized login/device association, not just a reusable push token. Recheck ownership, active binding epoch, consent and deletion immediately before dispatch. Stop unsent rows on logout/deletion; expire stale tokens. A second legitimate device may receive its own delivery, while token rotation or retry must not create a new logical completion.

**Retries and unknown outcomes.** Persist dispatch intent, attempt status and any provider ticket/receipt. Use the same provider idempotency key only where the adapter verifies support. Provider collapse identifiers can reduce repeated presentation but do not prove exactly-once delivery. After a crash/timeout following issuance, reconcile via supported receipts when possible. If outcome remains unknown and provider idempotency is unavailable, default to no blind resend for this optional notification: mark `outcome_unknown`, retain one logical in-app completion, and rely on foreground synchronization. Retry only documented known-nonaccepted/transient outcomes with bounded attempts; a missing receipt is not proof of nonacceptance.

**Guarantee boundary.** We can enforce one logical completion/outbox entry and per-binding application delivery record. We cannot guarantee exactly one OS-visible alert or guaranteed device delivery across external push services. Expo explicitly allows duplicate or missing handoffs; APNs is best effort (N01–N03, `research/RECONCILIATION_SOURCES.json`). Store normalized provider states `issued|accepted|rejected|outcome_unknown`, never label an accepted handoff `device_delivered` without an actual device acknowledgement. Client in-app events deduplicate by logical identity; OS-level display may bypass app code.

**Logout/privacy.** Clear the prior account's cached reports and app-controlled notification state; remove delivered/pending local notifications when platform APIs support it. A generic remote alert already in flight cannot be reliably recalled. Its deep link must reauthorize and show a safe unavailable state for the wrong account or deleted report. The notification must not carry sensitive report data, and account switching must not allow a late alert to fetch the prior account's report.

P0 implements publication/outbox identity and deterministic fault tests only if an outbox is present; no paid/provider push credentials are needed. Real push transport is a separate P3 feature gate, not a prerequisite for durable research. Acceptance: J02/J14/S12; never reinterpret those as an external exactly-once guarantee.

## 11. Public API contract to implement
| Operation | Required behavior |
|---|---|
| POST /v1/runs | Auth, consent, schema, attachment ownership, idempotency key, atomic admission; returns run ID and accepted snapshot. |
| GET /v1/runs/:id | Ownership, current phase/outcome, revision basis safe subset, next action and preserved artifacts. |
| GET /v1/runs/:id/events?after=cursor | Ownership; ordered paged events; retention-gap signal; never another user’s diagnostics. |
| POST /v1/runs/:id/corrections | Expected briefRevision and idempotency key; conflict on stale edit; new revision/impact request. |
| POST /v1/runs/:id/cancel | Idempotent request; phase-independent cancellation policy and current authoritative outcome. |
| POST /v1/runs/:id/continue | Explicit scope/allowance policy and valid consent; child run lineage; not automatic resurrection. |
| POST /v1/attachments; POST /v1/attachments/bytes; GET /v1/attachments/:id | Explicit pasted notes or bounded binary upload, MIME/magic/UTF-8/size validation, owned processing metadata and honest extraction limits. |
| GET /v1/reports/:id and /sources/:id | Ownership/retention; canonical blocks, evidence access labels and signed short-lived artifact URLs where needed. |
| POST /v1/reports/:id/challenges | Claim/block reference, feedback category and task scope; not unrestricted raw SQL or source instructions. |
| Account/consent/export/purchase endpoints | Separate verified authorized operations, deletion precedence, server purchase verification and replay protection. |

Errors have stable code, user-safe message, retryability, correlation ID and preserved-state summary. Distinguish invalid_input, permission_denied, consent_required, stale_revision, capacity_unavailable, allowance_exhausted, source_unavailable, extraction_partial, provider_outcome_unknown and internal_failure. Do not expose secrets or raw stack traces.

## 12. Reports and context
Canonical report: reportId, version, runId, basis, outcome, blocks with stable IDs, claim/citation links, coverage outcomes, calculation links, limitations, source-access summary and changeSummary. Text/heading/list/table/quote/code blocks are safely rendered; arbitrary HTML is forbidden.

W07 / V6-F18 Markdown export renders those same canonical blocks and escaped canonical limitations with ordered footnote references and a Sources bibliography. Each cited passage resolves through authorized_run_passages for the exact report account/run, including digest-pinned reused evidence. One database statement reads the report and its source metadata under the same snapshot. Entries preserve title/publisher, safe public HTTP(S) link when available, retrieval time, access/coverage, extraction method, document/page/block locator and full passage/source-version identities. Missing or unauthorized evidence is explicitly unavailable; private/credentialed/unsafe locators never become links. Source metadata is escaped as text. Repeated citations share one bibliography entry; matching UUID prefixes cannot collide. No PDF export, raw-document sharing, new schema/dependency/migration/model route or monetary authority is introduced. Requirement tests: report-export.unit.test.ts and E09 in p3-remaining.integration.test.ts, including wrong-account, absent membership and changed-digest controls. Rollback disables the export endpoint while retaining canonical reports and all ownership/deletion gates; abbreviated IDs alone do not satisfy E09.

The answer-first view and expanded report reference the same claim set. Report blocks preserve enough semantic identity for stable reading anchors. Store bookmarks as reportVersion + blockId + offset; map across versions only when valid, otherwise explain the moved/removed section rather than resetting silently.

Within-run memory is structured evidence and task state, not a profile. Context assembly selects relevant passages, active constraints and unresolved gaps. Preserve traceable IDs during compression and test that contradictions/negative evidence are not dropped. Reuse answers only within authorized context/freshness; do not share private cache text between users.

## 13. Stop policies and resource limits
Successful completion: all critical task items have supported or explicitly qualified outcomes, hard constraints are preserved, citations resolve, computations pass and material conflicts are addressed. Useful partial: report names remaining access/budget/input blockers and preserved findings. Failure: execution cannot produce the declared useful artifact. Cancellation: user stopped work; no new paid actions issued afterward except unavoidable in-flight accounting.

Depth is governed by decision sensitivity and evidence accessibility, not target words or citations. Initial concurrent investigation limit is one; permit two independent branches only behind an evaluated policy. Numeric per-route budgets are selected during a real authorized capability/cost spike, not invented here. Reserve a fixed configured finishing allowance before exploration; test it against actual task costs and adjust openly.

Gateway/model/search adapters must represent capabilities as supported/unsupported/unknown with checkedAt, test evidence and exact effective processor when known. Unsupported cancellation or internal search limits remain unsupported. A route incompatible with promised privacy or enforceable spending is rejected, not silently substituted. See `docs/SECURITY_PRIVACY_COST.md` for the ledger and privacy policy.


### V6 W04 binary evidence implementation
Migration 013 stores scoped attachment extraction JSON beside immutable original bytes. PDF-like filenames cannot turn pasted text into PDF evidence. Bytes may be stored before research; only a successful, owned, fence-checked extraction joins run evidence. Parsing holds no database transaction. Failed/unsupported input persists an explicit unavailable source with no supporting passages, never an error body as evidence. The selected Docling Parse 7.20.0 geometry adapter records actual page indexes and text-cell rectangles; pypdf 6.19.0 is only strict metadata/encryption validation. No OCR/table-layout model runs. PDF results remain partial. Changes of extractor version invalidate attachment extraction-cache reuse; old pypdf text results cannot bypass the corrected decoder. Source inspection returns sourceVersionId, passageLocator, extractionMethod, coverage and warnings. Content deletion clears every copy of extraction JSON/bytes. Existing general coverage/completion gaps remain open; a file's presence or parser completion does not satisfy a criterion.


Executable argument boundary (W02/F15): `packages/contracts/src/action-arguments.ts` owns strict bounded argument schemas. Admission rejects undeclared keys before transformations and admits the actual transformed executable action again. Proposals cannot supply authority, arbitrary code or verification outcomes. Schema validity alone does not establish named-action execution or evidential support.

Provider reservation or request errors cannot become successful search progress. This includes adapter-returned failed/unknown receipts and resumption of an issued attempt without stored search output: these terminate as unresolved, with no searched event, evidence-revision bump or invented empty result. A known zero-cost successful response remains a valid completed search. Financial receipt and state persist atomically; subsequent result failure does not overwrite them. The worker persists a sanitized blocked/unresolved reason, terminates that attempt path and settles only known receipts. Unknown outcomes retain allowance. A stored source count or empty fabricated search event cannot stand in for an executed request.

Paid OpenRouter admission locks the server credential bucket before the project bucket and records its opaque digest. Different project scopes cannot bypass one credential cap. Legacy intents without an attributable credential remain conservative held/confirmed cost, never zero. Configured cap zero or missing credential blocks before provider dispatch.

Late supplied provider receipts reconcile under account/run locks independently of worker authority to publish. A stale worker may acknowledge incurred cost but cannot acquire new spending or evidence authority. Null cost remains unknown. Live worker fetch/template paths cannot add fixture tariff intents. Historical settlement basis is never fabricated; explicit cost fields separate confirmed spend, held exposure and simulation.


W05 gateway boundary: `ResearchModelOutputs` owns six strict operation output schemas. Backend JSON Schema is generated from these definitions; account/run IDs, revision, permissions and money never come from model output. Pure `validateModelBindings` checks exact UTF-16 question/evidence spans, handles, target completeness and proposed public-query provenance; it does not claim semantic entailment. Context is an explicit bounded projection of owned passages/source versions. The coordinator reserves before transport, includes schema/prompt/policy plus brief/evidence revisions in logical identity, stores typed results under active fences, and reuses valid recorded output without another request. Unknown attempts are not resent. Missing/corrupt cached results are pending/blocked, never fabricated success. Migration016 results are private derived data and are purged on deletion. The production structured worker now composes these operations with scoped support, coverage and publication gates described below; general semantic quality remains unproven.


W05 task identity (`research-task.v1`): migration017 persists one task per owned run/brief revision, with server-generated criterion/question UUIDs and exact original-question digest. A task adopts only a successful gateway brief result with matching schema/prompt/policy, request digest, financial receipt binding and exact question provenance. A new evidence revision does not change task identity or spend again on interpretation; adoption can recover the already stored proposal after a crash. Corrupt/stale owner, question, IDs, version or receipt bindings fail closed. Material ambiguities/hard unresolved alternatives require clarification. Planning-ready is not evidence-complete: no coverage/support is inferred or written by task preparation. These are still fallible model interpretations; provenance alone does not establish complete or correct intent extraction.
The production processRun path now uses this preparation service before extraction, scoped support, criterion-linked coverage and generic writing. Typed replacement corrections recompute the task; granular edits remain open. Historical heuristic coverage is not this task model. Task rows are private derived content, deleted explicitly and through model-result cascade; rollback preserves schema and disables structured preparation.


W05 assertion extraction service: extractEvidenceAssertions accepts only a nonempty, duplicate-free selection of at most24 owned passage UUIDs and the current task identity. It reads whole immutable passage text/version/hash/access plus source handles inside the run fence, orders selection canonically, and calls the shared extract_assertions gateway outside the transaction. Oversized/unreadable input is blocked rather than prefix-truncated; material task ambiguity blocks extraction. Exact quote, criterion and candidate bindings are locally validated. Returned assertions remain unverified proposals; neither source presence nor extraction completion grants support, coverage or publication. The production worker and substantive scoped checker now consume these proposals; extraction alone never grants support.

Migration018 input_manifest records every selected passage ID/source version/digest/access, source handles and hashes of the task/question/assertion/draft inputs beside each model-operation result. This includes evidence the model omits. Cached results require the identical manifest and receipt binding; passage digests are independently recomputed before dispatch. Brief adoption requires its exact empty-evidence input manifest. Historical NULL manifests remain unknown and cannot be silently upgraded or reused; do not resend an unresolved logical attempt. The manifest is deleted with its model-operation result. It proves selected input membership, not whole-document reading, evidence support or correction reuse.


W05 scoped support execution (`scoped-support.v1`): the worker loads assertions only from a successful owned extraction operation with exact input manifest, current evidence/brief basis and pinned schema/prompt/policy/receipt. It supplies those assertions and whole selected passages to assess_support through the shared durable gateway. The pure resolver executes exact evidence/target binding checks, scope equality and grounding, readable-access requirements, numeric/unit-context checks and contradiction/qualification vetoes. It checks all selected in-scope passages, including those omitted by the model; counterevidence IDs persist. Supported, contradicted, partial, insufficient, out-of-scope, unavailable and disputed remain distinct. Missing evidence cannot become full support. A valid paraphrase may use the model's semantic judgment only after independent guards pass.

These guards are conservative and incomplete natural-language checks; names/scopes/units require literal grounding, and aliases/unit conversions may remain unresolved. The semantic component remains fallible and uncalibrated on independent real-model held-out tasks. No human adjudication or live semantic superiority is claimed. A model verdict alone is not an executed check or publication permission.

Migration019 extracted_assertions binds each extraction-local assertion key to a stable server-owned claim/revision and typed scope/quantities/criterion/source-version dependencies. scoped_support_results records exact claim revision, model/extraction operation IDs, task, brief/evidence basis, full selected-evidence digest, scope digest, checker version, decision and actual guard outcomes. Replay reuses validated immutable identities/results; mismatched stored revisions/results reject. Claims remain unverified for publication and no report is created by this service. The production worker and final publication now independently restore these records; existing publication protections remain intact. Account deletion removes both new derived stores before claim revisions. Rollback disables structured support while preserving privacy, receipts and publication gates.


W01/W05 scoped publication integration: publishReport independently resolves managed assertion IDs from storage and re-executes their current owned scoped-support checks in read-only mode. It compares canonical text, exact passage membership, task/brief/evidence basis and pinned schema/prompt/policy/checker versions. Caller-provided approval maps are ignored. Managed assertions with partial, rejected, missing or stale checks cannot fall back to literal approval, including through a derivation/type-label change. Other legacy claims retain the existing literal gate and all unmapped/ownership/version protections.

The pure citation compiler may use a scoped approval only for the exact bound assertion text and passages supplied by that backend validation; it still rejects missing/duplicate claim IDs, unmapped material block text and unowned citations. Accepted scoped claims keep their original persisted claim/revision identities, gain the published direct-support status and source bindings, and reopen through the existing canonical report API. Publication validation cannot create missing assertion/support records. The writer must still map its final wording exactly to checked assertions or obtain additional support for changed wording; this change does not authorize new prose on the strength of nearby citations. General writer/criterion-driven worker integration remains open.


W05 generic writer service: createResearchDraft revalidates source assertions and passes only their approved keys to the structured write_report operation. Migration020 stores immutable owned lineage from writer operation to source extraction, source support, task and brief/evidence basis. Parent origin must be an extraction operation; self/cyclic draft references reject. Replaying an identical valid draft uses the stored operation and lineage without another provider call. Oversized statement expansion blocks explicitly without prefix truncation.

Final writer wording is not authorized by its premise citations. Each nonstandard heading, paragraph and limitation becomes a separate inference assertion proposal, tied to premise claim revisions and original source versions. Neutral application headings need no factual assertion. The assess_support operation checks these new texts against original passages, never against the draft as a source. Supported text compiles to its own canonical claim ID; unsupported text becomes a localized application-owned unresolved block with source links. Raw unchecked limitations/headings are not copied into the published report. The model title is not a canonical report field and is not published. The real report gate revalidates the final claims again; published synthesis remains labeled inference.

Draft scope retains common premise scope fields; differing fields remain unknown rather than being silently generalized. Composed dependency completeness is explicitly partial, requiring conservative correction invalidation. Aliases, mixed scopes, arithmetic and actual criterion satisfaction still require the remaining tools/evaluation. Final criterion coverage now distinguishes completed from completed_with_limitations; it never infers answered questions from source or statement counts. The production worker composes retrieval, support and writing as described below.

scoped-support.v2 supersedes v1 for new decisions and adds explicit currency-symbol/code preservation. Migration020 keys results by semantic operation, claim key and checker version so rechecking the same recorded comparison preserves prior-version results without a new paid request. Prior approvals are not silently upgraded; current publication requires current-version records. Account deletion removes draft lineage, source/writer model results, composed claims/revisions and checks. Migration/index changes have only been executed locally; hosted rollout remains separately gated.


W05/F04 coverage execution impact: adds a pure criterion-linked coverage resolver, fenced review coordinator and additive migration021 for immutable review outcomes. The existing structured review operation and prompt stay unchanged. The review uses original question, versioned task and exact selected evidence/assertions, with independently restored support checks. Supported coverage requires each cited assertion to have one supported check and each requested criterion to have relevant scope-matching evidence; model waivers, missing criterion bindings, ambiguity and omitted original requirements cannot produce complete coverage. This is answer coverage, never proof of positive candidate eligibility. Semantic relevance remains fallible and requires held-out model evaluation. Typed outcomes preserve review reasons and failed deterministic checks, canonical claim revision IDs and support/coverage checker versions. Replay revalidates all stored inputs and results; account deletion purges outcomes. No new dependency, service, public contract, prompt, processor or spending authority. Rollback disables coverage execution and retains ownership, versioned support, deletion and financial holds. The main worker and final report completion now execute this review and independently restore its proof.


W05 final-report coverage integration: the generic writer executes review_coverage after final-wording support, then reloads the typed resolved review inside the publication transaction. Completion requires complete question coverage and no rejected final statements. The canonical publication gate independently revalidates stored coverage and recompiles the exact draft; dropped/changed blocks, changed claim membership, missing/corrupt review and caller completion flags cannot grant completion. Incomplete coverage retains completed_with_limitations. Runs with no structured task retain the historical publication contract; this is not general controller integration. No dependency, migration, public schema, prompt, processor or model-route change. One additional bounded structured review call uses existing admission/receipt rules; failures block publication rather than claim completion. Rollback disables structured writing; retain completion gate, financial holds and private evidence deletion. Local fabricated semantic responses prove boundaries only; real research quality and broader stopping-policy evaluation remain open.


W05 production worker integration: with structured activation enabled, processRun now prepares the versioned task, ingests owned attachments, extracts selected whole passages, executes assertion support and question coverage, and writes/rechecks/publishes a generic report. This path never invokes the fixture proposer or template composer. No readable evidence, invalid/unknown operations or no supported assertions end explicitly unresolved/failed. Bounded public discovery/refinement now executes through durable search/read adapters; larger evidence selection remains unfinished and activation stays explicit. Existing historical routes remain separate. This is a first evidence-backed worker path, not the complete unfamiliar search/correction journey.
Impact: new worker composition module only; existing contracts, gateway, persisted results, source extraction and publication gates reused. No dependency, migration, public schema, prompt, processor or monetary authority change. Source preparation precedes extraction, and all network/model work remains outside transactions. Typed support/coverage executions back the evidence_checked event; an event alone grants no support. Resumption uses saved logical operation outputs; terminal unresolved paths settle actual known costs and preserve unknown holds. No silent passage truncation or fixture fallback. Rollback disables STRUCTURED_MODEL_ENABLED, preserving current identity, privacy, financial and publication safeguards. Bounded discovery and typed replacement corrections are integrated below; granular correction policies, broader mobile journeys and real-model evaluation remain open.


W04/W05 document journey and qualification repair: a new API/worker/extractor regression uploads actual digital PDF bytes with a same-length randomized entity name, executes the structured worker, reopens its cited report/page and verifies raw/derived deletion. The first run failed because an unrelated offline-only sentence downgraded a separate underwater prohibition. A pure paired regression reproduced the defect. Literal qualification screening now checks the qualifying sentence's subject matter while retaining conservative explicit discourse references (Only…, However…, This result…). It does not use word overlap to grant support; literal publication still requires the complete statement or its existing narrow passive equivalent, and structured support still runs all other guards. scoped-support.v3 and literal-scope-v4 prevent stale checker reuse. Direct/adjacent omitted-qualification negatives remain. This is a narrow lexical repair, not complete semantic/coreference checking; broader paraphrased exceptions require real held-out evaluation. No dependency, migration, public schema, prompt, model route or spend authority change. Rollback disables structured activation, retaining versioned evidence, deletion and fail-closed publication. Test model outputs are fabricated; PDF parsing/API/worker/database execution are real local evidence. No paid/native/hosted or superiority claim.


W02/W05 search transport now rejects redirects, enforces a 45-second total fetch/body deadline and 1MB strict-UTF8 response limit, validates bounded citation envelopes, rejects malformed/truncated output, and preserves parsed actual cost on invalid results. Titles no longer become invented snippets; citations deduplicate by URL and share an origin cluster. No engine/model/prompt/allowance change or paid call. Durable discovery output, explicit processor/policy selection and structured-worker discovery remain open.
Impact: existing adapter only, no dependency, migration, public schema or new service. Query/output bounds are checked locally; unknown transport outcomes are not converted to zero cost or empty successful search. Response metadata is parsed before output acceptance so malformed citations retain known cost. No retry/fallback is added. Existing reserve200000 and run allowance remain unchanged, so ordinary live admission can still block. Eight nonbillable transport cases cover valid citations, malformed envelopes, too many results, truncation, missing costs/snippets, oversized/invalid bytes, timeout despite ignored abort, credential URLs and returned-model mismatch. Rollback disables live discovery while retaining bounds, receipts and safety gates.


W02/W03/W05 durable discovery impact: new default-off STRUCTURED_DISCOVERY_ENABLED policy pins openai/gpt-4o-mini through OpenAI without fallback, and the single-search web plugin to Exa auto with at most3 results. Consent2026-09-17.1 explicitly names Exa; old consent cannot issue. Model price ceilings reuse the structured text route; reservation28658microUSD combines the full advertised model-context/output ceiling with the documented7000microUSD single Exa search fee. This is a versioned documentation-based bound, not a verified live invoice or protection against an external tariff change; actual overrun accounting remains visible and live activation still requires current authorization and a registered probe. No allowance increased. The existing legacy search policy remains separate.
Strict executable search proposals revalidate exact original-question spans and query terms after trimming; source text is never included. Mixed attachment tasks fail closed until explicit public-query approval is implemented. This is not a complete sensitive-question classifier. A stable logical identity binds query body, policy and brief revision, deliberately excluding unrelated evidence arrivals. Migration022 stores owned immutable typed results, task identity and exact request/receipt links; replay revalidates them and never resends unknown outcomes. Financial receipts persist despite lost lease/deletion, while private search outputs require a fresh fence. Deletion purges the new table. No network call holds a database transaction. New modules use existing gateway/admission/receipt boundaries; no dependency, hosted service or public mutation API. Rollback disables structured discovery, preserving consent, unknown holds, receipt accounting and deletion. Structured worker search/read scheduling and real discovery quality remain open.


W04/W05 durable source reading impact: structured discovery now executes an initial original-question query only when readable evidence is missing or a prior discovery pass needs replay. Exact executable search/fetch proposals retain current task question bindings; over-bound inputs block instead of truncating. Adoption reads only persisted owned confirmed search results with receipt/policy linkage, reuses owned URLs, labels snippets explicitly, and never uses snippets as readable documents. Returned source handles resolve to owned URLs before the existing DNS/socket-pinned safe fetch and isolated extractor. No transaction spans network/extraction. Migration023 keys issued/finished read operations by run/source/brief/reader version and binds their immutable source version. An issued operation without finished evidence remains explicitly unknown, without automatic repeat; future read-attempt reconciliation remains open. Current completion still requires actual final support and coverage, not successful reads or source count. Initial-pass exhaustion can yield a limited report; further criterion-driven discovery remains open.
The worker resumes an existing discovery pass even if an earlier read already created passages. Changed source URLs, stale leases, cancellation and deletion reject late content; account deletion purges read operations before evidence. No new dependency, service, public schema, prompt, provider or allowance. The existing flags remain off by default; mixed-document public-query approval remains gated. Migration is additive/idempotent locally; no hosted migration performed. Rollback disables structured discovery while retaining records, deletion, safe fetching and financial holds. Tests distinguish fabricated search/model/source transports from actual sandboxed PDF parsing; no live semantic or external exactly-once claim.


W05 criterion-driven discovery: unresolved executed coverage can now select a distinct query from the relevant criterion’s exact original-question span. New results are read and all selected evidence is extracted/supported/reviewed again before writing. criterion-discovery.v1 stops explicitly at three distinct queries or no available distinct public criterion wording. The query ceiling is checked atomically during provider admission and includes unknown attempts; replay remains allowed. This is a bounded refinement policy, not proof of general discovery or semantic quality.
Impact: new pure core policy and worker loop, plus an optional server-owned per-route attempt ceiling inside the existing account/run-locked reservation transaction. Current discovery sets that ceiling to3, after checking existing logical identity so replay cannot create another attempt. No dependency, migration, public schema, prompt or provider change; no allowance increase. Source text never supplies public-query terms. Coverage controls whether further discovery is needed; source count does not grant completion. A fourth research iteration can finish checking/writing after the three queries; exhaustion has an explicit reason. Existing support/publication checks remain authoritative. Initial absence of readable passages/assertions still exits explicitly unresolved; broader query generation, alternative source strategies, large evidence selection and recovery from unknown reads remain open. Rollback disables structured discovery while preserving durable attempts, financial holds, deletion and publication gates.


W06 correction membership impact: additive migration024 introduces run_evidence_membership and research_change_sets, plus authorized_run_passages. Membership pins exact passage/source-version identity, original run and both digests; the read projection revalidates all account/source/run relationships and excludes changed/deleted evidence. Original rows retain their owned run; reuse never copies bytes or silently selects a newer timestamp. Structured extraction, model-context validation, support restoration, writer source access and publication use this projection. Raw memberships are never updated by application paths; deletion removes them. Source inspection retains original locators/access warnings. Current loadEvidence for the historical controller remains unchanged; the structured path independently restores exact scoped evidence.
CorrectionRequest adds an optional strict replace_question patch with exact question and explicit reuse_snapshot/refresh policy. It does not parse a replacement by appending contradictory text. The existing freeform historical route is retained separately and is not incremental evidence. New admission requires the structured controlled route, current consent, expected parent revision, account/conversation locks, and hashes the accepted patch/message for idempotency. Distinct edits explicitly branch from the named parent and receive unique conversation revisions. The change set preserves accepted user text and unknown dependency completeness. Recompute all criteria/assertions/support under the child basis; never reuse old approval as current. Snapshot policy deliberately links owned readable evidence; refresh links none. Reuse is measured by exact membership and avoided source creation, not fullRerun flags. Public discovery reopens for changed tasks; mixed attachment queries retain the existing privacy gate. Matching discovered URLs reuse already admitted snapshot evidence rather than create new source IDs. Attachment reuse also checks current authorized bytes/digest.
No dependency, hosted service, model prompt, provider or allowance change. Additive local migration and public request extension require coordinated client adoption; no native typed-correction UI claim yet. A format-only request is not implemented by this patch and should not be presented as a zero-cost formatting path. Granular patches, complete dependency traversal, freshness policy beyond explicit snapshot/refresh and live/full-rerun semantic evaluation remain open. Source-specific purge and computed result change summaries are implemented below. Rollback disables structured correction admission/discovery; retain the membership read projection, tombstones, deletion, budget holds and report publication safeguards. Do not drop evidence tables or invalidate history by rolling back ownership rules.


W06 result-derived comparison impact: structured publication derives report-changes.v1 after canonical claim persistence and before the atomic report insert. It compares exact whitespace-normalized assertion wording plus semantic scope/quantities as a multiset, and criterion definitions including group membership/operator while excluding newly allocated keys and input offsets. Added/removed IDs are actual owned claim revisions and criterion IDs. Report outcome/limitation changes are recorded separately. The legacy conclusionChanged field indicates an assertion inventory or outcome/limitation change; it does not certify semantic difference or paraphrase equivalence. Newly feasible/infeasible lists remain empty rather than infer eligibility from edits.
Cited source-version sets determine evidenceUpdated. Reused cited versions require actual current run-evidence membership; newly cited versions are absent from the parent report’s citations. A reused snapshot can also be newly cited if the parent had read but not cited it. Neither metric asserts avoided model calls or full-document reading. Unknown/missing/ambiguous parent revisions, criteria or citations omit the comparison instead of claiming unchanged. Current structured reports ignore caller-supplied change flags even without a parent; historical diagnostic reports retain their existing representation.
The additive public comparison object includes method version, parent report, exact revision/criterion IDs and measured version sets, stored in the existing change_summary field. No migration, dependency, service, prompt, model route or additional provider call. Deletion already clears this field. Mobile uses the computed note and does not append a claim that old conclusions were carried forward for this version. Rollback disables comparison display/derivation while retaining claim recomputation, membership ownership and deletion; never restore inferred reuse flags as production evidence. Native UI execution, semantic comparison, granular correction controls and broader evaluation remain open.


W06/W07 client adoption impact: owned run responses add correctionMode and correctionReserveMicro. Typed correction advertisement/admission requires configured structured/live routing, provider key and nonzero project/key caps; actual remaining allowance is still atomically checked server-side. Disabled fixture routing rejects historical demo corrections as well as initial demo creation. The request patch schema remains unchanged; an exported public type supports mobile validation. No dependency, migration, prompt, provider or monetary allowance change. Mobile shows a replacement-question editor and explicit source policy; unsupported capability does not fall back to freeform production correction. Rollback gates the controls, retaining route/consent/budget and account isolation safeguards.

W05 production isolation impact: no new dependency/schema/migration/model route. Separate diagnostic composition preserves prior bounded tests without claiming production quality. Shared executeLeasedRun/prepareRunStep owns existing lease, heartbeat, cancellation, consent and deletion gates for both runtimes; research policy stays separate. Production executor never processes fixture runs and never falls back to the historical controller when structured/live processing is disabled: an owned queued controlled run ends explicitly failed with structured_route_disabled and settles through existing reservation rules. Production API rejects newly requested controlled runs when structured processing is disabled. The diagnostic executor/entrypoint rejects production environment or production auth, even if manually invoked. The development live launcher uses the structured worker and requires both configured caps; launch does not confer monetary authorization. Existing diagnostic integration assertions are retained with only their import target changed; actual model-gateway/PDF workflow suites remain production checks. Import-graph controls follow public workspace packages, re-exports and dynamic edges and include a positive diagnostic fixture detection control. Rollback disables structured admission/execution; never reconnect the historical fallback or remove permission, receipt, deletion or publication gates.

The older fixture-route PDF integration case explicitly uses the diagnostic executor with actual binary extraction; structured upload/search/correction PDF cases use the production executor. Test runtime labels follow the created run route, and assertions are retained.

W05 compare_scopes contract (scope-comparison.v1): a strict action selects2–60 unique model-local claim keys from one owned extraction/support basis. The server injects account/run/task/brief/evidence fields and resolves exact claim revisions. Pure core compares six recorded scope fields for pairs sharing criterion keys; unrelated pairs are counted separately. Absent/empty fields are unknown even if both are absent; differing spellings are retained without alias inference. Equal recorded scope is not factual agreement, eligibility, predicate equivalence, quantity compatibility or contradiction resolution. Results explicitly mark entailment and quantity compatibility not_assessed. Unsupported assertions may be inspected but cannot gain support from comparison.
Migration025 stores the comparison identity, selected claim revision IDs, exact manifest/input digest, support checker version and typed result. Fenced account/run transactions serialize replay/concurrent calls without network I/O. Restore re-executes support/binding validation and rejects altered claims/evidence/owner/basis/results; account deletion removes derived comparisons. The production loop executes comparison after support when at least two assertions exist. Writer context receives the durable, independently recomputed result; context validation recomputes it again before model admission. model-input.v2 adds its digest for comparison-bearing contexts; old contexts keep v1. Final draft support does not inherit comparisons keyed to different source assertion keys. Existing publication and coverage gates remain authoritative.
Impact: new additive public action/result schemas and local migration025; domain owner is scoped evidence/support. No dependency, provider, prompt text, allowance, model call or service is added. The optional versioned writer context is a behavior change; real model benefit remains unmeasured. Existing comparison-free saved writer contexts remain v1 only while no comparison is adopted for that basis; a new comparison/context upgrade is blocked while a same-brief writer attempt lacks a result or measured cost, or has an unknown outcome; its existing hold remains. Deployment remains unauthorized. Rollback disables new structured processing, retaining comparison records, deletion, unknown holds and publication checks; never drop records or silently strip comparison context from an in-flight request. Named calculation, counterevidence execution and complete semantic contradiction handling remain separate open work.

Comparison capacity: model preparation retains the existing strict byte/context ceilings. Exceeding either ceiling now returns an explicit blocked operation before provider admission; it does not throw into an automatic job retry or silently truncate pairs. Large-context selection/summarization remains separate work. New comparison records cannot be adopted while a same-brief writer attempt lacks a stored result, measured cost or known outcome; existing unknown reservations remain held.

W05 compact comparison projection (scope-comparison-context.v1): writer input contains one sorted claim-key dictionary, the explicit six-field order, and groups of identical relation vectors with zero-based key-index pairs. Criterion bindings are the intersection of the two supplied assertions' criterion keys; original scope values remain in those same assertions. All compared pairs, every equal/different/unknown relation, excluded-unrelated count and unassessed entailment/quantity status are preserved. This is representation compression, not evidence selection or a semantic summary. The gateway independently reconstructs the expected projection before admission. Oversized complete contexts still block explicitly; no pair truncation.
Migration026 adds writer_context_version with a legacy full-format default. New result identity hashes the prior exact input digest with the compact context version. Restoration checks both known identities, rejects ambiguous duplicate representations or mismatched version metadata, and never silently upgrades an existing record. Legacy context keeps model-input.v2; compact context uses model-input.v3 with its exact digest. Unknown prior attempts keep their original request context and holds. No dependency, model, prompt wording, provider call, consent processor or allowance change. Domain ownership/deletion stays with scoped evidence. Rollback disables new structured processing while retaining both readers, version pins, deletion and accounting; a code rollback that strips or rewrites pending context is unsafe.


W05 evidence-calculation handler (evidence-calculation.v1): strict calculate actions select bounded claim-key/quantity-index operands and one of sum, ordered difference, product, ratio, percentage or annual_cost. Account/run/task/revision authority is injected by the server; caller numbers, expressions and executable code are rejected. The handler restores the current owned extraction/support basis under the worker fence. Only supported exact claim revisions with uniquely bound printed decimal-plus-unit spans can supply operands. Duplicate numeric evidence aliases, absent/qualified/negated bindings, ambiguous notation, incompatible currency/period and zero denominators remain typed unknown outcomes. Ratios are exact fractions; percentage means left/right times100, not relative change. Product only permits an explicit nonnegative integer count multiplier; annual cost requires a named monthly currency and states the twelve-identical-charges assumption. No implicit unit conversion, currency exchange, rounding, invoice-total or population-aggregation claim is made.
Pure core uses bounded BigInt rational arithmetic. Migration027 evidence_calculations stores the ordered action, exact input claim revisions, evidence manifest digest, support/calculator versions and typed result. Each input includes exact numeric source spans; replay recomputes support and arithmetic before accepting the stored result. Concurrent fenced transactions share one identity; tampering, changed revisions and foreign ownership reject. Account deletion purges these private derived records. No provider/network work occurs in the calculation transaction.
Impact: additive shared action/result schemas, core function, backend domain/worker handler and local migration027; no new dependency, service, processor, model route, prompt, allowance or paid call. This checkpoint does NOT select calculations in processRun, add them to writer context, publish derived numbers or satisfy criterion coverage. Existing citation/publication gates remain unchanged and reject unsupported calculated prose. Production scheduling and proof-aware publication/recalculation are the next required integration, followed by counterevidence and held-out evaluation. Rollback disables invocation and keeps records/read validation/deletion plus all permission, receipt and publication gates; no destructive down migration or hosted rollout is authorized.


W05 calculation-report.v1 publication: prepareCalculationClaim restores the exact owned calculation at the current brief/evidence basis, recomputes its supported inputs and arithmetic, and renders a canonical arithmetic statement. Output retains exact fractions without implicit rounding; all input statement wording and calculation assumptions remain visible. This is an arithmetic claim, not an inferred population total, eligibility answer or invoice. Unknown calculations do not produce report claims. Migration028 calculation_claims maps one calculation to a canonical claim/revision and renderer version. Revision scope records ordered input revision IDs, input digest, operand scopes, formula, exact output and assumptions; it does not merge distinct operand scopes into an asserted common scope. Replay validates stored claim text/digest/type/owner/revision/scope as well as the original proof.
The real publication gate discovers these managed claims from storage, not caller-supplied approvals. It restores the proof again without model/network work under the account/run transaction and requires exact canonical wording, calculation type, input passage set and visible block citations. Tampered results or revisions reject; wrong owner/basis cannot prepare a claim. Published claims preserve their identity and record calculation-report.v1 support provenance as arithmetic inference, not direct source wording. Current final-draft coverage remains authoritative: a computed result cannot manufacture a completed report. Account deletion removes the mapping before its proof and claims.
Impact: additive local migration028, internal pure renderer and domain publication integration; no new public request schema, dependency, service, model prompt, provider route, allowance or paid call. Production planning, derived-answer rendering, final criterion review and correction recomputation are integrated below; semantic applicability remains separately evaluated. Rollback disables calculation claim preparation/publication while retaining records, validated read/deletion support and existing safety/financial gates. No destructive down migration or hosted rollout is authorized.


W05 calculation-planning.v1: plan_calculations is a separately named gateway operation with calculation-planning-prompt.v1, leaving existing operation schemas, prompts and request bodies unchanged. A plan contains at most six uniquely keyed actions with requested question keys and rationale. Actions use only the shared calculate schema (formula and supported claim-key/quantity-index references), never caller numeric values, code, account/run/version or budget fields. Binding validation rejects unknown/unapproved quantities, unrelated criterion/question references and duplicate actions. This validates references, not semantic choice of formula or complete question coverage.
The production structured loop invokes planning after support when supported quantities exist, before coverage review. The worker constructs approved keys from restored support, issues through normal consent/reservation/receipt/fence boundaries, and revalidates exact context/support and the executable actions after the proposal returns. Arithmetic then runs locally and persists typed computed/unknown results. A calculation event references those real records; empty plans emit no calculation event. Stable context/request identity reuses stored plan and arithmetic without another provider request. Unknown external outcomes retain holds and are not resent. An already-admitted writer without a prior plan preserves its existing path rather than adding a new plan on resume.
The existing model-operation result stores the plan, its input manifest and receipt; evidence_calculations stores exact actions/proofs. Account deletion already purges both. Validated calculations now feed canonical derived-answer rendering and final coverage as described below. Planning alone grants neither publication nor satisfied eligibility; pending identities and independent applicability review remain required.
Impact: one additive model output schema/operation/prompt, internal coordinator and production scheduling; no new migration, dependency, processor, provider, hosted service, API mutation or allowance. Each issued plan uses the existing pinned text route and full21658microUSD reservation; the existing account/run/project/key gates apply, and no live authorization is added. Source text cannot expand tools or authority. Rollback sets STRUCTURED_MODEL_ENABLED=false to stop structured processing while retaining cached plan/proof readers, unknown holds, ownership/deletion and publication gates; do not rewrite pending operation inputs.

Planning replay hardening: the shared gateway now compares stored operation/schema/prompt/policy metadata with the prepared request in addition to exact request digest, receipt and input manifest. A mismatch blocks cached reuse without issuing again or releasing its financial record. Operation-specific domain restores keep their existing version filters.

W05/W06 calculated-report.v1 integration supersedes the writer/coverage limitation at the prior calculation checkpoints. New write_calculated_report and review_calculated_coverage operations use calculated-report-prompt.v1; existing operation contracts and admitted legacy writer identities remain unchanged. The writer references calculation keys alongside ordinary cited source prose. The server alone renders exact calculation wording, assumptions and operand citations; final source prose still undergoes its existing independent support checks. Unknown or omitted required arithmetic cannot grant completion, and arbitrary derived numeric prose receives no arithmetic exemption.
Migration029 pins the plan to its exact source extraction/support and calculated IDs, the draft to that plan, and the final review to exact compiled content. model-input.v4 binds the full calculation context, including selection flags. Restoration recomputes supported inputs, numeric spans, proofs and canonical claims; publication independently checks the proof and complete report. Final coverage reviews the original question and selected results, with deterministic scope/criterion/reference checks. This does not independently prove the model chose the semantically correct formula. Arithmetic is not aggregate validity, source independence or eligibility. Preliminary source-only coverage can still cause unnecessary discovery before final arithmetic coverage; that efficiency issue remains open.
Typed replacement corrections reuse only authorized immutable source versions and recompute child plans, arithmetic, claim revisions and reports. The local control changes a sum to a ratio with the same passages and new claims. Its model transport and source rows are fabricated, so it is neither an extraction experiment nor live semantic evidence. Actual document tests remain separate. Account deletion purges calculation coverage, drafts and plans before proofs; provenance removal cascades through their dependent derived records.
Impact checklist: additive strict output/context contracts, two versioned prompts/operations and local migration029; ownership remains scoped evidence/report modules. No dependency, service, processor, provider route or allowance increase. New operations use existing gateway reservations and receipts, potentially adding final calculated coverage work; costs/latency require measured live evaluation under new authorization. Existing writer attempts retain their original operation. Rollback disables new structured processing while retaining both operation schemas, provenance, deletion, unknown holds and fail-closed publication. No destructive down migration or hosted rollout is authorized.

### W05 bounded counterevidence execution (counterevidence.v1)

Default-off `STRUCTURED_CHALLENGE_ENABLED` enables one bounded challenge objective targeting up to six initially supported claims for a consequential question. Capture original extraction/support receipts, claim revisions/scopes and evidence digest before searching. Re-extraction cannot remove those targets from the obligation. Execute an admitted public search, durable reads and substantive scoped reassessment through existing tools. Persist search/read/source-version identities, current evidence digest, checker version and typed outcome: `counterevidence_found`, `no_counterevidence_found_in_inspected_evidence`, `blocked`, `unresolved_at_limit`, or `outcome_unknown`. Events are observations, never executable result proof.

Only an original-question provenance span plus the closed application suffix `contradictions limitations exceptions` may form the transformed public query; revalidate after transformation and before admission. The action shares existing search and monetary ceilings. Replay excludes its search from ordinary-discovery resumption and restores finished read versions without network. Unknown issued outcomes preserve holds and prohibit blind resends.

Migration030 atomically records `runs.counterevidence_required_revision` with captured targets. Publication independently restores original target and current execution/support proof; required proof remains required if the derived row disappears or scheduling is disabled. Contradicted, qualified, stale or unresolved targets add localized canonical limitations, including when a fresh extractor omits them or a model optimistically approves coverage. Missing proof fails closed before legacy/no-task compatibility. This is a bounded selected-target check, not all-claim coverage or semantic correctness certification. POST report challenges still stores feedback; explicit requested-verification scheduling is separately specified below under ADR037. ADR025 records impact, tests and rollback.

Counterevidence checkpoint repair: every publication outcome restores required proof and includes every target-specific warning. A missing required record rejects all publication, even if a generic warning is supplied. Restart identifies committed challenge searches by the exact server-owned request digest while the challenge pointer is not yet stored. Regression artifacts: verification/v6/checkpoint-review/.

### Matched strategy, source removal and financial recovery

`runs.research_strategy` pins `criterion-adaptive.v1` or `iterative-baseline.v1` at admission. Server configuration selects new runs; owned corrections/follow-ups inherit parent identity. Both run the same production tools/writer/checker and limits. A1 uses original-question criterion order and B unresolved-criterion prioritization. No model/public request field grants strategy or budget authority. See ADR026 and evaluator-only protocol.

`DELETE /v1/sources/:id` authenticates owner, serializes with worker account/run locks and returns invalidated run IDs plus pending-file-cleanup state. Exact source/attachment/reuse membership seeds conservative descendant invalidation. Private raw and derived target content is purged; dependent reports become redacted and non-exportable; active work is terminal-cancelled with new fences. Tombstones deny new parent admission and source access. Repeated deletion is idempotent; consent revocation does not prevent privacy deletion. Unrelated raw evidence is retained.

Provider receipt recovery accepts only an already recorded generation identity and matching credential/project scope. One bounded metadata GET occurs outside locks; settlement rechecks identity in an account/run transaction, retains the original model/search receipt and cannot authorize content publication or resending. Minimal reconciliation records enforce unique generation identity. Historical repair is explicit, terminal-only and requires known previous settlement. Missing costs retain holds; overruns block further issuance. Operator default is read-only inspection; `--apply` is explicit. No live recovery was executed during local verification.

ADR031: optional UUID Idempotency-Key on both upload routes binds the exact account/name/MIME/bytes; mismatch or deleted upload returns409. POST /v1/run-requests/resolve accepts a UUID idempotencyKey; returns accepted+owned run metadata or withdrawn after durable account/key-hash tombstone. Under the same account lock, admission vetoes withdrawals; resolving never schedules/cancels. Keep replay checks, withdrawal table/veto and unknown reservation semantics on rollback. Receipt metadata deadlines now use an explicitly owned timer to survive Node20 GC.

W02 wall-clock authority (ADR032): lease expiry is checked using database clock_timestamp after relevant locks. Fenced semantic/terminal callbacks are rechecked after completion and rolled back if expired. Renewal locks run then lease before expiry validation; claims stamp new expiry from current database time. Shared/key/project budget waits cannot preserve expired issuance authority. Financial receipt reconciliation remains a separate permitted mutation basis; these checks do not erase unknown holds.

Cancellation API mutations serialize active ownership and run state with their event under the account→run lock order. Event insertion failure rolls back cancellation, deletion cannot be followed by a cancellation event resurrection, and direct Pool event emission holds the sequence lock through insertion. ADR033 and real PostgreSQL cancellation-atomicity regressions govern rollback.

### W05 explicit requested claim verification (requested-verification.v1; ADR037)

Controlled `POST /v1/runs/:parentId/follow-up` accepts the shared strict request containing report ID/version, claim ID, optional saved note, snapshot/refresh policy and UUID idempotency key. The server owns child ID/revision, target capture, allowance and dispatch. It returns the child and verification identities, reuse status, evidence policy and `reopenedDiscovery:false`. Ordinary report challenges remain feedback storage. Historical fixture follow-up is diagnostic research and discloses reopened discovery; it cannot claim targeted verification.

Migration036 atomically captures the owned target and `runs.verification_required_revision`. Capture restores the original structured extraction/support receipt, task, immutable assertion/scope, claim revision and selected source/passages/digests. Positive prior verification claims require recursively restored provenance; cycles and a ninth consecutive verification child reject before recursive restoration/reservation. Missing legacy provenance is unavailable, never fabricated.

Snapshot policy reuses authorized immutable passages. Refresh policy executes existing attachment ingestion or durable reads of the selected known source URLs; it performs no discovery. Cached extraction of identical attachment bytes is allowed and must remain distinguishable from a fresh extraction. The assessment receives the original question, captured scope/assertion and actual selected passages. The private note is retained feedback, not assessed and not a query. Existing model operation/prompt/schema/policy identities and financial gates apply; no new extraction receipt or assertion may be invented to admit the target.

Persist a recomputable scoped result: supported within inspected evidence, contradicted within inspected evidence, qualification needed, unresolved, blocked or outcome unknown. Positive reports persist a new owned claim/revision with exact supporting passages. Negative reports have no positive claim and link validated contrary/qualifying passages, including those omitted by the model. Every published result carries scope and fallibility limitations. Unknown/blocked processing cannot substitute a generic report; unknown issued cost retains its hold and cannot trigger blind resend.

Publication restores required target, evidence basis, input manifest, actual receipt and scoped result independently of worker progress. Exact report blocks, claim membership and limitations must match that restored result. Existing target rows and required markers both prevent generic fallback; loss of either required proof component fails closed. Account/source deletion purges target/note/result and dependent content, preserves opaque proof obligations/financial holds and invalidates late writers.

`POST /v1/run-requests/resolve` optionally accepts `verification:{parentRunId,request}` alongside the exact original key. A mismatched nested key rejects400; an existing ordinary/conflicting run or unavailable verification proof rejects409. Accepted children match parent and canonical request digest and restore their target; absent admissions still receive an atomic exact-key withdrawal. This read/withdraw operation remains available with scheduling disabled. Rollback retains recovery, proof readers, publication vetoes, deletion and financial settlement. Synthetic transport controls prove these paths, not arbitrary semantic correctness; ADR037 and the evidence registry retain failures and limitations.

### W06 exact accepted-question span corrections (ADR040)

The shared correction patch is a strict discriminated union. Existing `replace_question` retains its acceptance behavior. Add `replace_question_span` with `originalQuestionSha256` (64 lowercase hexadecimal characters), integer UTF-16 `start`/`end` offsets, exact `quote`, explicit `replacement`, and existing `evidencePolicy` (`reuse_snapshot` or `refresh`). No undeclared authority fields or implicit replacement question are accepted. The ordinary correction request still carries the expected owned parent brief revision and accepted correction text.

Admission locks the active account and owned parent, validates current consent and expected revision, then derives the resulting question from the stored original. Backend SHA256 computation is authoritative; the core helper compares that computed digest with the patch, checks the exact selected substring, and rejects invalid ranges or surrogate-pair boundaries. Original/replacement Unicode must be well formed. Empty-span insertion requires an empty quote; empty replacement permits removal. The derived question must remain nonblank and at most20000UTF-16 code units. Do not normalize whitespace, append the correction note or ask a model to interpret the patch. Rejections expose bounded reason codes only.

Full accepted patch, original correction text and expected revision bind correction replay and remain in existing change-set JSON. Existing ownership, attachment, allowance and dispatch gates apply atomically. Snapshot reuse retains authorized immutable passage membership; refresh inherits none. Both preserve unknown dependency completeness, full child recomputation and conservative discovery reopening. No inherited support approval, semantic criterion equivalence or selective savings follows from a smaller input edit. Parent history remains unchanged except required privacy invalidation. This API addition has no mobile semantic-editing UI; ADR040 records the schema impact, executed controls and rollback readers.

Required correction rediscovery (ADR039): a non-document child whose change set requires discovery cannot run with structured discovery disabled. Capability rejection occurs before task preparation/model issuance and records correction_rediscovery_disabled with no new provider intent. Explicit snapshot reuse still saves source reads when discovery returns the same URL; every conclusion is recomputed. Document corrections keep the existing public-query privacy boundary. Model-extracted criteria equality cannot waive the conservative obligation.

Stored requested-verification target failures use a closed typed error for invalid schema/record, digest and original-target identity (ADR042). The worker persists a blocked outcome under its fence without issuing a new provider request. Operational database/lease/provider errors are not classified by message text; financial settlement and unknown holds keep existing semantics.

HTML structure-v2 preserves otherwise omitted source lists as bounded supplemental passages with original lead-in, nesting and struck-through labels. Main-content/table/list ordering remains explicitly partial and supplements may overlap. Historical structure-v1 receipts remain readable and completed source reads are not automatically repeated. Preserving a span does not prove bounded model selection or claim support (ADR044).

HTML structure-v3 preserves semantic strike/deletion markers in every main/list/table text and cell representation, excluding comments while retaining visible tails. Historicalv1/v2 receipts remain immutable/readable (ADR046). Model input capacity is128 whole passages with one backend constant shared by selection and proof readers; no slicing/ranking or truncation. All per-text/source/serialized-body/token-policy and financial ceilings remain unchanged. Larger source counts/byte totals can still block; count capacity is not evidence of attention or factual correctness (ADR045).

W06 owned document append (ADR048): `append_attachments` keeps the exact original question and old evidence, adds1–3distinct owned IDs within the existing total3 limit, and uses snapshot inheritance followed by full conservative assertion/support recomputation. It never declares new documents correct or silently replaces/deletes old sources. Owned parent/child brief differences establish the new-document readability obligation even if optional change metadata disappears. Missing parent basis or unreadable newly supplied input blocks a new successful report. Whole/span question changes retain required rediscovery; document-only append does not expand public queries. Exact content-bound correction resolve-or-withdraw works while scheduling/processing consent is disabled, with ownership, accepted-patch proof and deletion vetoes. Markdown export is the existing deterministic format-only representation of the same stored claim set; it does not initiate research.

GET `/v1/runs/:id` includes boolean `contentInvalidated`, derived from the owned run's existing source-deletion tombstone. When true, `reportId` is null regardless of a retained redacted report row; ordinary cancellation alone is false. Deleted accounts and foreign runs remain denied. This signal authorizes no new research or source access; clients must discard dependent cached content before loading ancillary events/report data (ADR050).

W04 admitted document artifact boundary (ADR051): binary document admission and durable `evidence_artifacts.body` both allow at most8MiB (8388608 bytes). Public HTTP fetch retains its separate1500000-byte ceiling. Store the original validated bytes/digest before claiming a durable extraction receipt; constraint or transaction failure is not a completed read. Migration037 is additive/idempotent and cannot be rolled back by deleting admitted evidence or shrinking storage below existing documents.

Saved HTML admission (ADR054): `POST /v1/attachments/bytes` additionally accepts UTF-8 `text/html` up to1,500,000bytes, with existing filename, binary/PDF mismatch and control-character validation. The notes endpoint remains text/plain or text/markdown only. HTML is initially stored/not-read and runs through the same isolated structure-v3 parser as fetched HTML. Owned receipts use attachment locators and null HTTP status; no original-site retrieval or authority is inferred. Existing extraction limitations, immutable source membership, cancellation/deletion and publication rules apply. The8MiB PDF/text and durable artifact limits are unchanged. Native picker capability is unchanged.

HTML normalization identity is now `trafilatura-2.2.0/structure-v4`: strictly valid UTF-8 bytes use explicit UTF-8 decoding, avoiding lxml's unlabelled Latin-1 interpretation; other public-page bytes retain declared/inferred decoding with an explicit warning. Original bytes/digests and immutable older extraction versions remain unchanged. Fresh attachment ingestion recomputes pre-v4 cached HTML; explicit snapshot reuse preserves its original version and limitations. This is not OCR or a completeness claim.

### W05 whole-passage selection and replay (ADR055)

New runs pin whole-passage-selection.v1; migration038 keeps preexisting runs on legacy-all.v1 to preserve issued/unknown model request identities. The pure selector retains all input if bounded; otherwise ranks arbitrary Unicode query terms against exact passage tokens, with whole adjacent passages in source-version locator order. No prefix truncation, candidate-name routing or evaluator gold. Required original challenge evidence and its neighbors cannot be dropped to fit. Limits:4096 candidates/16MB candidate text,128 selected passages/40sources/48000 double-serialized passage+source bytes; executable model policy remains separately authoritative. No relevant assertion still means unresolved.

Backend evidence_selections owns immutable candidate metadata and recomputable selection proof. Run obligations and proof commit under account/run/lease fencing, without network I/O. Deletion of a required proof cannot cause another logical action. Input-manifest.v5 and strict context metadata bind identity/digest/counts; exact owned text/source versions/locators/digests/question/revision/policy restore before reuse. Restoring original challenge proof must use its saved candidate inventory rather than newly discovered passages. Current operations reject stale evidence revisions. Deterministic proof corruption becomes a failed unresolved run with no additional provider dispatch; transient database and lost-lease errors keep existing semantics.

Source presence and lexical rank never count as coverage. Every publication, including completed_with_limitations, independently restores current selection proof. Any omissions require the canonical warning identifying selected/available/omitted counts and remaining unassessed qualifications/counterevidence; completed is forbidden. Existing exact-claim support/citation/coverage checks still execute. Corrections select again using the changed question over immutable authorized reuse membership; selection neither invents reuse nor claims full dependency completeness. Source inspection retains all authorized evidence, including passages not assessed by the model.

ADR056 / W01 inventory support: a selection-aware claim can become a writer premise, calculation input, coverage answer, published assertion or requested-verification target only after the existing scoped support checker also runs against the full immutable authorized selection inventory. `selection-inventory.v1` records exact claim revision, source-proof digest, scope, checker identity and originating support intent. This deterministic extra check may downgrade but never promote the selected-context decision. It does not imply semantic assessment of omitted passages. Restore validates ownership and bytes; absent historical additive cache re-executes the gate without unfenced persistence, while conflicting stored results fail closed. No model input expansion or paid reissue follows from this check. Migration039 and ADR056 own its storage/rollback decision.

ADR057 / W05 empty extraction recovery: new runs carry empty-selection-recovery.v1, existing runs retain none.v1. A successfully executed extract_assertions result with no assertions may select a different whole-passage context from the same authorized immutable inventory. Recovery must add previously uninspected passage IDs, preserve neighbors and original byte/source/passage limits, and restore identical selection/provider identities on replay. It shares the existing four-iteration bound and existing action budgeting, never retries unknown external outcomes, and ends explicitly on exhausted inventory, indivisible evidence or a remaining-evidence limit. Nonempty extraction still requires substantive support and criterion coverage; all conclusions retain ADR056 inventory checks. Policy is server-owned and unknown versions fail before model cost.

## ZDR route continuation — 2026-09-18

Structured runs persist immutable model_policy_id (ADR059/migration041). New configured Azure-ZDR runs use Azure at the same model/tariff, require ZDR, disable fallback and carry the policy through task/support/publication/financial bindings. Children inherit policy; historical OpenAI request identities are unchanged. Public discovery keeps its separate policy.

ADR060: explicit Azure exact-quote-v2 policy resolves only unique unchanged quotations within their original question/passage, records original/resolved coordinates in an intent-bound event, then runs unchanged schema/binding/support/publication validation. Legacy policies remain strict without coordinate resolution.

W02 evaluation continuation (ADR061): an explicit registered held-intent-continuation.v1 grant can permit a distinct research task while exact terminal unknown model intents remain held. Account/key/scope, request/receipt/question/policy identity and reserve must restore; all unconfirmed liabilities must match. Budget includes confirmed spend, retained holds and the full next run allowance. No resend or settlement is authorized, and every new unknown halts the comparison. Ordinary production accounting/fencing and default evaluator stopping remain unchanged.

W05 authorization query batching: validateOwnedModelContext restores bounded current passage/source rows in bulk, then checks each exact input tuple independently (owner/run/version/digest/text/access/title). It does not cache prior authorization or widen source membership. The25-passage case uses4queries instead of28. Database deletion-race tests identify the actual blocking connection rather than accepting an unrelated blocked account query.

W05 model-validation-diagnostics.v1 records only bounded fixed structural field names, schema issue codes and numeric indices on output-schema rejection. Unknown keys become `other`; values, rejected prose and validation messages are not retained. Existing rejection reason, paid receipt, cached replay and publication denial remain unchanged.

ADR062: only new openrouter-azure-mini-zdr-discovery-v3 runs select public-discovery-azure-zdr.v2. Existing OpenAI/Azure-v1/exact-quote-v2 discovery identities remain unchanged. All search/challenge/adoption/receipt bindings derive from immutable run policy; Azure generation requires ZDR/no fallback, while existing public-query projection and separately disclosed Exa plugin remain unchanged. Search-provider ZDR is not asserted.

### Research intent compiler and model portfolio (ADR063)

`compileResearchIntent` is a pure research-core function. The original question string is copied and never rewritten. Hard constraints and soft preferences stay distinct; non-blocking unknowns become explicit assumptions or branches. `neededClarifications` asks only consequential questions (jurisdiction/safety), not cosmetic ranking or tone. Session B/C should consume this contract rather than re-extract meaning from model prose.

`research-portfolio.v1` admits registered model policies by privacy/ZDR and structured-output capability, cheap-first, fanout 1. New runs stamp the admitted route; explicit pins stay only when that policy is privacy-admitted; children inherit. Escalation is a new admitted attempt with a recorded trigger and a depth/budget bound, never a second dispatch of an unknown intent. Migration043 stores an immutable routing audit; `runs.model_policy_id` remains the replay identity. Live reserves keep writing/verification leftover ahead of exploration and structured attempts. Optional receipt `cacheReadTokens`/`cacheWriteTokens` are attributed when the provider envelope includes them; missing `actualMicro` stays null. Portfolio eval records are dated and must not claim dynamic routing is better until measured. Live semantic task classes exist behind the existing explicit-authorization runner. Document-grounded live briefs pass an owned passage rather than inlining source text in the question.

ADR061 brief binding repair (exact-quote-v2 only): unique exact provenance quotes may have their UTF-16 offsets repaired, and criterion keys that the model omitted from every question are attached to a covering question. Quotes that are not exact unique substrings of the original question remain invalid. v1 policies stay unrepaired. This does not invent citations or mark unknown as answered.

