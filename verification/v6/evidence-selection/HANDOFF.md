# Evidence-selection and inventory-support checkpoint

Runtime repair: f0640db7e12d24d96e3ad4eb3334e058c83d523b. Baseline selector:9b0f9e7c86c4c7f6ebcf0082217c44d4516ae12d, with repairs b5973e4700271aef1ca08895fed074a4318a2d00 and d784c274217a218c0f73f00a4609090fa1b0d5d5. Branch codex/v6-saved-html. User ZIPs remain untouched. No push, deployment, hosted execution, new native build or paid call.

ADR055 selects bounded whole passages and records the complete immutable inventory, selected IDs, omitted count, owner/revision and proof digest. Full-context legacy request identities remain. Missing/corrupt proof prevents reissue and publication. Oversized documents can now enter structured extraction without replacing pages by prefixes. Corrections recompute selection over exact authorized source versions.

The separate negative probe found a real remaining safety failure at d784c27 despite443 passing PostgreSQL tests: a known contradictory passage was omitted because its adjacent bundle was too large, and the affirmative limited report published. OMITTED_CONTRADICTION.json preserves that failure and the small-document control. ADR056 adds a deterministic check against the full authorized inventory before selected claims can become writer premises, calculation inputs, coverage answers, published statements or verification targets. A separate versioned receipt binds claim revision, scope, original selected assessment, support intent and selection proof. It only downgrades eligibility. The original provider assessment remains unchanged. The committed before/after probe now withholds both contradictory cases without expanding model input (inventory-probe receipt, exit0).

New repair evidence: inventory-verify passes164core/149backend/206mobile/6governance with configured types and import boundaries. Actual isolated parser/API/worker controls pass51/51 (inventory-extraction receipt). Full PostgreSQL repair results pass446/446 across32files,795.005s wall; inventory-integration.receipt.json records the terminal exit0. The previous selector baseline's443/443 is separately pinned in RESULTS.json and must not be represented as repair coverage. Development12case focused controls passed; the final suite additionally includes cache restoration. The first full baseline420pass22fail and second442pass1fail are retained with their actual logs. Type-declaration and targeted expectation failures remain documented; no production assertion was weakened.

The synthetic useful-question trace asks which firmware supports offline recording, then changes the constraint to underwater recording. It uses production extraction/support/writer/correction code and25 exact reused passages; the revised report replaces the firmware statement with the negative underwater qualification. Fabricated model/extraction transport is explicit: this is wiring/publication/reuse evidence, not semantic quality. The baseline and repair traces are separate. Exact selected passage IDs/digests, canonical report blocks and change summaries are retained.

Actual saved public document bytes use isolated Trafilatura/Docling Parse. The post-freeze reference-presence diagnostic found all8 referenced spans in extraction but omitted3 ESP32 spans from selection. Its exit1 is retained. These inspected references are now exposed and cannot count as fresh heldout data for later tuning. RFC9112/TMP117/ESP32/SQLite fabricated semantic transport produces no assertions/no report; actual extraction is not a useful-answer pass. No semantic score, human adjudication or superiority result is claimed. Existing native evidence elsewhere is distinct and was not rerun here.

Next executable task: add bounded, replay-safe recovery when the first selected context yields no assertions while authorized uninspected evidence remains, using fresh synthetic/renamed tests rather than exposed reference answers. Keep whole-passage limits, evidence proofs, unknown outcome holds and the new full-inventory publication gate. Then run the registered real-model original/correction/full-rerun comparison only after current project-specific monetary authority and remaining allowance are established. Broader OCR/layout, physical/iOS/hosted identity and W10 release gates remain distinct.

Rollback can suspend new admissions but must retain admitted selection policy, immutable proof/obligation readers, the inventory veto, account/source deletion, fences, stable provider identities, receipt reconciliation and unknown holds. Never restore selected-context-only eligibility, relabel omitted text as read, recreate missing proof to resend, or reduce the baseline to claim improvement.
