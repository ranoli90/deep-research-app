# Append-owned-document correction and bound recovery

W06/W04/W07, with W02/W03 transaction/privacy controls. Source clone0602f96efff0eaa576e7654f08b226ef20108de5; eight-file patch: contracts, pure question helper, correction module, structured worker, two API additions, three new test files. No migrations/dependencies/prompts/model routes/mobile source changes; primary integrates canonical docs and mobile packet separately.

Strict append_attachments patch takes1..3unique owned UUIDs and literal reuse_snapshot. Parent expected revision and accepted correction text remain part of full correction-v2 identity. Under existing account/parent/conversation locks reject already-present/foreign/deleted IDs and combined count>3 before new run/reservation/outbox. UUID duplicates are case-insensitive; stored new brief UUIDs normalized lowercase while original accepted patch identity retains exact supplied strings. Parent question/old attachments/old authorized passage versions remain unchanged. No implicit replacement/deletion. Same lifecycle eligibility as prior corrections, not a new terminal-only condition.

All assertions/support recomputed. Old same-digest attachment versions skip parser reread through existing inheritance behavior; new documents use actual ingestion. Append change-set explicitly reopen_discovery=false because unchanged question plus private added input does not authorize public rediscovery. Existing whole/span discovery requirements unchanged. Private filenames and accepted note cannot become public query proposals.

Post-ingestion new-document obligation derives from immutable owned parent/child brief membership, not optional change-set row. Parent missing/malformed/not-owned basis fails before model prep. Each appended ID requires an authorized readable passage at its exact attachment locator and current attachment digest; otherwise explicit appended_document_unavailable before extraction/support/writing. Empty PDF cannot silently republish old answer, even after change-set deletion. This guard remains on rollback for admitted children. Partial extraction still has existing caveats; it is not complete-document reading proof.

New POST /v1/runs/:parentId/corrections/resolve accepts exact strict saved CorrectionRequest, typed patch required. Shared correction identity prevents unrelated-run adoption. Account lock serializes resolve-or-withdraw with admission and deletion. Accepted result requires owned parent, exact stored accepted JSON change-set and no source-deletion invalidation; otherwise missing child is withdrawn using existing035 opaque identity. Admission honors withdrawal before replay/new child for whole/span/append. Resolver needs current identity but no new consent or enabled scheduling, never queues/spends/cancels. Source deletion/unknown financial holds not weakened. Settings appendDocumentsAllowed boolean equals existing typedCorrectionsEnabled, not budget/model-health readiness; missing flag on oldserver is a mobile failclosed condition.

Regressions: original new-kind unit1fail/1pass retained. Initial append implementation exposed real stale-old-answer behavior after unreadable new PDF:2failed cases including missing change-set, now blocked. Initial test imports incorrectly sourced storeAttachment from access instead of attachments; two type-failure receipts retained, imports fixed. Existing test assertions untouched. New actualPG11 plus existing span6/rediscovery2 =19passes; core157passes. RealPDF3controls exercise parser, source/version reuse, new contrary passage/page locator, disputed typed support with no child report, zero public search, empty-newdoc and missing-change-set safety. Fabricated provider responses/spend are not semantic quality or invoice evidence. No user/private docs involved. All source was frozen for final tests; final parser repeat follows UUID refinement.

Rollback: stop scheduling new append corrections/capability but retain typed patch parsing, child brief membership/newdoc guard, exact recovery/withdrawal identity, immutable evidence/proof readers, consent/ownership/deletion and financial settlement/unknown holds. Do not revert to a reader unable to interpret already admitted obligations.
