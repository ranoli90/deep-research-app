W06/W04 explicit append-owned-document correction, preserving W02/W03 admission/privacy. Add strict public patch kind append_attachments with1..3unique UUIDs and evidencePolicy literal reuse_snapshot. Same question, old attachments/evidence retained; no implicit source replacement/deletion or query expansion. Current account/parent/conversation locks, consent, whole accepted-patch digest, reservation/outbox and deletion veto reused. No new migration/dependency/service/prompt/provider route. Ingestion already skips inherited identical old docs and parses newdocs; all conclusions recomputed. Require foreign/deleted/already-present/combined>3/consent/revision/replay regressions plus actual parser newdocument conflict control. Existing whole/span semantics unchanged; parent lifecycle rules unchanged. Rollback stops new append admission while retaining child brief attachments, immutable evidence/proof readers and financial/deletion gates. No paid/native/hosted claim. Primary owns canonical integration; mobile implementation separate agent.

Expanded explicitly by primary before recovery implementation: add POST /v1/runs/:parentId/corrections/resolve with exact strict saved CorrectionRequest. Shared server correction-v2 identity covers parent and accepted request. Under account lock resolve an owned child only when research_change_sets matches exact accepted JSON; otherwise withdraw via existing035 opaque admission_withdrawals, with admission checking that tombstone. No new migrations. Recovery bypasses scheduling/renewed-processing-consent gates because it only reads or withdraws, never queues/spends/cancels. Source/account deletion still deny or withdraw, never revive content. Existing whole/span admissions honor explicit withdrawals too; retain replay/publication/unknown holds. No arbitrary generic UUID resolve adoption for correction hashes.

Additional reviewed impact: settings returns appendDocumentsAllowed equal to typedCorrectionsEnabled; old/missing capability is not upload authority. Worker retains admitted new-document obligation from immutable owned parent/child briefs even if change-set disappears; malformed parent fails before model preparation, unreadable appended input fails before extraction/support/writing. Required readable passage must bind exact owned attachment locator/current digest and authorized child membership. Keep this reader on rollback. Existing whole/span behavior without added documents is preserved.
