W08 evaluation runner impact and review packet

The previous eval:live stub treated credentials and a nonzero environment cap as authorization. The candidate command instead denies execution unless the operator supplies explicit execution/approval flags, a current scoped authorization record and its exact SHA256/approval identity. An operator-authored file is an attestation, not independent evidence of user permission. No paid run has been authorized or executed in this packet.

The protocol decision was reviewed with the primary before implementation: live_discovery only, question-only generation projection, original A1/B and B snapshot correction against fresh A1 corrected rerun. Frozen supplied documents are explicitly unavailable and every selected scheduled slot is recorded unrun. No hidden known-source injection substitutes for discovery. All registered selected repeats remain in the schedule; budget shortages and failed parent corrections remain visible. Evaluation grading remains separate, null and unclaimed.

Runtime composition imports the existing API, worker and adapters one-way. A separate governance assertion forbids transitive evaluation-directory imports from API/worker without weakening prior forbidden imports. There are no new dependencies, migrations, services, production schemas, model prompts, provider routes or reference-answer imports into generation context. The CLI requires a preprovisioned dedicated database, queue and existing consented account, uses no account creation/allowance grant/schema migration, and holds an evaluation-runner advisory lock. Operators must keep unrelated workers/dispatchers out of that dedicated database; this is not a guarantee against an independently misconfigured deployment.

Budget scheduling requires the entire admitted 100000-micro run reserve available before each step. Existing gateway account/project/key caps continue enforcing issuance. Prior liabilities across the selected account, project scope and same/unattributed provider key count; unconfirmed reservations remain holds and stop future steps. Provider-side invoice/cap enforcement is not certified by local application arithmetic. Unknown execution is never blindly resent. Stable approval/slot idempotency keys prevent a new journal from generating fresh admission identities for a previously completed slot.

Durable JSONL records precede admission and follow acceptance/completion, with source/config/prompt/contract/policy hashes, exact owned trace identities, raw downloaded artifact bytes/identifiers/digests (runtime SHA256, source/transport/extraction digest and extractor-version agreement checked before trace export), extraction locators, immutable reuse identities, attempts/receipts, cost/holds and measured wall time. Tokens/keys are environment-only and redacted from serialized string fields as defense in depth. Arbitrary provider/DB exception messages are not emitted; bounded failure categories remain. Existing output directories are not overwritten. Scoring and independent adjudication remain null.

Rollback: restore the default-deny entrypoint or remove scheduling; retain admitted run records, receipts, outbox state, immutable readers, account ownership, deletion, unknown holds and publication vetoes. No rollback should retry opaque provider attempts or erase evaluation failures. Canonical command/protocol/status/ADR updates remain owned by the primary; this packet is implementation evidence, not a replacement specification.
