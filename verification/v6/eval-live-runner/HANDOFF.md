# W08 matched execution runner handoff

Source patch is against 0602f96efff0eaa576e7654f08b226ef20108de5 in /tmp/deep-eval-runner-0602f96; no agent commit or root source mutation. Final source.patch passes root git apply --check. Seven files, no dependencies/migrations/services. The provisional candidate.patch predates final integrity checks and test rename; apply source.patch only.

Final validation: backend typecheck exit 0; 12 runner units plus 6 governance controls exit 0; 3 actual-extraction/PG controls exit 0 under vitest.extraction.config.ts. Previous integration-config 3/3 pass and focused identity 1/1 pass remain preserved; tests moved to extraction filename because normal CI integration precedes Python setup. Initial typecheck and one SQL test failure are preserved, fixed without weakening assertions. Exact commands and scope are in RESULTS.json. DB slot released, queue/pool stopped; synthetic DB retained.

The four-run control uses actual production authenticated API, worker, isolated structure-v3 HTML parser, writer and checker with fabricated model/read transports. Three actual reads produce four reports; B correction reuses all three parent passages and extraction provenance. A new journal with the same approval replays all four runs without any extra provider or parser calls. A stored raw artifact mutation rejects trace export without network. Other controls count prior different-account same-key holds/confirmed spend and retain a real 21658-micro application reservation after one opaque fabricated provider failure, leaving subsequent slots unrun. Actual provider charges were zero; recorded confirmed costs (10 micro per successful synthetic run) are fabricated test receipts, not live bills or quality evidence.

CLI: pnpm --filter @deep/backend eval:live --execute --operator-confirms-user-approval --authorization <file> --sha256 <file-sha256> --approval-id <uuid> --output <new-directory>. Do not execute now. EVAL_SESSION_TOKEN and provider credentials are environment only. Authorization shape is strict in authorization.ts; operator attestation is not independent proof of current user permission. Canonical protocol/task/freeze hashes are pinned before work; only question fields enter production. Frozen supplied-document mode explicitly records every selected slot unrun without opening DB or accepting credentials. Live discovery is the sole implemented mode. Separate scoring remains null.

Requires preprovisioned isolated queue/schema, consented account and no unrelated workers; CLI does not create allowances, consent, accounts or schemas. Application caps/holds do not certify provider-side limits. There is no automatic resume of unknown work and no silent retry; stable approval/slot identities guard completed replay. Public source bytes and owned trace identities are retained for later scoring, with runtime raw/source/transport/extraction digest agreement. Exceptions expose bounded categories only. Configured model and effective policy/source/config pins are recorded.

Primary next: review/apply source.patch, register exact commands and canonical impact/rollback, run integrated checks. No live evaluation, human adjudication, superiority, hosted operation or native acceptance is established. Rollback disables evaluation scheduling while preserving admitted runs, receipts, account ownership, immutable readers, unknown holds and publication vetoes.
