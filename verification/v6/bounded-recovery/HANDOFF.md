# Bounded recovery checkpoint

Runtime: abdfee1ee623b563a0164def7e73ff94fb56d4f6, branch codex/v6-saved-html. Prior verified inventory publication repair: f0640db7e12d24d96e3ad4eb3334e058c83d523b, evidence checkpoint d1fc334. This adds ADR057 after the preserved ADR055 omitted-contradiction failure and ADR056 repair. The W01–W09 research-quality milestone remains incomplete.

New admissions recover from an executed empty extraction by inspecting different whole passages from the same authorized inventory. Source/version/locator order, whole neighboring bundles and the original passage/source/byte limits determine the next context; no reference answer enters production. Migration040 leaves already admitted runs on none.v1. The existing four-iteration bound, scoped reservations, ownership/fences, immutable selection/provider identities and full-inventory support veto remain. Unknown provider outcomes stop without resend and keep their reservation. Replaying the successful synthetic recovery does not reissue earlier extraction requests.

Before/after: the fresh decisive-passage regression fails on the earlier runtime after one empty context, then passes with actual different bounded contexts and a cited report through the production worker. First exhaustion-test expectation failed because the fabricated positive transport found the evidence on pass2; a separate always-empty model control now proves the four-iteration limit without suppressing a successful result. The old official-PDF one-selection expectation failed with3 selections; its replacement checks every proof/manifest, the original bytes/locators, exact union of inspected passages, no source fetch and no fabricated useful report. All development failures are retained with command/head/environment limits.

Terminal evidence currently available: verify166core/149backend/206mobile/6governance with configured types and import boundaries; actual isolated extraction51/51 across8files,300.783s wall; both metadata validators and their mutation suites pass. Full PostgreSQL passes451/451 across32files,777.152s wall; integration.receipt.json and RESULTS.json package the terminal result, logs, exact runtime hashes and source/correction traces. These suites run on separate synthetic PostgreSQL databases; timings are execution receipts rather than a controlled performance comparison.

Actual frozen documents retain exact saved bytes and isolated parser output. The post-freeze diagnostic now finds all8 references in executed contexts (0 extraction misses,0 inspection misses), versus3 selected-reference misses before recovery. These references were already exposed; this is not a fresh heldout benchmark. The fabricated semantic transport still produces no relevant assertions/no report for official documents. Semantic score and independent human adjudication remain null. The synthetic firmware question/corrected underwater constraint uses25 exact reused passages; a separate12-passage case demonstrates recovery of initially omitted decisive evidence. Complete contexts, source hashes, selection proofs, support results and canonical reports remain in compressed traces.

Paid execution is gated by more than credentials. A read-only GET of this project's OpenRouter key on2026-09-18T16:14:01UTC returned limit$5, remaining key quota$4.7859228 and reported usage$0.2140772. Key quota is not user authorization or a verified account credit balance. The configured project database has a legacy provider_intents schema lacking provider-key/receipt provenance. Read-only inspection found70 OpenRouter intents,56 unconfirmed with$5.545 in held estimates; its$2.80 reported confirmed amount is not receipt-validated by this schema.303 other-route intents also remain unconfirmed with$1.066 in held estimates. All are unchanged. No migration, reconciliation mutation, hold release, generation, new paid account, purchase, deployment or push was performed. The metadata query failures (SQL42703) and conservative legacy aggregates are preserved. A fresh evaluation database must not silently bypass these obligations.

Next concrete task: identify the current authorized financial ledger and evaluation allowance; establish provenance/reconcile legacy liabilities through actual receipts or other authorized evidence without blind resend. The question to the user is pending; elapsed time is not approval. Then execute the smallest registered non-sensitive real-model comparison through the existing API/worker, with paired originals, snapshot correction and full rerun, preserving every failure, actual spend and unknown hold. No claim of useful live synthesis or superiority is justified yet. Broader OCR/layout and applicable physical/iOS/hosted-identity/W10 release gates remain separate; prior native evidence is not a new build of this checkpoint.

Rollback may set the default for new admissions to none.v1, but must retain admitted recovery policy/readers, immutable selection/obligations, inventory support veto, account/source deletion, current-time fences, stable paid identities, receipt reconciliation and unknown holds. Do not recreate missing proof or reinterpret a held estimate as confirmed spend. Preserve all three user ZIPs and the earlier failure packets.
