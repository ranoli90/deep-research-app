# W04 document extraction checkpoint

Based on b84fc8d plus the recorded document diff. No paid model call, native build or hosted deployment. Raw public PDF and experiment runtimes remain under /tmp; this directory retains limited provenance, package/license hashes and failures. Agent reference inspection is not human adjudication.

The API previously accepted pasted text as PDF; two production API regressions failed before repair. It now accepts authenticated bounded binary bytes and distinguishes stored from read. The worker parses outside transactions and commits only through its current account/run fence. Three actual API/worker/parser/database cases cover byte identity, source reopening, raw/derived deletion, and cancel/delete after parsing before persistence. The surrounding research route is a labeled fixture in these safety tests, not evidence of general synthesis.

On identical 649012-byte RFC9112 PDF bytes (SHA256 b260bba790c2da55a7e0795f356fcd9b70743686c55250f0ef1cc993c4d1abac), pypdf 6.19.0 text modes fragmented words. PDFium 5.13.0 and Docling NativePdfPipeline 2.128.0 detached the bold negation from its sentence. Raw cell/line parser order also failed. Do not count their returned 46 pages as successful decisive-text extraction. Docling Parse 7.20.0 cells grouped by physical line coordinates preserved the complete reference prohibition on page 18. Comparison permits only Unicode compatibility normalization, whitespace and a physical line break after a retained hyphen; stored evidence retains its original line boundaries. The reference was checked against publisher HTML and a rendered PDF page by the agent. This is one diagnostic document, not a general quality benchmark.

Selected PDF path: Docling Parse 7.20.0 with recorded text-cell geometry, 2-point physical line grouping and page/block locators. Pypdf is retained only for strict PDF metadata/page-count/encryption validation, not competing text extraction. No Docling ML/layout/table/OCR pipeline or model assets are installed in the production lock. The native-pipeline comparison did not run layout/table models and cannot establish their quality. PDF results stay partial; tables/columns/OCR remain explicitly unverified. Empty, encrypted and malformed files produce no supporting passages. The original mixed-font regression is under backend test fixtures.

Measured isolated Docling Parse geometry diagnostic: 46 pages, 1429 ms, peak RSS 186880 KiB; 512 MiB virtual-memory / 15 CPU-second cap. The production fetch-plus-extract probe took 4714 ms and retained the same byte digest and decisive page. Keep the earlier 404, script startup failure and extraction failures in RESULTS. No conclusion about competitor superiority follows.

Exact wheels and license-file digests are in docling-selected-dependencies.json; the executable Linux/Python3.12 lock is apps/backend/extraction/requirements.lock. Includes MIT Docling components, Docling bundled PDF/font assets and transitive licenses; retain their installed license/notice files when redistributing. The PyPI version advisory lookup has limited coverage and does not replace a security audit. Docling adds a native parser and Python dependency footprint; isolation, byte/CPU/memory/output limits, disabled network and no project/credential mounts remain mandatory. Full Docling and PDFium were experiment-only and are absent from the production lock.

Unfinished: larger independently referenced digital/scanned/multi-column/table corpus, table-cell semantics and OCR, native binary picker, per-file lifecycle/retention policy, general criterion-linked research and correction reuse. Rollback disables binary processing or returns unavailable; retain raw-byte ownership, deletion, geometry/digests and publication gates. Never restore pasted-text-as-PDF or raw successful-parser output as proof of reading.
