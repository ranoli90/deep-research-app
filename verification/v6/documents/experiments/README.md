Diagnostic comparison scripts, not application runtime. Original staging-script hashes are in ORIGINS.json. Reproduction copies read the single reference.json mounted read-only at /reference.json; otherwise preserve executed logic. Feed identical RFC9112 saved PDF bytes on stdin inside bubblewrap with no network, credentials or project mounts. Use the pinned comparison.lock for Docling Native/PDFium, plus pypdf 6.19.0 for the text baseline. The selected production code is apps/backend/extraction/extract.py. The full native comparison requires a read-only /etc/passwd binding for Path.home() initialization; no home directory is mounted. Native pipeline uses no models and proves no layout/table/OCR quality. Every comparison retained partial/failure evidence.
