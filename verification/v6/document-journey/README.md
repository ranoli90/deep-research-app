# Local binary document journey

Evidence: real API upload, sandboxed PDF parsing, PostgreSQL, production structured worker, report/library/source reads and account deletion. Provider model/usage responses were fabricated locally; no paid call occurred. This is not a held-out benchmark or human adjudication.

`renamed-input.pdf` reconstructs the exact tested bytes by replacing the six-byte entity in the existing synthetic PDF. Its SHA256 is in `trace.json`. The report preserves the explicit underwater prohibition while source inspection retains page coordinates and partial-access/layout warnings. The first run failed an unrelated qualification check; its failure and repaired controls are retained in RESULTS.json and logs.

Correction, broader document fidelity, web discovery, native execution and real model quality remain open. Reproduce with the registered `test:extraction` command and the pinned offline runtime; the test generates a fresh entity name each run.
