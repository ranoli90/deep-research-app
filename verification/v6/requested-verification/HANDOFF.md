Requested verification packet — W05 with W01/W02/W03/W06 controls

Implementation is a working-tree change; primary owns the reviewed checkpoint and canonical reconciliation. No paid requests, deployment, dependency addition or native build occurred in this packet.

Public behavior: controlled POST /v1/runs/:parentId/follow-up accepts strict requested-verification.v1 {reportId,reportVersion,claimId,note,evidencePolicy,idempotencyKey}. Response {runId,parentRunId,briefRevision,reused,verificationId,reopenedDiscovery:false,evidencePolicy}. Ordinary POST report challenges remains feedback storage only. Diagnostic fixture follow-up still performs diagnostic research, explicitly discloses reopenedDiscovery:true and verificationMode:diagnostic_research, and no longer manufactures a claim-verification change narrative from a question regex. R19's old expectation failure is retained; ownership/child-report assertions remain.

Recovery: POST /v1/run-requests/resolve retains ordinary {idempotencyKey}; optionally accepts verification:{parentRunId,request:<strict original request>}. Exact UUID bytes must match (400 otherwise). An existing child must match parent, canonical request digest and independently restored target proof (409 conflict/unavailable otherwise). Accepted/withdrawn response shape is unchanged. No-existing-run withdrawal is serialized by account lock, and late verification admission respects the durable 035 withdrawal. Recovery is independent of scheduling flags.

Target scope: only owned structured assertions with actual extraction/support provenance, or positive prior verification claims with restored support provenance, can be checked. Legacy/unsupported claim lineage rejects explicitly. Each target captures report/version/claim/revision/task/assertion/scope/passages+digests and selected sources. Private note is stored feedback, never assessed and never a query. Original question and immutable assertion/scope drive assessment. Repeated positive verification is bounded to eight children; cycles and ninth-child admission are rejected before recursive restoration/reservation.

Execution: reuse_snapshot assesses immutable owned selected passages; refresh_sources re-ingests selected attachment bytes or rereads selected owned URLs with durable source-read/extraction records. An identical attachment may honestly use extraction cache. No challenge/discovery search is scheduled. One assess_support operation uses the existing strict model gateway and scoped checker; web refresh can first use the existing brief operation required by durable reads. No fabricated extraction/assertion receipts are created. Result labels describe inspected-evidence support, contradiction, qualification or unresolved scope. Every result report retains limitations; blocked/unknown processing publishes no substitute report. Positive reports persist new owned claim/revision and exact citations. Negative report blocks include independently detected contrary/qualifying passage links even when the model omits evidence citations; positive claim support bindings remain unchanged. Publication independently restores target, manifest, actual provider receipt, scope and result; missing required row/result/marker cannot fall back to ordinary writing. Unknown cost retains existing financial hold/no-resend semantics.

Privacy and concurrency: admission locks active account and conversation; exact key replay, current consent, original route, owned attachments, budget allowance and outbox primitives are reused. Account/source deletion purges requested targets/private notes/results before dependent provenance, invalidates descendants and retains required markers/financial tombstones. Deletion during an issued assessment permits cost settlement only, not semantic resurrection.

Impact checklist: additive idempotent migration036 creates requested_verifications and runs.verification_required_revision; public strict contract exported by contracts; existing assess_support/brief prompt/model versions are retained, with a new independently validated single-target context composition; no new dependency/service/model route or processor. API validates, domain admission owns transactions, worker performs network through existing adapters outside transactions, pure outcome classification remains in research-core. No fixture/evaluator imports were added to production. The canonical ADR/spec/security/status/ledger/command registry must be updated by primary using this packet; this file is evidence handoff, not a competing specification.

Rollback: disable new scheduling while retaining resolve/withdraw, immutable target readers, required-marker/row detection, publication veto, deletion and receipt settlement/unknown holds. Never route admitted required checks through generic research or erase their obligations.

Evidence class: local real PostgreSQL + API injection + real worker/queue and attachment extraction, fabricated deterministic model transports and fabricated web read transport. Synthetic micro-cost receipts are controls, not charges or real-model semantic validation. No human adjudication, live quality superiority, OCR/native/hosted evidence is claimed. Failed initial typecheck, refresh fixture mock mismatch and historical diagnostic assertion are preserved. Actual eight-level lineage and cycle regressions added. Remaining limitation: arbitrary malformed provenance currently fails closed via exception/retry rather than every corruption becoming a terminal typed blocked result; no publication/spend bypass identified. Wider real-model applicability and useful unfamiliar-question native journey remain primary milestone work.
