W03/W07: apply source.patch after append backend packet. GET owned run now returns contentInvalidated boolean using exact account/run/source_deletion tombstone. True forces reportId null, because existing report lookup returns a redacted report ID. Ordinary cancellation remains false. GET rejects deleted authenticated accounts as session already does; full deletion revoked sessions already passed, retained-session tombstone regression proved missing explicit run guard. Four actual API/PG controls and types pass. Two failed receipts retained, including correction of test assumption that cancellation synchronously terminates (actual cancelling with null outcome). No mobile source, native, paid or hosted claims. Primary owns canonical docs and mobile integration.
