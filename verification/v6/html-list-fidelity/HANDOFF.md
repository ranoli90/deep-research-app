W04 development HTML list fidelity — isolated clone proposal

Root cause: frozen MC-D01 SQLite HTML contains the decisive network-filesystem prohibition in an ordered list. Trafilatura2.2.0 main XML itself omits all four ordered lists; original saved extraction has72blocks and misses that reference. A generic operational guide independently reproduces ordered and unordered restriction-list omission when two ordinary background paragraphs follow the list. No heldout task/reference/document was inspected or tuned in this packet. Frozen corpus/gold and original failure receipts remain unchanged.

Change: add bounded source-DOM list supplements after existing main/table extraction. Keep the immediate nonempty paragraph/heading lead-in (skipping only nearby empty anchors), nested source order, explicit original numbering metadata and struck-through qualifiers. Item markers identify source positions rather than invent original numbering. Exclude sanitized nav/header/footer/form content plus aside, navigation/menu roles, hidden ancestors and exact menu/navigation/sidebar class tokens. Fully retained plain list text is not duplicated; partial overlap remains explicitly warned. New list:n locators are deterministic for the same sanitized DOM/bytes and parser version; they are not publisher anchors. Supplements are separate reading-order blocks, never silently relocated into another paragraph.

Limits: inspect at most100 top-level list positions,1000 supplemental items,8 nested-list levels and250000 supplemental characters. Omitted bounded material raises supplemental_list_limit_reached; the document remains partial. Main-content omissions, separate list ordering and possible overlap are explicit warnings. Generic menu-class exclusion can omit a legitimate documentary list with that class; this is still partial extraction. Supplemental blocks may duplicate part of main text or appear beyond the model's current passage-selection bound. This packet proves extraction preservation, not downstream evidence selection, semantic support, complete reading or benchmark superiority.

Version/cache impact: new HTML extraction emits trafilatura-2.2.0/structure-v2; adapter retains structure-v1 in its accepted version enum. Existing source-read.v1 finished operations and immutable source versions are not mutated, invalidated or automatically reread. A new/explicitly refreshed source read gets current parser output and version provenance. PDF/plain-note branches keep their prior version behavior. No migration, dependency, service, model prompt or provider route change; no fresh network/paid authority. Root source remains untouched until primary review applies source.patch.

Checks: initial generic4fail/1pass retains actual OL/UL loss; first repair7/8 passed, with the cap fixture correctly deduplicated because main extraction retained its whole plain list. Cap fixture was corrected to include struck-through text needing structural supplementation; no bound assertion removed. Final8/8 HTML controls and4/4 actual PDF regressions passed. Actual MC-D01 same SHA256 f3467b530b883d4a00574fe1a898b3d121ed72764ae28cf66941080ac0badb9e now has75blocks, restriction atlist:3 and preserved single-writer span; status remains partial. Backendtypes/boundaries/diffcheck pass. Runtime /tmp/deep-v6-extraction-runtime, normal Linux bubblewrap adapter; no DB, model transports, network acquisition or native claims.

Rollback: disable new supplement generation or restore earlier extraction scheduling while retaining both version readers, saved raw bytes/digests/locators, partial warnings, ownership/deletion and immutable prior evidence. Do not relabel v1 output as repaired or drop historical failure. Primary owns ADR/spec/registry/canonical integration and any further source-selection work.
