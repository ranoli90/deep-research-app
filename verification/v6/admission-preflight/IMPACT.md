W03/W07 fresh configured-route preflight before private upload, preserving W02 retry identity. Mobile-only existing submitAdmission requires fresh authenticated settings port; selected route flag is strictly true, all false/missing/nonboolean results block save/upload/admit. No new public schema, endpoint, dependency, prompt, model route or migration. Existing Settings flags are unknown until validated in owning admission helper. Existing journal semantics/keys/account-view epochs remain. Check/Withdraw remains independently callable while routes disabled. No readiness/health/budget guarantee; server admission remains authoritative and flags may change after preflight. Rollback may revert client preflight while keeping backend authorization/consent/budget/recovery gates, but would restore known unnecessary private-upload behavior. New failure regressions and unchanged existing mobile controls; no native/live-model/paid claims. Canonical integration owned by primary.

Review failure: original /v1/session premise was false; that endpoint has no flags. Initial mocked148pass is not real endpoint proof. Actual existing /v1/settings is used in the corrected patch; prior incorrect patch/logs preserved, never root-applied. Actual API contract probe now passes on isolated PostgreSQL. See CONTRACT_REVIEW_FAILURE.md.

Corrected terminal actual API proof: api-contract.log exit0, authenticated /v1/settings booleans, /v1/session no flags, unauthorized401, disabled routes zero uploads/admissions, enabled routes actual uploads/queued admissions, accepted recovery with disabled flags. No worker/provider calls; zero provider intents. Synthetic accounts deleted, API/queue/pool closed; DB slot released. Corrected148mobile/types/JSexport648modules2.18MB/boundaries/diff all pass.
