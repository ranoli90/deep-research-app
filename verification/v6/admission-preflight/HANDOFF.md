# Configured-route preflight

W03/W07 privacy and W02 retry behavior. Isolated detached0f0c7c0; no rootsource/device/paid/network-provider actions; isolated actual API probe below. Required preflight port in existing submitAdmission is the single upload/admission flow. Each attempt requests authenticated existing /v1/settings, sends no question or document, then accepts only boolean true for the selected mode. Missing/nonboolean/false flags stop before journal writes, upload or run admission. Fresh session fetch may itself fail authentication/network; existing handling remains. Existing API settings result is validated by the owning helper before using its flags.

App removes the duplicate pending-only settings call, supplies fresh callback for all submissions and guards late account/view responses. A successfully received current authenticated response clears offline state; disabled scheduling is distinct from unreachable network. Existing canSubmit/new-draft offline gate and foreground reconnect remain, so a brand-new offline draft waits for the existing reconnect path. Pending retries preserve their exact journal and may perform preflight while offline. No new pending journal is invented on preflight failure. Initial local fingerprinting may occur before preflight; document bytes never leave the device through that preparation.

Check/Withdraw remains a separate unchanged endpoint/callback. It can resolve an already accepted unknown request when scheduling flags are disabled. Disabled preflight never withdraws or clears identity, changes route, or resends unknown requests on another key. Old journals require no migration. Existing confirmation uploads/reselection/idempotency/digest/storage and account-generation tests preserve all assertions; test IO explicitly grants enabled flags to exercise their prior intended paths.

Tests before fix:12 controls,10fail/2pass; disabled or missing flags previously reached upload/admission, and preflight callback was unused. Final full mobile148/148 (136existing+12new) passes with boolean failures, enabled ordering, preserved journal/confirmed uploads, late account/view changes, and route-disabled accepted-request resolution. Backend server admission remains authoritative: flags are a current configuration hint and can change between check/upload/admission. No cost allowance, provider health, model readiness, live semantic or native acceptance promise. JavaScript export is separate from signed/native build.

Rollback: retain all backend consent/ownership/admission gates and client protected journal/recovery. Client-only preflight revert is possible but restores known unnecessary uploads for disabled routes. No new dependencies/services/public schemas/prompts/routes/migrations. Canonical ADR/mobile-spec/status integration is primary-owned.

Review failure: original /v1/session premise was false; that endpoint has no flags. Initial mocked148pass is not real endpoint proof. Actual existing /v1/settings is used in the corrected patch; prior incorrect patch/logs preserved, never root-applied. Actual API contract probe now passes on isolated PostgreSQL. See CONTRACT_REVIEW_FAILURE.md.

Corrected terminal actual API proof: api-contract.log exit0, authenticated /v1/settings booleans, /v1/session no flags, unauthorized401, disabled routes zero uploads/admissions, enabled routes actual uploads/queued admissions, accepted recovery with disabled flags. No worker/provider calls; zero provider intents. Synthetic accounts deleted, API/queue/pool closed; DB slot released. Corrected148mobile/types/JSexport648modules2.18MB/boundaries/diff all pass.
