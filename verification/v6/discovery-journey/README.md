# Discovery/read integration evidence

W04/W05, local API + PostgreSQL + production structured worker + actual isolated Docling Parse. See trace.json and input.pdf for exact bytes, passage/page geometry, final assertion revisions and support guards.

The search/model responses are fabricated. The safe-fetch boundary is replaced with these saved bytes; no external network or paid provider call occurred. Real safe-fetch security has separate transport regressions. The production readSource, durable read coordinator, extractor, assertion/support/writer/publication services and source/report/library APIs execute. Account deletion is checked after inspection. PDF remains partial, with OCR/table/layout warnings.

This is an integration control using a renamed entity, not held-out semantic evaluation or proof of discovery quality. Corrected follow-up is not yet tested. Run with the registered extraction command; raw output is ../source-reading-extraction.log. No native/hosted proof or release claim.
