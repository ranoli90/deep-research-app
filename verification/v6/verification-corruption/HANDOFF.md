W05/W02 deterministic target corruption packet (isolated clone only)

Scope: explicit typed domain errors at stored verification target/schema/row/digest/original-target boundaries. Worker handles only this class before any fresh model/read issuance; ordinary errors with identical text, PostgreSQL transient errors and LostWorkerLease retain their existing propagation/fence semantics. No generic exception-to-terminal conversion.

Blocked result uses a closed reason code without target/private note/schema diagnostic values. Missing obligation remains required; no report or substitute work is allowed. Existing settleRun determines confirmed receipt charges and retains the entire admitted hold if any issued outcome is unknown; target corruption must not erase liabilities.

Tests are added before source repair in detached5fbec3a plus reviewed Vitest4-only dependency patch. Real PostgreSQL run requires primary exclusive slot after ongoing extraction. Synthetic prior financial rows have explicit unique key/project scopes (never NULL historical liability contamination). Transient/lease/lookalike exceptions are injected boundary controls, not real network outages. No provider calls/spend are authorized; existing fabricated parent gateway harness remains labeled.

Impact: no schema/migration/dependency/public contract/model prompt/route change in source patch. Error messages and HTTP status remain bounded; new class enables precise internal handling. Production proof/publication/readers remain fail-closed. Rollback disables new scheduling rather than restore a generic fallback; keep financial holds, target markers, deletion and publication veto. Primary owns root integration/canonical reconciliation after review.
