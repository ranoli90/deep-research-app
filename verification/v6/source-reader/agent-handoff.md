> Historical pre-device handoff preserved below. Native follow-on observations and cleanup supersede pending device language: see RESULTS.json and root HANDOFF.md.

# Source reader integration handoff

W04/W07, V6-F17/F18, M04/V2-12. Mobile-only implementation over checkpoint 002e2d6, uncommitted. No dependencies, schema, migration, provider route, paid calls, database tests or deployment. Exact receipts and hashes: RESULTS.json. Retained failure: actual extractor table-cell objects were initially rejected, reproduced by a new regression, then fixed without removing protections. Final 67 mobile tests/typecheck pass; Android JS export633 modules/2.1MB, not native build.

Canonical MOBILE_SCREEN_STATES amendment for primary integration: The source sheet validates source metadata before rendering, preserves exact passage text and ordered extracted table cells (header/span/scope), displays actual document page/block, coordinates with their recorded coordinate system, publisher, access/coverage, extraction method, warnings and immutable source version. Dates unavailable from the current API are explicitly unavailable. Coordinates are textual inspection, not a rendered/highlighted PDF. Only HTTP(S) original links without embedded credentials are offered, by explicit user action; local/private document locators stay non-linkable. Source account/request fencing remains unchanged. Reading anchors use the actual invoking report block and signed within-block offset relative to the report card; normal scrolling saves the visible block at drag/momentum end. Removed-block fallback resets stale offset. Regression tests are pure/contract and static component-preservation checks, not native acceptance.

Rollback: revert the source presentation/anchor changes as a small mobile packet; preserve account/request fences, source API ownership, exact evidence and extraction limitations. Do not restore hardcoded answer-anchor behavior when retaining the enhanced reader. No data migration or server rollback is needed.

Remaining: native source open/close/back, process reopen, text scaling, screen-reader focus restoration, rotation/long table behavior, current authenticated full research/correction/share journey. No claim of screen-reader focus return implementation. Reading anchors are saved at scroll-end, so abrupt process death mid-drag can lose the latest partial gesture. Public source dates require a future backend response change. Upload idempotency, content-cache encryption and correction-draft recovery remain separate. ADB authorized reconnect succeeded to10.0.0.167:43417; no app launch or raw picker inspection performed by this agent.

Files: apps/mobile/App.tsx, src/{SourceSheet.tsx,source-view.ts,state.ts,report-layout.ts}, test/{source-view.test.ts,lifecycle.test.ts,report-layout.test.ts}. Existing static accessibility/layout checks now read the extracted component where those elements moved; assertions remain.
