# Corrected document journey

W04/W06 local API, PostgreSQL, actual sandboxed PDF parsing and production structured worker. Exact bytes and passage/support trace are preserved here.

The first question asks about underwater recording. The accepted typed replacement asks for the offline firmware requirement. The corrected report retains the exact original passage/source-version identities, with no child source rows; it recomputes the task, assertions, support, writer and final checks. A separate full-rerun API request over the same document creates its own source version and reaches the same corrected text.

All model responses are fabricated boundary controls. This is not independent semantic adjudication, a live quality benchmark or evidence of selective model-call savings. The full rerun may use the existing digest-checked attachment extraction cache; it independently executes the research operations. The account and all derived content are deleted after inspection. Parser cancellation/deletion tests now explicitly wait for actual extracted blocks before revocation.

Command and raw results: ../RESULTS.json and ../correction-membership-extraction-final.log. No paid calls, native execution or deployment. Granular patches, result-derived change summaries and native correction input remain open.
