# W01/W02/W05/W06/W08/W09 — ZDR routing and exact quotation coordinates

Runtime commits: `634a8c8` pins disclosed Azure routing per immutable run; `f12c2fc` adds opt-in exact-quote-v2 coordinate resolution. No model, prompt, dependency, production default, hosted service or account privacy setting was silently replaced. Migration041 preserves all legacy OpenAI request identities. Children inherit the parent policy.

## Evidence classes

- **Actual provider:** `live/receipts.jsonl`, `EXECUTION.json`, `COST_CHECK.json`. Registered MC-D01, public saved SQLite HTML, production API/worker. A1 received Azure HTTP200 and a separately retrieved generation receipt for $0.0005388, then failed exact-span validation. The rejected output was not retained, so wrong offsets versus a nonmatching quotation cannot be distinguished. B timed out without a generation ID. Two of four steps attempted; correction and full rerun unrun. No report, source extraction, semantic score or human adjudication succeeded in this live comparison.
- **Actual financial operator action:** `REJECTION_RECONCILIATION.json`. Exactly three earlier completed plugin-free HTTP404 requests were reconstructed and bound to the same credential/account. Documented error waiver, unchanged key usage and zero daily usage supported an explicitly labeled operator zero-cost decision. Original error receipts remain unchanged; no generation cost receipt was invented. The later timeout is not covered and retains its21658micro reserve.
- **Local production-path controls:** `three-policy-controls.json.gz` and `LOCAL_JOURNEY.json`. Actual binary uploads, isolated HTML/PDF parsing, PostgreSQL, API, worker, support, writer, publication, correction and replay. Six selected cases pass across three policies; eleven other tests were not selected. Model responses and cost receipts are fabricated, with zero real spend. Renamed synthetic device questions are controls, not held-out semantic evaluation.
- **Local correctness:** `verify-span.txt` passes173core/170backend/206mobile/6governance plus configured types/boundaries. `span-integration.txt` passes20real-PostgreSQL controls; `span-unit.txt` passes7pure controls. Legacy byte digest, provider mismatch, immutable policy, quote ambiguity, wrong handle and replay remain tested.
- **Preserved failures:** initial type/unit mistakes remain in their original logs. `integration.txt` has452pass/6fail and spanned source edits/concurrent checks, so it is not final-revision evidence. `timeout-recheck.txt` has two timeout failures while other suites ran. `integration-serial.txt` is the fresh frozen-source run; consult RESULTS.json for its final outcome. Assertions and timeouts were not loosened.

The full v6 W01–W09 milestone remains incomplete. W10 native/hosted/store/release gates are separate and unexecuted here. No deploy, push, private-document test or native build. Current provider quota is key capacity, not a claim about total account credits.

Rollback: disable new Azure/v2 admissions; retain migration041, admitted policy readers, consent, exact request identities, ownership/fences, publication controls, original receipts and unknown holds. Do not reinterpret old runs or resend the uncertain action. The registered evaluator remains stopped on unknown outcomes. Next paid evidence must preserve this failure denominator and reconcile that specific timeout before using the current protocol again.
