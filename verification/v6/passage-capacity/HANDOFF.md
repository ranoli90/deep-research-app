# Bounded whole-passage capacity packet

W01/W02/W05/W06/W08; base5fb44af682a58c7b417fae64e6f8a4a40e2d3f2c plus reviewed HTML structure-v2 prerequisite in isolated clone. Root sources untouched. `source.patch` contains only five backend model/proof readers and two new test files; excludes HTML changes and package/lockfiles.

One backend model-port constant raises whole-passage membership from24 to128. Extraction selection, ModelContext, support manifest, requested-verification target and proof use the same bound. Selection still takes all authorized readable passages; UUID order is identity stability, not relevance. No batching, ranking, text truncation, output limit change, or query changes. Request fingerprints retain complete context; smaller inputs generate unchanged requests. No public API/schema, prompt, migration, dependency or provider route changes.

Per-passage24000characters, ordinary40sources, verification24sources, serialized context240000bytes, complete body<=128000bytes, output4096tokens and full-context21658micro reservation remain. Larger count does not guarantee serialized context fits. Every oversize run still may incur a brief before rejection; controls assert only that no extraction/support attempt is issued afterward. No price ceiling increase or external exactly-once claim.

New production test includes late exact firmware qualifier at25, full input text/IDs/digests/sourceVersions equality through extraction/support/writer, report, no-dispatch terminal replay, immutable snapshot requested check and correction. Separate late contradiction reaches support and persists disputed, with no report.128 small whole passages run through production publication.129 and serialized oversize block after brief alone. Unit checks preserve per-text/total byte/price bounds. Receipts are fabricated model/extraction transport data on real local PostgreSQL, not parser output/live model quality. SQLite frozen corpus and heldout untouched.

Failures retained: original25 invalid_extraction_selection; initial test UUID reuse collided with retained redacted passage IDs (unique per-run prefix fixes fixture); synthetic attachment locator lacked real attachment (changed to web source plus fabricated empty public search for correction); old error expectation was model_context_too_large while unchanged full-body gate correctly emitted model_context_exceeds_policy;128 workload exceeded generic30s test timeout (only that case has120s); authoritative support decision is scoped_support_results.disputed, while legacy claims.support_status remains unverified. Final test uses exact typed decision rather than weakening allowed outputs. Preliminary late-contradiction before-fix missing context TypeError retained; improved explicit assertion records invalid_extraction_selection.

Rollback must retain expanded immutable proof readers/required markers for admitted checks. Disable scheduling if needed; reverting reader capacity on admitted obligations would destroy replay ability. Ownership, digests, financial liabilities and unknown holds remain. No commits/paid/network/native/deployment claims. Increased passage count increases local proof/database work; measured synthetic boundary runtime is not a live latency benchmark. Remaining semantic attention, applicability, factual correctness, large-document context strategy, source-count limits and independent quality evaluation remain open.

Terminal receipt:40/40 PostgreSQL tests (5new+35existing),214.48s exit0; unit1/1, backend types/boundaries/diff exit0. DB slot released to primary. No broad suite repeated.
