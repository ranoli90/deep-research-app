# Saved HTML and evidence encoding checkpoint

Runtime implementation: `643e20358c5d05a0480e701215f5f1df7963d68c`. Stronger numeric publication regression: `47c3c1a53e637cd83b08e04e1c8d756095dc48d4`. Branch `codex/v6-saved-html`, based on merged main `4d818feb1dcf8808b38c980ac2f2f204977e2d7b`. No push/deployment/native build occurred. All three original user ZIPs remain untracked and untouched. INPUT_RECHECK verifies both original probe-module Git blob hashes and the review-base ancestry.

ADR054 implements saved UTF-8 HTML uploads up to1,500,000bytes through the owned binary API. They remain stored/not-read until the existing isolated parser runs. The frozen evaluator uses this same API/worker/writer/checker, with no discovery, external script execution or inferred original-site authority. Structure-v4 repairs reproduced UTF-8 country/entity corruption without charset metadata, exposes legacy decoding uncertainty and reparses old cached HTML for new ingestion. Existing immutable v1–v3 receipts remain readable; prior reports are not silently rewritten. Native picker formats remain PDF/text/Markdown.

## Evidence and failures

RESULTS.json owns exact commands, command-start revisions, environments, exit codes, scopes and compressed raw logs. SOURCE_FILES.json pins the final implementation/test tree; FINDINGS_RECHECK maps all19 review findings to current regression checks and limits. Local unit/types/boundary/governance verification passes157core/149backend/206mobile/6governance. Actual isolated extraction plus PostgreSQL API/worker tests pass51/51. Additional cached-v3 upgrade control passes1case with12explicitly deselected; the broad suite covers those other cases. Backend test types pass after the assertion repair.

The first full PostgreSQL run retained423passes/1timeout in an unchanged25-passage journey. Its unchanged focused rerun plus new HTML upload passes6/6. The second full run retained424passes/1test failure: a valid claimUUID containing9999 triggered a blanket serialized-object substring assertion although all three invented statements were correctly withheld. Forced UUIDs ending999 reproduce this failure. The replacement asserts the exact id/kind/text of all four blocks, retaining publication, unresolved-key, caveat-count and supported-text assertions. It passes the focused regression. No runtime gate or timeout was weakened. The final full suite passes425/425 across31files in443.46s on the committed implementation and test repair. Earlier failed results remain failed historical attempts; this is the subsequent full passing receipt.

JOURNEY_SUMMARY.json and eval-traces-v4.json.gz preserve actual source/assertion/support/report identities. The randomized synthetic HTML journey produces the original underwater-recording qualification, then the corrected firmware4.2 qualification; correction reuses3exact passages and fresh rerun uses separate versions. Both use7fabricated attempts/7syntheticmicro; there is no model-call saving. Measured2680ms corrected versus3356ms full rerun is local synthetic timing, not live superiority.

The registered SQLite input preserves75actual passages, including network-filesystem and single-writer restrictions, with exact bytes/digest/locators. Fabricated transport yields no relevant assertions and no report. RFC9112/TMP117 likewise retain unresolved no-assertion failures; ESP32 retains78passages but exceeds model-context policy. Real source extraction is not a useful semantic answer. Semantic scores and human adjudication remain null. Paid calls/cost/actual external unknown outcomes are0; synthetic unknown holds are retained in isolated test databases. Current project monetary allowance is unknown.

## Remaining work and rollback

The W01–W09 milestone is incomplete. Next independent implementation target is bounded evidence selection for large documents, preserving decisive table/neighbor qualifications and explicit omitted-context limits. Use development cases; do not tune to held-out reference answers or simply raise policy caps. A current explicitly authorized, balance-checked non-sensitive live original/correction comparison is needed for semantic usefulness; this ZIP grants no spending authority. No broad design expansion is justified by these synthetic controls.

W10 remains separate: hosted identity/network/storage/retention/restore/abuse/privacy readiness, current signed native builds and both-platform/accessibility acceptance. No new native check was run here; historical Android evidence retains its original scope. OCR/difficult layout and broader source-reader acceptance remain partial/unavailable.

Rollback removes HTML from new admission/evaluation allowlists or disables structured scheduling, while retaining parser/receipt readers and admitted bytes, immutable memberships, source inspection, deletion, authorization/fences/publication gates, receipt reconciliation and unknown holds. Never restore ready pasted-HTML notes, fixture fallback or unsandboxed parsing. No background continuation is promised after execution stops.
