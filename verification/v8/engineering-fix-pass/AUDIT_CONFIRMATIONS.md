# Independent explore confirmations (orchestrator)

Reviewed at integration `72c78ea` / `d56b0dc`. Explorers did not edit production code.

## Wave 3 (FP-022–028) — all CONFIRMED
Unknown tokens default `user-public`. Pending approval not consumed. Approved private terms unioned. Gate A is blanket. No CHECK/unique on `query_authorizations`. Attachment counterevidence blocks categorically. Token heuristic splits hyphens.

## Wave 4 (FP-029–048) — CONFIRMED as listed
Azure live `maxResults=3` (`AZURE_DISCOVERY_POLICY` copies v1). `queries`/`classesAttempted` process-local. Full-text `readable` predicate is `partial-text` only (latent: inserts stamp partial-text). `userSuppliedUrls` never fetched. Unknown freshness not unmet. `prefer_primary` excludes secondaries.

## Wave 5 (FP-049–059) — CONFIRMED
Candidate ledger not imported by backend src (P0). Per-conclusion falsification helper not imported (P0). Evidence Needs used as in-memory gate only.

Do not treat helper unit tests as production PASS.

## Wave 7 (FP-077–095) — CONFIRMED
Mobile ignores typed `activity`; `/events` still returns raw `publicSummary`. Assumptions POST is FP-003 in-place mutate. IME +16 does not clear Gboard suggestion bar. Gate A/pending approval overlap Wave 3.

## Wave 6 (FP-060–076) — CONFIRMED / PARTIAL
FP-068 outcome bypass still `if(outcome!=="completed") return true`. FP-073 calculationKeys strip fixed in `72c78ea`. FP-062/063 empty-array fail-closed; omitted writer-key fill still invents support. Reconciliation lexical.
