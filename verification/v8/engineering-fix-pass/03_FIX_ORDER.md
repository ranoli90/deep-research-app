# Fix order

## Wave 0 — freeze truth
- Keep `main` untouched.
- Snapshot exact integration SHA.
- Mark all disputed matrix gates FAIL/PARTIAL.
- Do not change test expectations merely to recover green.

## Wave 1 — research-state invariants
Fix FP-001–010.
Goal:
- original question immutable,
- confirmed clarification passed separately,
- semantic changes revisioned,
- explicit pending-input identity,
- full planning-state digest.

Run focused API/task/correction/replay tests.

## Wave 2 — provider/cost/replay
Fix FP-011–021.
Goal:
- durable attempt chain,
- fallback persistence/replay,
- unknown-cost HOLD,
- no repair after unknown financial outcome,
- correct reservation math.

Run crash-boundary and financial tests before any live calls.

## Wave 3 — public-query privacy
Fix FP-022–028.
Goal:
- no default user-public provenance,
- exact query proof,
- pending approval consumed,
- no cross-query private-term reuse.

Run canary/adversarial tests.

## Wave 4 — retrieval durability/depth
Fix FP-029–048.
Goal:
- Azure v3 search breadth,
- persistent/reconstructable controller state,
- robust source read semantics,
- correct freshness,
- actual direct-URL path,
- prefer-primary semantics.

Run crash/restart, blocked-source and freshness suites.

## Wave 5 — innovation becomes real
Fix FP-049–060.
Goal:
- durable Evidence Needs,
- real candidate completeness,
- per-conclusion challenge state,
- semantic document/web reconciliation,
- honest intent capability.

Do not mark these PASS from helper unit tests.

## Wave 6 — support/writer/publication hardening
Fix FP-061–076.
Goal:
- no inferred model assessments,
- limited publication proof,
- stable arithmetic path,
- useful grounded wide-task report.

Run full PostgreSQL suite twice from fresh DBs.

## Wave 7 — functional mobile contract
Fix FP-077–095.
Goal:
- typed activity DTO is authoritative,
- stale approvals gone,
- revision-safe assumption/steering UX,
- keyboard send usable,
- deleted identities centrally rejected,
- strict API schemas.

This is functional correctness, not the visual UI phase.

## Wave 8 — schema and evidence hardening
Fix FP-096–120 as appropriate.
- DB constraints,
- matrix honesty,
- requested verification efficiencies,
- exact final evidence.

## Wave 9 — final gate
At one exact SHA:
- `pnpm verify`
- fresh migration
- upgrade migration
- full PG twice
- extraction
- targeted concurrency/crash suite
- live semantic journeys needed to validate changed behavior
- rebuild current APK only for functional mobile changes
- regenerate acceptance matrix.

Only then begin the dedicated visual/Product Experience phase.
