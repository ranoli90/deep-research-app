# ENG-004–009, ENG-030 provider change evidence

Base: d0ddbb264b5b93ee123ee37760beb77db926a0dc. Lane: codex/phase-a-provider. These are local deterministic transport/real PostgreSQL controls, not live model or release acceptance.

Implemented: conservative byte-bounded input cost plus operation output maximum and 25% reserve margin without legacy clamp; strict v4 context byte upper bound plus output and 1024-token overhead; immutable per-operation admission before live reserve; fresh-operation health check repeated in reserve transaction; missing/retired routes fail closed; processor mismatch retires the route; historical results and held attempts still restore. Beta has one model family (GPT-4o-mini), distinct processor/policy identities do not claim quality tiers. Explicit cache session support is unavailable; observed cache receipt fields remain observations. Strict repair prompts receive only validated structural issue paths/codes. Unknown cost, including successful text without usage, stays held; transient unknown primary outcomes cannot trigger availability fallback.

Impact checklist: no dependencies/services/public API added. Migration 048 adds server-only route-health and operation-admission records; immutable request and policy identities unchanged; new strict-v4 policy identities for new unpinned admission, low-level explicit historical pins and children preserved; production configuration advances legacy configured policy to same-processor strict-v4 for new API admissions. Account/source deletion removes operation rows. Health rows contain no customer content. No source text grants route authority; no new processor, paid evaluation, deployment or spend grant. Strict repair feedback is versioned by strict policy identity and bound in request digest. Historical prompt bytes remain unchanged. The changed cache assertion removes a false capability claim; the synthetic higher-tier test now uses a distinct fake model; known-zero failover controls use HTTP404 and separate HTTP429 HOLD regression preserves unknown-outcome protection.

Rollback: disable new admission or retire affected routes; preserve migration tables, historical policy readers, immutable receipts, exact request identities and unknown holds. Never restore the old reserve clamp or unknown-outcome failover. Parent integrates canonical STATUS/ENGINE_CONTRACTS/SECURITY_PRIVACY_COST/ledger/handoff and exact final acceptance evidence.

Checks executed on the lane working tree: backend typecheck exit 0; focused unit 57/57 exit 0; complete backend unit 235/235 exit 0. Artifacts attached. Focused PostgreSQL failures are retained: migration rerun lacked IF NOT EXISTS (fixed), and fallback test expected the former unknown primary although its trigger is now known-zero404 (fixed). Final focused PostgreSQL: 12 selected tests passed, 152 intentionally outside the selected pattern, exit 0. Production admission advancement typecheck and focused units are currently running; their result is not yet established. An initial whole gateway/policy run was interrupted after code changed and is not acceptance evidence. Final parent verification must execute exact integration SHA; these component results do not pass the Phase-A gate.
