# Security, privacy, economics and operations
Owner roles: security/privacy + reliability/cost. Status: requirements and economic model, not verified controls. Reviewed: 2026-09-16.

## Trust boundaries
The mobile client owns presentation, never provider secrets or authoritative entitlement. The API authorizes every object and request. The worker has bounded task permissions. Model suggestions are untrusted. Webpages, snippets, files and provider tool outputs are untrusted data. Runtime tool permissions cannot be expanded by source content. No read action authorizes purchases, outbound messages or cross-user access.

Prompt-injection defenses require privilege separation and data-flow controls, not only an instruction to ignore malicious content (S36). Fetching must enforce DNS/IP/redirect/network restrictions, including IPv4/IPv6 loopback/private/link-local/metadata targets, credential URLs, unsafe schemes and response limits (S37). Put controls at the egress/network layer; repeat checks on redirects and resolved destinations. Never bypass access restrictions or licensing.

## Threat → control → required test
| Threat | Deterministic boundary | Acceptance evidence to obtain |
|---|---|---|
| Malicious source demands secret upload or new tools | Source treated as data; tool allowlist + argument/auth/policy validation | Injection fixture yields no unauthorized tool call, query leakage or policy change |
| Private text leaks into public search | Query-minimization/classification with explicit user processing scope | Canary private phrases never reach search logs/adapter in protected tasks |
| SSRF or malicious redirect | Network-target resolution and revalidation; isolated fetch service; byte/time limits | Loopback, metadata, IPv6, redirect and DNS-change suites in deployed-like egress |
| Cross-user report/evidence access | Tenant-scoped queries, ownership at every entry, signed limited artifact access | Two-user negative tests over guessed IDs, versions, event cursors and exports |
| Hostile document/parser bomb | MIME sniffing, size/page/CPU/memory limits, constrained parsing and sanitized output | Malformed/encrypted/scanned/oversized files fail clearly without false “read” claims |
| Late completion after deletion | Consent/cancel epochs, tombstones, fenced publication, retention-aware redrive | Delete during fetch/write, then deliver late callback; no private resurrection |
| Fake purchase/duplicate charge | Server verification, idempotent entitlement events, append-only ledger | Replay, refund, out-of-order renewal, restore/account-switch tests |
| Sensitive diagnostics | Redacted structured telemetry; least-privilege audited access | Log/trace/export scan excludes prompts/files/tokens/signed URLs by default |

## Personal data and retention
Obtain explicit processing permission before sending personal data to third-party AI; disclose actual gateway/downstream processors and optional search/extraction services. Apple's applicable third-party AI permission rule is a release requirement, not approval assurance (S41). Google Play also requires relevant in-app AI output reporting (S42).

Provider training opt-out, retention, gateway logging and a separate retrieval processor are distinct settings. OpenRouter exposes routing/privacy controls, but a ZDR model route alone does not make the complete app zero-retention (S29–S30). Route allowlists must preserve agreed privacy/geography constraints through failures; do not silently fall back to a noncompliant processor.

Keep long-term inferred personal memory off by default. Saved preferences are explicit, viewable and removable. Reports and raw source storage have declared retention classes; retain limited supporting passages when source terms do not allow full caching. Delete private original/derived text, caches, future embeddings and affected report versions on valid deletion. Preserve only permitted minimal audit metadata. Backups have a documented expiry and deletion-tombstone replay after restore. User-downloaded external copies cannot be recalled; state that limitation.

Account deletion and subscription cancellation are different actions; explain both without claiming an app can always cancel a store subscription. Revoking consent prevents future processing and triggers applicable stored-data rules. User-supplied documents cannot authorize new connectors; connectors remain out of initial scope.

## Per-run economics
No measured costs or retail prices exist in this review. Use actual versioned provider tariffs when implementing, not numbers copied from an old prompt. Each run records:

`C_run = C_model_input + C_model_output + C_reasoning_if_separately_billed + C_search + C_fetch + C_browser_if_used + C_extraction_OCR + C_worker + C_storage + C_observability + C_retries`

Do not double-count reasoning already included in a provider’s reported output charge. Some tools bill per call, per search, per page or via included allowances; normalize units and state uncertainty. Measure cancelled/failed runs as well as successful ones. Prototype spend can be subsidized; production economics cannot assume that subsidy persists.

`Cost per usable decision = total cost of all attempted tasks, including repair and failures / number of tasks adjudicated usable`

This denominator is more informative than average cost per generated report. Track median and tail costs, reuse savings, reserve utilization, uncertain provider cost, user repair minutes and refund rate. A fast cheap incorrect report is not an efficiency win.

## Concurrent budget enforcement
Maintain separate customer allowance and operator-cost ledgers. Before dispatch, within a row-locked/serializable transaction, compute remaining authorized spend after settled spend and all outstanding reservations. Reserve an enforceable upper bound for the action plus finishing headroom. Competing branches cannot each read the same available balance and spend it.

If a provider cannot bound hidden internal searches/reasoning, a local token cap is not a hard dollar cap. Use a route whose bound fits policy, obtain explicit bounded-risk operator authorization, or decline the route. Never advertise certainty the integration cannot enforce.

Issued calls with unknown outcomes retain their reservation until reconciled or resolved under a documented conservative policy. A customer may receive a goodwill credit even if provider spend occurred; keep that adjustment distinct. Avoid silently debiting twice because a retry completed after the initial timeout. Enable user/day/global limits, concurrency controls, circuit breakers and a live-spend kill switch. Reconciliation itself must not bypass consent.

## Latency and quality tradeoffs
Start with one model route and explicit retrieval. Use deterministic bookkeeping/calculation rather than model calls. Add a specialist verifier only for consequential or disputed claims, and measure its false positives as well as catches. Parallelize independent questions only when elapsed-time savings exceed coordination/budget cost under the same quality gates. Browser or visual extraction is conditional on a demonstrated accessible evidence gap.

The strongest objection to detailed claim lineage is overhead. Use material claim granularity, retain a full bounded recomputation fallback and measure whether selective repair actually pays for its maintenance. No exact cents/report promise is justified before a live benchmark.

## Operations and release
Record model/config/schema/prompt versions, effective processor, run and tool IDs, ordered stages, redacted failures, reservations and measured usage. Health/readiness separates process liveness from database/provider readiness. Operator access supports safe run lookup, unknown-outcome reconciliation and permission-aware redrive without exposing a developer dashboard to customers.

Required drills: worker death during fetch/write, queue duplicate, database interruption, storage failure, token expiry, provider outage, allowance race, deletion during run, stale mobile cursor, backup restore with tombstones and rollback. A written runbook is not a passed drill. Record actual environment and outputs before release. No production services, stores or subscriptions were changed in this review.

## Revision 3: notification disclosure and dispatch boundary
Notification content is nonsensitive by default and never substitutes for report authorization. Dispatch checks current account/device binding and consent before sending; every deep link reauthorizes. Revoke unsent jobs and app-controlled caches/alerts on logout or deletion. Already-issued generic remote alerts cannot be reliably recalled; do not claim that an epoch or dedupe key provides that transport power. Provider handoff, device receipt and user reading are different events. `specs/ENGINE_CONTRACTS.md` §10 owns the completion/delivery/unknown-outcome contract; N01–N03 in `research/RECONCILIATION_SOURCES.json` supply the newly checked official delivery documentation. Local database evidence does not establish hosted auth/storage/retention behavior.


Local W02 accounting implementation: active owned run allowances and run caps now gate provider reservation alongside the project bucket. Controlled-run settlement derives confirmed provider cost from receipts. Any missing receipt retains the entire allowance conservatively; a later transactional settlement after reconciliation releases it once. Actual overruns are recorded without clamping. Provider-key buckets and automatic receipt reconciliation remain unfinished; historical settled rows are not retroactively claimed repaired. The existing default live-call reserve exceeds the run cap, so that route fails closed until a genuinely bounded authorized route is configured.

Provider-key cap: migration014 adds opaque credential identity for new OpenRouter intents. LIVE_KEY_SPEND_CAP_MICRO defaults to zero; admission counts confirmed or unknown cost across accounts/project scopes under a shared key lock. Null historical identities count against every key conservatively. Credentials remain server-only and are never stored in this ledger. Key/project/run/account checks are independent; none grants permission to spend without current authorization.

Receipt reconciliation now serializes on account/run ownership and applies an immutable actual amount. Repeated identical receipts are idempotent; conflicting amounts reject. A confirmed receipt updates active provider spend and settles terminal/deleted-account allowance only when all outcomes are known. This is financial bookkeeping for already-issued requests, never authority to resend or restore deleted content. Missing provider receipts are not fetched automatically. Migration015 records amount/basis for new settlements; historical null-basis settlements remain explicitly unreconciled and are not automatically recharged.

`GET /v1/runs/:id/cost` returns a repeatable-read snapshot with confirmedProviderMicro, heldProviderMicro, unknownProviderIntents, simulatedFixtureMicro and allowanceReconciled. Live spentMicro is confirmed provider cost only; intentTotalMicro includes held exposure and is not confirmed spend. Reconciled describes provider/run ledger consistency; allowanceReconciled separately requires a matching recorded settlement basis/amount. These amounts exclude unmeasured worker/storage costs. Fixture tariffs remain labeled simulation and do not appear as live provider costs.

Structured text operations require consent policy2026-09-17, which names OpenAI via OpenRouter. The gateway requires current recorded consent, uses the pinned provider without fallback, and stores only whitelisted receipt metadata separately from deletable outputs. New controlled-run admission requires current consent; request defaults use the same canonical constant. Structured route activation remains off by default. Public endpoint metadata is not permission to spend or proof of live correctness. No real private documents or paid calls were submitted in this checkpoint.

Migration017 research_tasks stores private derived criteria, questions, server IDs and question digests. Account deletion removes these records explicitly; deletion of source model-operation records cascades to the task. Adoption/reuse occurs only for the same owner/run/brief revision with an intact versioned gateway receipt binding, inside worker fences. No shared-account semantic cache, new processor, new spend route or public endpoint is introduced.

Migration018 model input manifests contain selected passage/version IDs, access classifications and hashes, not duplicate source text. They are private derived metadata and share model_operation_results deletion. Whole-passage selection rechecks current owner/run/task; it does not authorize cross-run reuse. New/changed source bytes with a stale digest reject before spending. Old NULL manifests stay unknown; rollback must disable structured operations instead of restoring unvalidated cached-result reuse.

Migration019 adds private derived assertion mappings and scoped support results. They bind owner/run/task/model operation to exact claim revisions and evidence/scope digests; account deletion purges them explicitly, with cascades from deleted model operations/tasks. All content writes remain in the fenced account/run transaction. The support call uses the existing pinned processor, reservation and unknown-outcome rules, outside database transactions. No new processor, spend route or public endpoint.

Scoped publication derives approval only from owned persisted assertion/check records and recomputes checks against current source manifests. Missing/partial/stale managed checks do not enter the legacy literal fallback. The final gate revalidates without minting content records and retains the existing account/consent/revision/publication transaction. Route/prompt pins were moved unchanged to the backend model-policy port so domain validation and adapters share one server-owned policy; this grants no new processing or spend authorization.

Migration020 research_drafts stores private operation/task/basis links, not a new source copy. Account deletion explicitly purges lineage; model/task deletions also cascade. Writer-derived assertions use original owned passages, retain premise revision links and partial dependency completeness, and cannot self-reference a draft as evidence. Synthesis is not published until its exact text has its own support record and final gate. Checker-version rechecks retain old decisions and do not resend a known model comparison. No new processor, provider route or spend allowance.

Coverage review uses the same bounded consented structured gateway and reserved logical attempts; no network I/O occurs within its fenced transactions. Migration021 coverage data is private derived evidence and is purged on account deletion. Stored replay requires exact owner/task/basis/claim revisions and current deterministic checker versions.

Generic writing now includes one separately reserved structured coverage review. Invalid/unknown review outcomes do not publish. Publication revalidates the saved review without network I/O, under existing account/run locks. No new allowance or live authorization.

Structured processRun now uses the existing consent/account/run fence and bounded model reservations throughout task, extraction, support, review and writing. Explicit failed outcomes settle known spend without releasing unknown provider holds. No new allowance or public network authority is introduced. Structured discovery remains unavailable pending the bounded adapter integration.


W02/W05 search transport now rejects redirects, enforces a 45-second total fetch/body deadline and 1MB strict-UTF8 response limit, validates bounded citation envelopes, rejects malformed/truncated output, and preserves parsed actual cost on invalid results. Titles no longer become invented snippets; citations deduplicate by URL and share an origin cluster. No engine/model/prompt/allowance change or paid call. Durable discovery output, explicit processor/policy selection and structured-worker discovery remain open.
Impact: existing adapter only, no dependency, migration, public schema or new service. Query/output bounds are checked locally; unknown transport outcomes are not converted to zero cost or empty successful search. Response metadata is parsed before output acceptance so malformed citations retain known cost. No retry/fallback is added. Existing reserve200000 and run allowance remain unchanged, so ordinary live admission can still block. Eight nonbillable transport cases cover valid citations, malformed envelopes, too many results, truncation, missing costs/snippets, oversized/invalid bytes, timeout despite ignored abort, credential URLs and returned-model mismatch. Rollback disables live discovery while retaining bounds, receipts and safety gates.


W02/W03/W05 durable discovery impact: new default-off STRUCTURED_DISCOVERY_ENABLED policy pins openai/gpt-4o-mini through OpenAI without fallback, and the single-search web plugin to Exa auto with at most3 results. Consent2026-09-17.1 explicitly names Exa; old consent cannot issue. Model price ceilings reuse the structured text route; reservation28658microUSD combines the full advertised model-context/output ceiling with the documented7000microUSD single Exa search fee. This is a versioned documentation-based bound, not a verified live invoice or protection against an external tariff change; actual overrun accounting remains visible and live activation still requires current authorization and a registered probe. No allowance increased. The existing legacy search policy remains separate.
Strict executable search proposals revalidate exact original-question spans and query terms after trimming; source text is never included. Mixed attachment tasks fail closed until explicit public-query approval is implemented. This is not a complete sensitive-question classifier. A stable logical identity binds query body, policy and brief revision, deliberately excluding unrelated evidence arrivals. Migration022 stores owned immutable typed results, task identity and exact request/receipt links; replay revalidates them and never resends unknown outcomes. Financial receipts persist despite lost lease/deletion, while private search outputs require a fresh fence. Deletion purges the new table. No network call holds a database transaction. New modules use existing gateway/admission/receipt boundaries; no dependency, hosted service or public mutation API. Rollback disables structured discovery, preserving consent, unknown holds, receipt accounting and deletion. Structured worker search/read scheduling and real discovery quality remain open.


Durable source reads now bind owned handles, task/brief and exact source version under the worker fence. Network/extraction execute outside transactions; a late URL change, deletion or lease loss prevents content persistence. Issued unknown reads are not blindly repeated. Deletion removes migration023 records. Initial search uses only the original public question; attachments still require a separate public-query approval flow. Activation, current paid allowance, native identity and real-model quality remain unverified. Rollback disables structured discovery without removing existing authorization, fetching, financial or deletion safeguards.


Criterion discovery reuses only exact original-question provenance and existing public-query validation. Per-run/per-route query attempts are capped at3 under the same account/run locks as reservation; unknown attempts count and existing identities can replay. No new processor, consent policy, route price, allowance or live authorization. Disable structured discovery to roll back; retain atomic spend and privacy gates.


W06 snapshot reuse is explicit per typed patch. The read projection checks active account, source owner, original run, selected version and passage/version digests on every use. A same-owner UUID outside admitted membership is insufficient; even a corrupt cross-owner membership grants no readable row. New question/claim checks incur existing bounded gateway costs; no claim of selective model savings. Correction, allowance, membership and outbox commit together; duplicate accepted patches reuse the same child. Account deletion purges memberships and accepted correction text. No paid/native/hosted authorization is added.

Scope comparison derived store: migration025 belongs to the account/run evidence boundary and is purged by account deletion before scoped support/claims. Requests contain only bounded claim keys; run/account/version authority is injected server-side. Results are reconstructed from exact authorized evidence/support, never supplied by source text. This local computation adds no provider call; normal infrastructure costs are unmeasured. Writer-context size is checked before any reserve. New comparison context cannot be adopted over an unreconciled same-brief writer attempt, and does not release its existing hold. Real provider receipt acquisition/reconciliation remains separate work.


Calculation derived store: migration027 contains private source spans, input revisions and exact arithmetic under existing account/run ownership. Account deletion explicitly removes it before support/claims; task/model-operation removal also cascades. Strict actions accept references only, never caller numbers or expression code. Restored support, evidence identity and fencing precede arithmetic and persistence. This local handler performs no network/model call and confers no publication or budget authority. Production selection/publication integration remains open.


Calculation publication mappings (migration028) are private account/run-derived records. Revalidation checks calculation, input support/evidence, canonical claim/revision ownership and current basis. The server constructs approvals; request-supplied approvals are ignored. No network transaction or additional provider call. Deletion removes mappings before calculation proofs and claim revisions. Rollback retains deletion and rejects unvalidated calculation publication.


Calculation planning uses the existing pinned text processor, consent and full-context21658microUSD reservation, with account/run/project/key checks. No new monetary authority is granted. Strict output cannot introduce values, SQL, shell, network access or authority fields. Execution restores current support under the lease after the model returns. Unknown plan outcomes retain their reservation and cannot be resent on replay. Saved planning output and arithmetic proofs are account-private and covered by existing deletion. Existing writer requests are not silently upgraded to add planning. Real paid costs and model-quality outcomes remain unmeasured.

Calculated report integration (migration029): model-selected arithmetic keys cannot supply numbers, authority or arbitrary code. Plan, writer and final coverage records pin exact owner/run/revision and source-support provenance; restoration and publication recompute proofs. New write_calculated_report/review_calculated_coverage operations retain the existing gateway provider, consent, reservation and receipt policy. No additional monetary authorization is inferred. Derived plan/draft/coverage content is purged on account deletion and cascades when its model provenance is removed. Rollback preserves these deletion and financial boundaries. Local model doubles are not provider-spend evidence.

### Counterevidence impact and rollback (W02/W03/W05; ADR025)

`counterevidence.v1` adds an idempotent owned result table and a required-proof revision marker, with no new dependency, processor, service, prompt or allowance. Search transformation permits only an original-question span and the fixed `contradictions limitations exceptions` suffix. The executable query is checked before spend admission; source text, private excerpts and captured target text confer no public disclosure authority. Mixed attachment/public search remains blocked pending a separately authorized disclosure flow. Existing three-search ceiling, consent and account/project/key holds apply.

Captured original target revisions and evidence digests cannot be discarded by re-extraction. Publication restores proof independently; missing/corrupt results and flag disable cannot clear a required obligation. Replay uses persisted receipts/read versions and never blindly resends unknown search/model attempts. Financial unknown holds survive cancellation and content deletion under existing settlement rules. Account deletion purges counterevidence content before provenance records; the integer required revision contains no private text.

Rollback disables new scheduling while retaining proof readers/markers, publication vetoes, deletion and receipt/unknown-hold handling. Local multi-connection PostgreSQL and fabricated-transport controls cover concurrency, stale cancellation, privacy, missing proof and deletion; they are not live processor-cost, semantic-quality or external retention evidence. Session-transition retains failed attempts and final receipts. Current paid allowance remains unknown and no activation/spend authority is inferred.

Source-specific deletion (ADR027) purges target raw bytes/text/locators and dependent model/report/brief copies, conservatively invalidating descendants when provenance is incomplete. It preserves unrelated raw evidence/accounts and retains only minimal source/run tombstones, financial records and required-proof markers. Late processing loses its fence; redacted reports are excluded from export. Legacy storage failures remain pending in the existing deletion outbox. Native caches and external processor/backup retention are separate verification gates.

Provider recovery (ADR028) never requests stored prompt/completion content. Metadata requests use a fixed GET endpoint, encoded generation ID, no redirects,10-second/64KB limits and strict cost/identity checks. Credential scopes are hashes, not credentials; unknown old scope cannot be guessed. Financial settlement may finish after deletion but cannot recreate research. Historical repair records numeric prior costs and restores unknown holds only from known accounting amounts. Default operator inspection performs no provider request; paid completion/search authority is unchanged. All tests are nonbillable.

ADR029 mobile protection: new private snapshots/guest drafts use a separate native SecureStore service; no content is copied back to AsyncStorage. Existing plaintext is deleted after protected commit; failed migration is explicitly pending and not a protection pass. Durable ordinary denial flags prevent a failed keychain cleanup from rehydrating content; unknown/corrupt allocations remain a reported cleanup failure. No new processor, key export, cloud storage or fallback. Existing credentials stay separate. Native Expo Go tests inspect only boolean presence/chunk counts and synthetic content; no credential values are emitted. Standalone/iOS/backup guarantees remain unproven.

ADR030 correction text inherits protected snapshot ownership, bounded parsing, account epochs and logout cleanup. Neither restored run IDs nor a client rebase bypass server ownership/revision/budget checks. Native testing used a new synthetic account in the separate local native database, actual PDF extraction and fabricated transport; its14model calls incurred zero provider charges. The harness deleted only its created account on shutdown.

ADR031 upload/admission retry keys are account-scoped; private filenames/digests/question stay in protected native storage. Server upload key hashes and withdrawal tombstones preserve vetoes after content deletion without retaining private payload. Resolve-or-withdraw cannot schedule work or spend and serializes with admission/deletion. Failed writes or unknown responses retain identity; changed reselected content is rejected locally. Receipt lookup owns a finite timer under GC and caller cancellation. SDK54 crypto adds a native declaration but no processor/network/storage; missing native capability blocks hashing, with no fallback; dependency audit is separately recorded and remains nonzero.

Cancellation API mutations serialize active ownership and run state with their event under the account→run lock order. Event insertion failure rolls back cancellation, deletion cannot be followed by a cancellation event resurrection, and direct Pool event emission holds the sequence lock through insertion. ADR033 and real PostgreSQL cancellation-atomicity regressions govern rollback.

Source-specific deletion records account-scoped exact admission-key hashes before clearing invalidated run keys, preventing delayed retries from recreating that research. Only opaque replay vetoes remain; raw question/source content is still purged. Already-erased historical keys cannot be reconstructed. ADR035 records local regressions and rollback.

Mobile source deletion records only an opaque source-ID obligation in the account-owned protected snapshot, and redacts dependent local content before issuing DELETE. Durable storage failure forbids the remote request; unknown responses retain the handle and block cache repopulation. Source confirmation is bound to the currently displayed passage/source version. Account change invalidates late responses. The original external file is unchanged; selected local attachment bytes are dropped. ADR036 and source-deletion-flow regressions specify these boundaries.

Requested verification (ADR037) grants authority only to reassess one immutable owned report claim against selected evidence. A private note is stored feedback, never assessed or disclosed through public queries. Snapshot reuse preserves owned passage digests; refresh rereads selected known sources under existing safe-access/consent rules and does not discover sources. Model output cannot replace the target, extend scope, authorize tools or clear an admitted proof obligation. Restored provider receipts and deterministic binding/scope checks gate publication; negative outcomes expose owned contrary-passage links without creating unsupported positive claims. This is inspected-evidence assessment, not independent factual certification.

Strict request replay binds exact UUID bytes, parent/report/version/claim, policy and note. Optional verification-bound resolve rejects ordinary-run collisions and changed requests, restores proof before returning acceptance, and atomically withdraws absent requests even with scheduling disabled. Eight successive verification children are the limit; cycles/deeper lineage reject before recursive proof work. Private targets, notes and derived results are purged by account/source deletion; opaque required markers and existing minimal financial liabilities remain. Unknown provider outcomes retain holds and forbid blind resend. No new processor, provider route, dependency or budget is introduced; actual reassessment still consumes existing gated allowance when authorized. Rollback disables new scheduling while keeping recovery, proof/publication readers, deletion and settlement. Local synthetic/fabricated-transport controls establish neither live semantic quality nor external processor/backup retention.

W09 PostCSS remediation (ADR038): scoped Expo Metro config override resolvesPostCSS8.5.28 and removes the older8.4.49 edge. Root offline frozen install,136mobile tests/types and AndroidJSexport pass. Audit still exits1 with3moderate/2high (Vitest/mocker, uuid and image-size); no audit exclusions or standalone/native security proof. Exact dependency graphs and failed/security receipts remain under verification/v6/dependency-postcss-integrated/.

W09 Vitest remediation (ADR041): test-only Vitest/mocker4.1.11 removes two moderate findings without adapting assertions, tests or runner configuration. Full393 PostgreSQL and17 actual extraction controls pass. Current audit retains1moderate/2high (uuid/image-size), exit1. Experimental Metro/UUID graphs are separate and do not describe root security. No processor, native dependency or paid authority change.

ADR042 target corruption emits only bounded reasons, never target/note content. Confirmed provider liabilities settle from original intents; unknown liabilities retain reservations. Only explicit deterministic validation errors terminate as blocked; transient database and lease failures remain recoverable. Required publication markers survive.

Current UUID repair (ADR043) removes the remaining moderate JS advisory through scoped xcode>uuid11.1.1. Actual Node/Xcode compatibility controls pass; current audit intentionally remains exit1 with2high image-size advisories. Metro stays0.83.3 pending native runtime compatibility. No standalone native-security or release claim.

ADR045 expands whole-passage count to128 within unchanged source/text/request-body, provider-price and full-context reservation bounds. Ownership/digests/consent remain rechecked; exceeding count or bytes issues no extraction request, though preparing the task can already consume its ordinary brief operation. No new live authority. ADR046 preserves withdrawn text markers and excludes comment content in newHTMLv3 derived evidence; source content remains untrusted and cannot expand tool/query authority. Historical receipts and deletion/financial obligations remain intact.

W02/W03/W07 configured-route preflight (ADR047): a fresh authenticated settings read must confirm the selected route flag before a new admission journal/upload/admission. Failures preserve existing recovery identities; Check/Withdraw remains available for already accepted requests. This is configuration checking, not readiness or budget assurance. Account/view generations reject stale responses. Root148 mobile controls/types and648module2.18MB JS export pass; actual local API probe passes with zero provider intents. Incorrect initial session-endpoint mock proof is explicitly rejected and preserved. No native journey or live semantic claim. Evidence: verification/v6/admission-preflight/ and admission-preflight-integrated/.

ADR048 append privacy/accounting: attachment addition is explicit, owner-checked, consent-bound and capped at the existing total3. It grants no private-to-public query disclosure. Recovery resolves an exact accepted correction or stores an opaque withdrawal identity under the account lock; disabling scheduling or revoking processing consent cannot erase an accepted obligation. The mobile dedicated protected journal cannot be overwritten by stale ordinary snapshots; raw picker bytes remain transient. Unreadable appended input cannot silently reuse old evidence as a new answer. Uploaded but unused copies remain account data until deleted and are disclosed. No new paid allowance, processor, dependency, hosted migration or retention exception.

ADR049 evaluator boundary: explicit current operator-attested user approval, exact scope/hash/expiry and positive total budget are required before live admission. Credentials alone authorize nothing. A dedicated existing account/queue/database is required; no automatic allowance or consent grant. Prior account/project/same-key/unattributed liabilities count, and unknown holds stop later steps. Production gates remain unchanged. Evaluator labels cannot enter generator context; local fabricated transports do not establish provider invoice enforcement. Private fsynced journals redact credential values and preserve failed/unknown outcomes. Exported files remain operator-owned copies outside PostgreSQL account deletion; do not claim automatic external retention compliance.

ADR050 remote invalidation: the owned run snapshot exposes source-deletion tombstones and never returns a report ID for invalidated content. A tombstoned account cannot reopen a run even if a credential row remains. Mobile must hide dependent cached content before ancillary network work and persist an account/run-bound noncontent cleanup marker before replacing its protected snapshot. Independent composer and pending request identities remain recoverable. Failed storage cleanup is explicit; neither unit controls nor a successful server deletion prove physical device or external backup erasure.

2026-09-18 native graph follow-up on exactce2f889: isolated Android prebuild and actual Gradle releaseRuntimeClasspath resolution pass (Gradle8.14.3,12m25s). Selected CommonsIO2.6,Codec1.10,OkHttp4.9.2,Okio2.9.0,annotation1.8.1,biometric1.1.0,React/Hermes0.81.4. Apache CVE-2024-47554 affects selectedIO2.6 through XmlStreamReader; reachability is not established and no clean native-security claim follows. Official source https://commons.apache.org/proper/commons-io/security.html checked2026-09-18. Prior ADR024 unresolved-resolution statement is historical; actual graph is now inspected. No app compilation/APK/device install/release. Root source/dependencies unchanged. Local Gradle and NDK27.1.12297006 were downloaded under the existing SDK license. Evidence verification/v6/native-graph-current/RESULTS.json. Native compatibility/remediation and current device journey remain open.

W04 artifact-limit repair (ADR051): private document artifact retention now matches the existing8MiB upload cap; public fetch remains1500000 bytes. This does not raise model-context, per-call, account/project/key spend or source-per-run limits. Ownership, raw/derived deletion and source-version digest checks still apply. Existing larger admitted artifacts remain readable on rollback; disable new admission rather than erase evidence or weaken deletion. No hosted migration or spending is authorized by the local migration/control receipt.

W08 frozen inputs (ADR052): evaluator-supplied PDF bytes require pinned registration/hash, bounded contained files and the same authenticated owned binary upload/consent path. Frozen arms disable public discovery/retrieval/new challenges, so uploaded document text cannot silently become public queries. Successful uploads are durably identified before admission; expired authorization or failed journal leaves an explicit retained object and no authority to spend. Operator-managed evaluator files remain outside server account deletion. No actual paid scope is authorized by implementation or synthetic tests.

Saved HTML (ADR054) uses the existing private attachment/consent boundary. Only authenticated binary UTF-8 uploads within1,500,000bytes are newly admitted. HTML is untrusted parser input, never rendered/executed as a webpage or immediately accepted as extracted notes. The existing networkless parser handles scripts/resources; no new egress, model processor, paid allowance or public-document authority follows from a filename or embedded URL. Frozen evaluation admits public corpus bytes as owned attachments and retains that provenance. Rollback can deny new HTML admission while preserving deletion and existing immutable receipt readers.

W05 selection boundary (ADR055): migration038 adds backend-owned evidence_selections containing private source titles/locators and hashes, never duplicated passage text. Account deletion and conservative source/descendant deletion purge this table. Run obligations retain only opaque identities/hashes, not source content. New runs pin the policy; existing runs keep legacy request identities to avoid reissuing unknown external work. Selection creation and replay are lease-fenced transactions with no network. The fixed inventory/whole-passage limit does not increase any model token, byte, output, project/key or account spend allowance. Model metadata is server-authored, strictly validated and digest-bound; source instructions have no authority. Missing/corrupt proof fails closed without another model call. Rollback retains proof/policy readers, omission publication vetoes, privacy deletion and all financial obligations.

ADR056: selection_inventory_checks is owned derived evidence, deleted before selection proofs on account/source deletion and cascading with proof/claim/intent deletion. Its writes occur inside existing worker fences; read-only historical restoration recomputes missing checks without persistence. It grants no new provider/network permission or reservation. The full owned inventory is inspected server-side only; no extra source bytes enter model context. Disputed omitted evidence vetoes downstream supported use even when a report would carry an omission warning.

ADR057 recovery uses the existing scoped per-action reservations and four-iteration run limit. Every context change creates its own immutable, digest-bound logical model action; replay restores it. Unknown outcomes retain their reserve and stop recovery without blind resend. Server-owned policy denies unknown versions before paid task preparation; legacy admissions keep none.v1. No source text or model suggestion can expand budgets, selected whole-passage limits, connectors or ownership.

ADR058 forward-period evaluation: provider GET /key remaining quota is capacity already net of reported usage, not a per-generation settlement. An explicitly authorized new evaluation period may preserve terminal pre-receipt historical estimates separately without claiming they are pending new charges. The one-use setup refuses modern ledgers or nonconfirmed/nonterminal old work; it never edits/releases historical rows. Existing production same-key/unknown reservations remain blocking within the active ledger. Fixed-endpoint bounded quota checks precede paid evaluation and every step; quota alone grants no permission. See ADR058 and verification/v6/forward-accounting for exact authorization, historical digest, cap and execution evidence.

## ZDR route continuation — 2026-09-18

Consent2026-09-18.1 discloses OpenAI or Microsoft Azure via OpenRouter. Explicit server-selected Azure structured-text policy requires ZDR=true and data_collection=deny. Current configuration cannot retarget an admitted run. Operator HTTP404 reconciliation, if executed, is separately recorded and cannot invent generation receipts or release network-unknown outcomes; see ADR059.

ADR061 evaluator-only continuation preserves exact terminal unknown liabilities in the same account/key/scope. Its strict immutable allowlist is read-only evidence, never a settlement mechanism. The same aggregate cap must cover confirmed spend plus all holds plus the next full run reserve. Missing/extra/mismatched liabilities fail closed; repeated held questions are forbidden and new unknown outcomes stop execution. Rollback disables continuation admission without changing holds.

ADR062 public discovery: explicit new immutable v3 policy routes generation to Azure with ZDR=true, fallback disabled and data collection denied. Existing disclosed Exa search is a separate processor; do not describe its retention as covered by generation-provider ZDR. Public-question-only search projection and28658micro reserve remain enforced. Old run/request identities and unknown holds remain unchanged.

Consent2026-09-18.2 clarifies OpenAI/Azure generation during public discovery as well as structured text; approved public query/results and the separate Exa processor are explicitly described. New work requires renewed consent. Existing financial holds/receipts remain intact.
