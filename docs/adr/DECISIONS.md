# Architecture decision register
Reviewed: 2026-09-16. All decisions are proposed for implementation; none implies a migrated live application.

## ADR-009 — measured offline HTML extraction (2026-09-17 implementation checkpoint)

Select Trafilatura 2.2.0 in an isolated Python process behind the existing TypeScript worker. Keep lxml table cells, spans and linked/adjacent footnotes because XML extraction alone drops some table geometry. Do not add Readability as a second production fallback. On identical saved bytes, the Readability 0.6.0/jsdom 26.1.0 candidate preserved 10/11 selected real-document spans; Trafilatura preserved 11/11, including PostgreSQL's connect-timeout caveat. One additional oversized download failed and stays in the denominator. The original eight synthetic cases scored 8/8 for the initial DOM candidate and 7/8 for vanilla Trafilatura (script-shell failure). The production wrapper fixes the shell and is tested separately. Agent-inspected references are not independent human adjudication or a quality leaderboard.

Tradeoff: adds a pinned Python runtime and Linux namespace requirement. The measured caveat loss and lower extraction times justify this small adapter/runtime addition, without replacing backend orchestration. Exact versions, hashes, failure records and retention notes are in `verification/v6/extraction/`; no third-party full documents are committed. The actual adapter preserved 11/11 saved real-document spans, with partial coverage warnings and roughly 0.65–1.59 seconds per isolated invocation on this machine. Reconsider when a broader held-out corpus shows omission, deployment compatibility fails, or runtime cost materially exceeds the benefit. PDF/OCR remains unimplemented. Rollback disables reading; it cannot label raw HTML or snippets as full text.

## ADR-001 — one backend codebase, separate API/worker processes
Choose modular monolith with three shared packages only when they contain real code. Keep original native/server/Supabase/Render direction. Reject separate provider/evidence/billing microservices at launch. Rationale: the first risk is demonstrable research usefulness and reliable transactions, not independent team deployment.
Strongest objection: retrieval/parser workloads may need resource isolation. Separate constrained extraction runtime when actual parsing risk requires it, without turning every domain into a service. Reverse when measured resource/security/deployment requirements justify separation. Port boundaries make reversal moderate, not zero cost. Rollback retains data contracts and fails closed.

## ADR-002 — relational evidence and decision dependencies
Choose typed relationships among constraints, questions, candidate selection, source versions, passages, claims, calculations and report blocks. Reject graph database and universal semantic graph initially. Rationale: selective repair needs ownership and dependency semantics, not a new storage paradigm.
Strongest objection: implicit dependencies are easy to miss. Use conservative branch/full invalidation when dependency completeness is unknown and reopen candidate discovery on relaxed constraints. Reverse to more advanced graph tooling only after query/maintenance limits are measured. Content versions remain subject to deletion.

## ADR-003 — bounded controller with optional escalation
Choose one iterative controller, combined investigation behavior and separate publication checks. Six logical concerns may share calls. Critics/parallel investigators are conditional, capped experiments. Keep one hosted provider route as an evaluation baseline, not invented internal visibility.
Strongest objection: parallel decomposition can improve hard fact gathering (S32). Allow a tested independent-question branch under the same atomic budget; preserve it only when matched evaluation shows benefit. No model vote is independent evidence. Reversal is inexpensive at strategy interface, but output/evidence contracts remain stable.

## ADR-004 — revision-fenced publication
Choose revision basis with brief/evidence/consent/cancel epochs plus worker fencing. Validate at tool authorization and atomic publication. Reject last-write-wins final reports and treating a client stream as state.
Strongest objection: stricter fencing can discard expensive late output. Permit safe raw receipt reconciliation without publishing obsolete conclusions; count actual provider cost honestly. Do not trade correctness/privacy for sunk cost. Migration requires contract compatibility mapping if an app exists.

## ADR-005 — outcome benchmark before broad feature growth
Choose two launch jobs plus correction lifecycle, with independent decisive-evidence rubrics. Reject a large unreviewed synthetic leaderboard and shell-first completion theater. Keep proposed 95/90 citation targets labeled unvalidated; add denominators and source-discovery/repair metrics.
Strongest objection: narrow tasks may not predict consumer demand. Recruit users with recent decisions and test both usefulness and return behavior; pivot the task focus rather than adding every category. A failed experiment is a reason to remove complexity, not relabel the metric.

## ADR-006 — private-data retention outranks historical immutability
Choose logically versioned reports but purge/redact affected private content when required. Keep non-content tombstones and permitted audit metadata. Reject retaining old private excerpts under an “immutable report” exception.
Strongest objection: source removal reduces reproducibility. Disclose that tradeoff and preserve only permitted metadata. External copies already downloaded cannot be recalled; make this limitation explicit. No rollback can restore access after revocation/deletion without lawful new authorization.

## ADR-007 — local correctness and gated progression
Choose real local PostgreSQL/queue for P0-D when cloud access is unavailable; separate live-provider and native evidence as P0-L/P0-N. Make P0 the first checkpoint without postponing minimum security, accessibility or governance. Reject both cloud-credentials-as-a-universal-blocker and treating a fixture/local pass as production compatibility.
Strongest objection: local environments can drift from production. Pin versions and migrations, record permissions/connection mode, and run hosted auth/RLS/storage/pooler/parity checks before deployment. Reverse the local test environment when a measured incompatibility demands it, not because a managed brand is assumed necessary. Existing safety/publication contracts remain unchanged.

## ADR-008 — notification identity without an exactly-once delivery claim
Choose a durable logical completion identity `(runId, completionEpoch)`, created atomically with publication, plus per-account/device-binding delivery identity. Provider deduplication/collapse features are capabilities, not assumptions. Reject the external review's claim that a local key alone prevents a duplicate OS alert after a send/receipt crash. Expo documents possible duplicated or missing handoff; a successful receipt is not proof of device delivery (N01–N03 in `research/RECONCILIATION_SOURCES.json`).
Strongest objection: suppressing an unknown-outcome retry can lose an optional alert. Prefer an inspectable in-app completion and reliable foreground refresh; default to no blind resend after an unreconcilable issued attempt. Change retry policy only with documented provider guarantees and a tested product tradeoff. No rollback may weaken account binding or disclose report contents. P0 tests local outbox invariants; live push arrives with P3 if enabled.
## ADR-010 — verify current Supabase identity before resolving local ownership

Use the existing Supabase direction through its documented Auth user endpoint, pinned to a configured HTTPS project origin. The server does not parse client metadata into authority or implement JWT cryptography. A bounded request occurs outside database transactions; the returned confirmed user subject/issuer maps to a local owner under an advisory/account lock. No cache of positive identity results is introduced. This trades one Auth request per API request for current provider-side user/session checking; hosted latency and failure behavior still require measurement. The immutable digest mapping prevents a still-valid provider token from recreating a locally deleted account. Production account creation grants no spending allowance. Local transport/database tests do not establish hosted authentication or provider-side deletion, which remain activation gates. No new library or service was adopted.

Sources inspected 2026-09-17: [Supabase getUser](https://supabase.com/docs/reference/javascript/auth-getuser), [JWT verification endpoint](https://supabase.com/docs/guides/auth/jwts), and [session verification guidance](https://supabase.com/docs/guides/auth/server-side/advanced-guide). Reverse this choice only with measured Auth latency/availability evidence and equivalent revocation/deletion guarantees; never fall back to development session hashes in production.


## ADR-011 — binary PDFs with measured physical text order

Adopt Docling Parse 7.20.0 behind the existing isolated extraction adapter, with pypdf 6.19.0 used only for strict metadata/page-count/encryption checks. Baseline pypdf text, PDFium text order and Docling's model-free native pipeline failed to preserve a decisive negation in an actual 46-page RFC PDF. Grouping Docling cells by their physical line coordinates repaired that case and passed an original mixed-font-order regression. Persist original cell geometry and page/block locators; no semantic quote repair. Measurements, exact hashes/licenses/dependencies and failures are in verification/v6/documents/. No ML models, OCR, full Docling orchestration or additional service is adopted. This adds Python/native parser dependencies because the smaller tested baselines lost material meaning. The diagnostic ran within the existing 512 MiB/15 CPU-second isolation limits. PDF table structure/column reading order remains unverified and output stays partial. Broader independent document evaluation may reverse this choice; rollback returns unavailable and preserves byte/deletion/publication gates. Never use raw parse success as a quality claim.


## ADR-012 — credential-wide reservation buckets without storing credentials

OpenRouter issuance records SHA-256 of the provider namespace and trimmed server credential on provider intents. This opaque identity scopes an independent operator cap across account/project buckets; it is never exposed in client contracts or logs. Server configuration LIVE_KEY_SPEND_CAP_MICRO defaults to zero and is not itself evidence of new spending authorization. Lock order is account, run, allowance, key bucket, project bucket; no network I/O occurs within the transaction. Additive migration014 leaves historical identity null because today's credential cannot prove which key paid an old intent. Such unattributed costs count conservatively against every key until evidence-based reconciliation; do not silently migrate them to zero or infer a key. Separate actual keys retain distinct buckets but still share the project limit. This can over-reserve historical cost; availability is the explicit tradeoff. No new dependency/service. Rollback disables issuance while retaining ledger identities/holds. Automatic receipt reconciliation and bounded model route estimates remain separate work.


## ADR-013 — one strict structured-model boundary

Replace the unused permissive proposal adapter with a typed OpenRouter transport and one durable worker coordinator for brief, extraction, support proposals, action proposals, writing and coverage review. Model outputs carry local keys and exact evidence spans; server ownership, budgets and publication remain independent. Canonical Zod3 schemas generate provider JSON Schema with exact ISC zod-to-json-schema3.25.1 (no runtime dependencies beyond the installed Zod peer); this avoids a hand-maintained second schema. Do not add an agent framework.

The initial text-only policy pins OpenAI/gpt-4o-mini with provider fallback/plugins disabled and maximum price filters. Public endpoint metadata and official routing/usage docs were checked; no paid capability or semantic probe was executed. A conservative full-advertised-context reserve fits existing allowance without raising it, but actual receipts and explicit unknowns remain authoritative. The strategy stays disabled pending full workflow integration and authorized evidence. Model failures never become fixture output. Versioned results persist only under consent/ownership/revision/fence checks and are deleted with the account. Fences do not suppress already-incurred financial receipts.

Regressions include corrupt cached receipts, duplicate concurrent proposals, stale evidence, missing current downstream consent, cross-owner passages and delete/lease takeover during a returned result. Exact license/integrity/audit and metadata are in verification/v6/gateway/. Seven pre-existing Expo-chain audit findings remain open. Reversal is a different adapter behind the same contracts or removal of the converter during a measured Zod4 migration; rollback disables issuance, never weakens evidence or privacy gates. This is gateway implementation, not a completed general research engine.


## ADR-014 — stable task interpretation before evidence-driven execution

Persist one versioned task interpretation per run/brief revision, derived from a validated brief operation. Allocate criterion/question identities on the server; retain model-local keys only as proposal mappings. Evidence arrival is not a reason to reinterpret the user's requirements or pay for the same brief again. Recover an existing successful proposal after a crash, verify its receipt/schema/prompt/policy binding on every load, and require a distinct task for a distinct brief revision.

Use one additive PostgreSQL table with owned JSON specification/ID maps, rather than another service or task graph database. Existing fenced transactions serialize adoption; unique(run,brief revision) prevents duplicates. Material ambiguity remains explicit. This does not certify model completeness or implement support/coverage. Reversal is a measured relational normalization behind the same task service; rollback disables preparation and retains deletion/accounting gates. ProcessRun integration and typed correction interpretation remain open.


ADR-013 input-membership amendment: persist a bounded metadata manifest of the exact model context alongside the existing result, rather than storing another full prompt/source copy or inferring attempted evidence from output citations. Unmentioned passages still belong to the attempted input. Historical missing manifests are unknown, not synthesized during migration. Reuse checks the exact manifest; this intentionally blocks old cached operations until explicitly handled, without automatic paid resend. No claim of full-document coverage or semantic verification follows from selection.


## ADR-015 — executed scoped support, separate from final publication

Use a fallible structured evidence-comparison operation plus independent deterministic binding/scope/numeric/access/contradiction/qualification checks. The resolver inspects all selected in-scope passages, not only the model's chosen quotes. Preserve conflicts and unknowns instead of collapsing them into a progress event. Legitimate paraphrases can pass the combined check; no lexical-overlap score proves entailment. Semantic quality still requires held-out real-model/reference evidence.

Persist canonical claim revisions and typed checks behind existing fences in two small PostgreSQL relations, reusing source/model/task identities. Keep claims unverified at the publication boundary until the report service validates the full report mapping and current basis. Rollback disables the new strategy; never restore overlap-only publication or delete receipt/privacy safeguards. No new model/service/dependency is required; a later checker version or processor must produce separately versioned evidence, not silently reinterpret prior results.


ADR-015 publication amendment: the report gate now recomputes and validates stored scoped results instead of trusting caller flags or forcing a valid semantic paraphrase back through the literal-only checker. Managed assertions never fall back when their scoped result is absent/non-supporting/stale. Pure compilation still requires exact final assertion mappings; new writer wording needs a new support path. Canonical assertion IDs/revisions survive publication/reopen. Policy constants remain server-owned in a backend port, unchanged; validation is read-only until the existing final publication decision passes.


## ADR-016 — final writer text is a new checked assertion

Do not restrict synthesis to copying source assertions or let new prose inherit their approval. Project each material draft surface into a new inference proposal with explicit source-premise revision links, check it against original evidence, and publish only supported wording through the existing canonical gate. Standard application headings remain nonfactual; unchecked headings/limitations cannot bypass this process. Unsupported draft surfaces become localized abstentions. The generated title is not exposed as a canonical factual field.

A single immutable draft-lineage relation ties existing operation/task IDs together; parent origin must be extraction, preventing recursive/self-supported drafts. Common premise scope is retained; mixed fields remain unknown and dependency completeness is partial. This supports conservative later correction invalidation rather than claiming complete causal dependencies. Model schema/prompt/provider remain unchanged. Checker v2 adds currency-token preservation; result identity now includes checker version so old judgments remain historical. Rollback disables the structured writer while preserving reports, deletion, reservations and versioned records.


## ADR017 — Criterion coverage is a persisted checked outcome
Decision: execute review_coverage against current owned task/evidence and revalidated assertion support. Persist the actual resolved question statuses, failed guards and canonical assertion revisions under both checker versions. Question coverage is distinct from candidate eligibility; model not-applicable labels cannot waive user requirements.
Consequence: no source-count completion; semantic review still needs real evaluation. Local additive migration021 only; no hosted deployment. Rollback disables new coverage execution while retaining privacy, holds and existing evidence. Main worker/publication integration remains open.


## ADR018 — Fixed public search policy with durable results
Pin one Exa auto plugin search through the existing bounded OpenRouter transport and pinned OpenAI text provider. Require versioned disclosure and default-off activation; reserve the documented plugin fee plus the full model ceiling. No model-directed multi-search server tool. Persist results separately from financial receipts, keyed to the logical query/brief/policy, so evidence arrival cannot justify resending an unknown request. Mixed document/public queries remain gated pending explicit approval. Local migration022 only; no paid behavior or invoice proof. Rollback disables new discovery and retains ownership, receipts, holds and deletion.


## ADR019 — Durable reads separate discovery metadata from evidence
Persist a source-read operation and its exact extracted version before treating a discovered URL as evidence. Keep search snippets separately labeled; source existence grants no answer coverage. Stable run/source/brief/reader identity permits replay without repeat network requests, while unknown interrupted reads remain unresolved. Reuse the existing safe transport and isolated extraction instead of adding another browser/parser runtime. Additive migration023 shares account deletion and worker fencing; no network transaction. Reverse only behind the same ownership/access/evidence contracts; rollback disables discovery and retains safety gates. Additional read-attempt recovery, adaptive discovery and correction membership remain open.


ADR019 criterion-loop amendment: use a small pure bounded policy to narrow search to unresolved criterion provenance before adding another model planning call. Every evidence change triggers actual assertion/support/coverage work; durable route limits prevent restart or concurrency from expanding query count. This approach cannot invent synonyms or search terms absent from the user question, and may exhaust without a distinct query. Broader approved query formulation needs measured evaluation and explicit privacy design. Rollback disables structured discovery, retaining safe evidence and accounting.


## ADR020 — Explicit replacement patches with immutable evidence membership
Use a full replacement question as the first typed correction, preserving the accepted user message and named parent revision. This avoids interpreting a contradiction-laden appended question as a settled patch. Conservative recomputation is explicit because dependency completeness is unknown; old approvals never transfer. Fine-grained criterion and formatting patches remain separate unfinished capabilities.
Reuse source snapshots through digest-bound passage membership and a common authorized read projection, rather than duplicating source rows or changing their owner/run. Snapshot and refresh are explicit policies. New discovery reopens conservatively for public tasks; private-query boundaries remain. Account deletion overrides history. Atomic idempotent admission includes allowance and dispatch outbox. Rollback disables typed admission while retaining membership reads, deletion and publication safeguards; no destructive down migration or hosted rollout is authorized.


ADR020 comparison amendment: derive a bounded exact inventory comparison from published claim revisions, criterion definitions and cited source versions, inside the publication transaction. Do not pay a model to infer a diff or treat the accepted patch/reuse flag as an observed outcome. Preserve actual revision IDs and measured snapshot membership. Exact wording/scope equality is explicitly narrower than semantic equivalence; missing history produces no comparison. Candidate eligibility changes need separate executed outcomes. The existing report JSON field stores this additive versioned result; no extra service or table.


ADR020 mobile adoption amendment: expose configured correction mode and reserved allowance on the existing owned run snapshot, rather than making mobile infer engine availability from a label or credential. Send the full question patch through the shared schema; retain snapshot/refresh as an explicit choice. Missing capability is unavailable. Native interaction remains a separate gate. The build repair uses the [documented Metro resolver hook](https://metrobundler.dev/docs/configuration/#resolverequest) only for public workspace contracts’ NodeNext relative specifiers; it delegates to normal Metro resolution and does not add a general alias/fallback layer.

## ADR021 — isolate historical controller from production composition (2026-09-17)
The production worker previously imported fixture adapters at module load and could fall back to the bounded composer when structured processing was disabled. Move that historical controller behind an explicit development-only entrypoint, preserving its tests as diagnostic evidence. Extract only the shared lease/lifecycle preflight and queue runtime composition; production research stays on the structured pipeline. Enforce the transitive runtime source graph at governance checks and exercise disabled/fixture behavior against PostgreSQL. No package, provider, schema, migration or spending change. Disabling production processing yields explicit unresolved failure for existing controlled jobs and rejects new production admission. Rollback keeps production disabled; the old controller is not a rollback research provider.

## ADR022 — deterministic recorded-scope comparison (2026-09-17)
Implement compare_scopes outside model prose and persist exact claim/evidence/support identities. Compare recorded fields conservatively: unknown stays unknown; an exact match does not establish proposition or quantity equivalence. Feed the typed result to the existing writer rather than introducing another model role. Version the result and the comparison-bearing model input manifest. Additive migration025 shares account deletion. Existing v1 writer contexts remain historical; new context adoption is blocked in code over unresolved older writer attempts. Rollback disables the structured path while preserving receipts, evidence, deletion and fail-closed publication.

ADR022 projection amendment (2026-09-17): full comparison records repeated identical scope strings for each pair, making a60-assertion control exceed the writer input ceiling. Keep full durable results for inspection, but project references plus grouped relation vectors into new writer contexts. This preserves all pairs and binds to the supplied assertion scopes/criteria. Pin representation per result via migration026 and versioned input identity so older known or unknown requests retain their body/manifest semantics. Exact compression bytes are engineering evidence only; model comprehension and research quality remain unevaluated. No budget or provider change; rollback disables new processing, preserving both representations and deletion.


## ADR023 — evidence-bound exact arithmetic handler
Use exact rational arithmetic over supported, uniquely located printed quantities rather than model arithmetic or arbitrary expression evaluation. Retain numerator/denominator and explicit assumptions; refuse ambiguous/qualified inputs and implicit unit conversion. Bind durable results to ordered operands, exact claim revisions, authorized evidence and checker versions, and recompute on restoration. This proves arithmetic only; matching units do not prove comparable scope or a valid real-world aggregate. Keep publication unchanged until a dedicated proof-aware derivation path can revalidate these records. Additive migration027 is owned by scoped evidence and purged on deletion. Rollback disables invocation while retaining evidence, deletion and safety gates. No external library, model call, service or hosted migration is introduced.


ADR023 publication amendment: use a distinct calculation-to-claim mapping and exact versioned renderer, then restore the arithmetic proof at the real report gate. Do not pretend the derived numeric output appears in a source or relax literal support globally. Preserve visible assumptions/input statements and exact fractions; record inference and arithmetic-specific checker provenance. Keep the final question-coverage gate unchanged until derived results are explicitly integrated into structured writing/coverage. Migration028 is private derived evidence and purged before input proofs on deletion.


ADR023 planning amendment: introduce a separately named/versioned calculation planning operation rather than altering existing saved action/writer request schemas. Use the same bounded gateway, not another provider or agent role runtime. Model output selects references; deterministic code restores supported quantities and executes exact arithmetic. Numeric evidence is necessary to invoke planning but does not prove arithmetic is needed or that its result answers a question. Existing admitted writers without a plan preserve their original path. Writer and coverage adoption remain a subsequent required integration, never inferred from a progress event.

ADR023 writer/coverage amendment: keep source assertions and arithmetic proofs distinct in model context and report publication. Give new writer/review operations separate schema/prompt identities so older requests retain their exact bodies. Let the model select known calculation keys, then render numbers and assumptions deterministically and revalidate exact content at publication. Store plan/draft/review lineage in migration029 and purge dependent derived content when its provenance is removed. Corrections recompute results over explicitly reused immutable source versions. Do not treat an exact sum as population comparability, eligibility or proof that the selected formula answers the question. Preliminary discovery still uses source-only coverage; semantic applicability, latency and held-out quality remain measured follow-up work.


## ADR024 — native binary document acquisition

Native document input decision, W04/W07: use expo-document-picker14.0.8 and expo-file-system19.0.24 behind the mobile account/request boundary and existing binary API. Both exact package manifests declare MIT; picker has Expo peer only, filesystem has Expo/React Native peers and was already in the lockfile. No model assets or new JavaScript production transitive dependencies. Versions match installed Expo54 bundledNativeModules. Reviewed Android/iOS picker source: copyToCacheDirectory creates a dedicated DocumentPicker cache copy. Read it with File.bytes(), check size before/after, delete that copy; never delete original URI. Official API references: https://docs.expo.dev/versions/v54.0.0/sdk/document-picker/ and https://docs.expo.dev/versions/v54.0.0/sdk/filesystem/ . Local audit returns seven existing moderate/high advisories (image-size/postcss/uuid); no clean-audit claim. Runtime cost: OS picker/cache copy plus a bounded8MiB byte read/upload, no provider charge. Account changes discard late selection; transient bytes are excluded from persistence. Native and signed-build evidence remain separate.

Native dependency review adds an unresolved build gate: picker Android declares androidx.annotation1.0.0 and commons-io2.6; existing filesystem declares commons-codec1.10, commons-io1.4, OkHttp4.9.2, Okio2.9.0, legacy-support-v4:1.0.0 and documentfile1.1.0. iOS declares ExpoModulesCore. Actual Gradle resolution in a standalone build has not been inspected; npm audit cannot establish this graph. Apache lists Commons IO2.x before2.14 as affected by CVE-2024-47554 in XmlStreamReader (https://commons.apache.org/proper/commons-io/security.html); the reviewed picker uses FilenameUtils.getExtension, not that XML reader, but this is not a blanket absence-of-reachability proof. Resolve and audit the complete native dependency graph before private/production use; no clean-native-dependency claim. Expo Go has its own prebuilt graph, distinct from the unchanged standalone APK.

## ADR025 — bounded counterevidence with durable publication obligations

W05 / V6-F01/F03/F15: use `counterevidence.v1` behind default-off `STRUCTURED_CHALLENGE_ENABLED`. Capture up to six initially supported claim targets for one consequential question, including their original revisions, scopes and evidence digest, before one admitted public challenge search, durable reads and scoped support reassessment. Original targets survive fresh extractor omission. Typed outcomes distinguish found counterevidence, none found in inspected evidence, blocked, unresolved at limit and unknown; no counterexample found is never universal proof. This selects a bounded target set, not every report claim.

Impact checklist: the additive, idempotent migration030 owns `counterevidence_checks` and `runs.counterevidence_required_revision`; contracts/core own the strict action/search schemas and deterministic selection/outcomes, backend modules own restoration and worker orchestration. No new dependency, service, model route, prompt, connector or monetary allowance. The transformed search accepts only an original-question provenance span plus the closed suffix `contradictions limitations exceptions`; validate the actual executable query before admission. Private/source/target text cannot expand public-query authority. Existing consent, mixed-attachment restriction, three-search ceiling, account/project/key reservations and receipt rules remain binding. Search body/digest logic is shared through the existing search port; adapter composition stays in backend runtime.

Target capture atomically sets the required revision. Publication independently restores original extraction/support and current search/read/support proof; missing required records, stale or tampered evidence and optimistic model judgments cannot erase the obligation. Contradicted, qualified and unresolved targets produce localized canonical limitations even when fresh extraction omits them. Restart excludes challenge searches from ordinary discovery and reuses finished reads without network. Unknown provider outcomes retain holds and cannot be blindly resent. Account deletion purges challenge content before its raw/derived provenance.

Regression linkage: core counterevidence controls and `model-gateway.integration.test.ts` cover contradictory/qualified/supportive evidence, omitted original targets, event-only/missing proof, forged revisions/results, private-query rejection, restart, concurrent issuance, shared ceilings, unknown search/model receipts, stale cancellation and deletion. Session-transition evidence records the initial type/test/restart failures and final 305/305 PostgreSQL plus 148/99/61/6 verify passes. These use synthetic evidence and fabricated provider transports; they establish local boundaries, not live semantics or independent quality. The separate extraction suite and actual native research journey remain distinct evidence classes. Report-challenge POST remains feedback storage; it does not schedule requested verification.

Rollback sets `STRUCTURED_CHALLENGE_ENABLED=false` to stop new scheduling (or disables structured processing more broadly). Retain required-proof markers/readers, publication vetoes, deletion, receipt settlement and unknown holds; do not drop migration030 or erase admitted obligations. Engineering lead owns the flag; review at the W08 matched-evaluation checkpoint before activation or removal. Persisted typed outcomes provide audit telemetry; no private target text enters ordinary logs. No paid activation is authorized by this decision.

ADR025 review amendment: limited outcomes must pass independent challenge restoration and carry every target warning; lost required records veto every outcome because missing targets cannot establish safe qualifications. Request-digest recovery closes the search-receipt/challenge-pointer crash gap without another query. Markdown sharing retains canonical limitations. Reproduced negative controls and passing final focused results are preserved in verification/v6/checkpoint-review/.

## ADR026 — persist matched research strategy identity

W08 uses a server-owned versioned strategy, pinned atomically by additive idempotent migration031. `criterion-adaptive.v1` remains default; `iterative-baseline.v1` iterates through original-question criterion order while coverage is incomplete. Both use identical production admission/tools/extraction/model policies/checkers/writer/publication and ceilings; only refinement selection differs. Child runs inherit the owned parent choice; replay ignores changed config. No public strategy field, dependency, service, prompt/model change or spending allowance. Local paired controls include actual HTML/PDF parsing and retained failed runs; fabricated provider costs/semantics are not a quality benchmark. Rollback changes new admissions or disables structured scheduling, retaining stored strategy/proof/financial/privacy readers. Twelve real task references and independently adjudicated/live evidence remain separate gates.

## ADR027 — source removal conservatively invalidates dependent runs

W03/W06: authenticated source deletion tombstones the owned source, all copies of an owned attachment, reused evidence consumers and descendants under account/run locks. Incomplete dependency lineage requires conservative derived-run invalidation; unrelated raw sources/runs/accounts remain. Source bytes/locators/metadata and dependent model/report/brief context are purged, pending legacy-file cleanup is explicit, and stale workers cannot restore content. Public source-read response adds sourceId and DELETE accepts that identity. No migration/dependency/processor/spend change. Financial receipts/unknown holds and required-proof markers remain; redacted reports cannot export. Tests cover owner/retry, actual local unlink, reuse/descendants and paused model/parser responses. Rollback disables new deletion calls but retains tombstones, fences, parent-admission denial, file cleanup and financial holds. Native controls/external retention remain distinct.

## ADR028 — recover provider costs without resending work

W02: a fixed read-only generation metadata adapter and operator command reconcile previously issued, generation-identified intents with the recorded project/key scope. Generation ID/model/provider/raw cost must match; bounded body/time and no redirects apply. Network lookup occurs outside database transactions. Additive idempotent migration033 retains minimal cost receipts and numerical historical adjustments, never prompts/completions. Original semantic receipts/results are unchanged: money recovery is not research proof. Unknown/missing-ID/unbound-credential/conflicting outcomes never become zero or trigger a completion retry. Duplicate generation identities cannot charge two intents. Confirmed overruns remain visible and block further key/project issuance.

Historical repair only reclassifies demonstrably unbacked estimate-equals-confirmed rows in terminal live runs with known settlement amounts. It restores the original customer hold, records prior amounts and awaits a real receipt. Missing historical amounts or ambiguous cost basis block repair rather than invent a refund. Current live data was not repaired or queried; controls use local synthetic accounts and fabricated metadata. No dependency/service/prompt/model route or new paid allowance. Runtime cost bounds remain conditional on external provider enforcement; search plugin invoice ceilings require separate authorized evidence. Official metadata contract inspected: https://openrouter.ai/docs/api/api-reference/generations/get-request-&-usage-metadata-for-a-generation (GET generation ID, total_cost). Rollback disables lookup/repair invocation, retaining cost journal, unknown holds, overrun stops and privacy deletion.

W02 receipt-review amendment: account/run locks precede shared global legacy guard, key and project locks for both admission and actual financial updates; NULL-key historical receipts require the global guard exclusively. Actual PostgreSQL barriers cover all three overlapping scopes. Historical restoration blocks when the original hold cannot cover prior settlement or remaining liability. Test-only legacy key contamination is isolated after assertions; production guard remains strict.

## ADR029 — protect native content snapshots with the existing credential-store dependency

W03/W07: report/draft payloads move from ordinary AsyncStorage to a separate SecureStore service using existing expo-secure-store15.0.8 and WHEN_UNLOCKED_THIS_DEVICE_ONLY. Credentials retain their own service. Two fixed slots, bounded UTF-8 chunks, an allocation manifest and final active-slot switch preserve the prior snapshot during interrupted writes and permit orphan cleanup. AsyncStorage retains only non-content install/revocation flags. Legacy plaintext migrates only after protected commit; migration failure rejects restoration and leaves the old payload pending migration, never returns a plaintext fallback or claims encryption succeeded. Deletion writes a durable denial flag before native cleanup, retains it on failure and surfaces failure; corrupt/unavailable native metadata can still require cleanup retry. Reinstall-marker checks revoke surviving keychain snapshots. No new dependency, backend schema, service, processor, prompt, public API or spending authority.

Session persistence coalesces superseded snapshots, skips unchanged payloads and waits150ms of quiet on native; background transitions flush the latest queued write and propagate failures. Abrupt termination before acknowledged save can lose the latest input tail; this is not a power-loss durability claim. Existing account/request generations and credential revocation remain. Full account-switch/process-death races are a separate remaining W03 gate. Research/Library are the only bottom tabs; Settings stays reachable through Profile. The existing static accessibility assertion now matches the two-tab rendering and additionally requires the Profile control; no label protection was removed.

Pure controls cover bounded Unicode, migration, incomplete/corrupt payloads, interrupted slot allocation, failed native deletion, reinstall, ownership, coalescing and flush failures. Actual Expo Go observed prior synthetic report migration into two SecureStore chunks with no ordinary snapshot, then logout removed both chunks. Bulk ADB text injection did not deliver all2640requested characters in multiple attempts; failures are retained and are not a passing typing/performance claim. Paced injection120characters/batch with0.4-second gaps delivered all2640characters in59.89s; exact UI text and two protected chunks survived process restart. The unpaced bulk-input result remains failed/unexplained, not replaced by this narrower pass. Final Profile logout left no credential/report/plaintext and an empty guest draft. Native cache validation does not establish standalone dependency resolution, OS backup behavior, iOS accessibility or arbitrary device security. Rollback may disable persistence while keeping SecureStore readers/cleanup and denial markers; it must never resume plaintext snapshot writes or resurrect revoked content.

## ADR030 — recover a correction draft with its exact run and revision

W06/W07: preserve one active replacement-question draft, evidence policy, run ID and base brief revision in the existing protected account snapshot. Strict local parsing rejects unknown fields, unsupported policy/version and oversized text while preserving an otherwise valid report. Older snapshots hydrate without a correction draft. Browsing another run hides the draft; returning restores it. Input events require the current authenticated run/revision; stale text retains its original basis and submission is disabled until explicit review/rebase. Successful child admission clears the draft atomically with the child transition. Logout/account replacement use existing snapshot revocation. No new dependency, public contract, backend migration, provider, prompt, service or spend authority. This is a local serialized shape, not a server authorization input; existing server correction validation remains authoritative.

Six added regressions cover persistence, browsing, stale revision handling, account changes/late writes, malformed metadata and older snapshots. Android Expo Go cold restart retained the exact revised question and nondefault refresh policy, then actual local API/worker/PDF extraction completed the correction with zero reused passages. Parent setup was programmatic; model transport was fabricated,14calls/zero paid calls. This is not live semantic quality, repeated native sign-in/upload or standalone acceptance. Rollback may hide the editor/recovery while preserving protected readers, revocation and server revision checks; it must not silently submit a stale draft. Abrupt termination before acknowledged save remains outside the durability guarantee.

## ADR031 — immutable mobile admission journal and atomic retry recovery

W02/W03/W07: before network activity, one pending request is durably saved in a separately protected, account-bound native journal. It pins the question, route, UUID admission key and each upload key/name/MIME/kind/SHA256; document bytes are never persisted. Confirmed upload IDs are journaled before admission. Retrying uses identical identities; reselected files must match the original content fingerprint, including when the original upload never reached the server. A malformed/foreign journal fails hydration closed and ordinary sign-in is disabled until explicit local cleanup. Account changes/logout remove the journal. Accepted run state is committed before journal removal; unknown outcomes retain the journal. Recovery reads and revocation cannot be removed on rollback.

Dependency impact: exact SDK54-pinned expo-crypto15.0.9 native SHA256, MIT, declared explicitly in package/lockfile; no permissions, processor, network or storage. Official SDK54 API: https://docs.expo.dev/versions/v54.0.0/sdk/crypto/ . Current Expo Go exposes the module; the old standalone APK does not establish current native dependency resolution. Native adapter uses the public SDK digest wrapper, validates32output bytes and fails if unavailable; no JavaScript fallback. Tests inject Nodecrypto into the pure client retry state machine. Original Noble2.4.0 experiment was removed after actual8MiB native measurement: chunked JS48.695s/731eventloop ticks, native module16ms for identical SHA256. Preserve those failures/tradeoff receipts. Android SDK54 wraps synchronous native hashing in a Promise: yield/check cancellation before/after, but do not claim the native call itself is interruptible or runs off-thread. Immutable private snapshots bind uploaded bytes to the checked fingerprint. Missing digest capability blocks upload/admission rather than weakening identity. Digests remain protected local data, removed on cleanup; no public search disclosure. Signed standalone/iOS resolution remains a release gate.


Migration034 adds nullable account-scoped upload key hashes with a partial unique index. Optional UUID upload headers preserve existing clients. Same account lock as deletion serializes uploads; matching bytes/name/MIME/size replay the stored ID, mismatches/deleted content conflict. Opaque key hashes survive deletion to prevent resurrection; content/filename/digest remain scrubbed. Migration035 adds minimal account/key-hash withdrawal markers. Authenticated POST /v1/run-requests/resolve atomically returns the existing run or withdraws an unused key under the admission account lock. It bypasses route/consent scheduling gates only to resolve/withdraw, never schedules/cancels/spends. Delayed admission of a withdrawn key fails closed. This prevents a rejected request from permanently locking the composer without risking an unknown duplicate.

The native editor locks the pending question/route, exposes Retry plus Check or withdraw, checks connection on explicit retry, and requests missing original files. A native large-text defect placed the recovery notice inside the horizontal composer; it was reproduced and fixed by moving the notice into the scrollable conversation. Initial lost-response test was transparently retried by Android; a stronger harness holds all admission responses until explicit resume to test unknown-outcome restart. Evidence is local/API/native with fabricated model transport, not real-model quality or external exactly-once delivery. Rollback disables new retry scheduling while retaining journal readers/cleanup, upload deduplication, withdrawal markers/admission vetoes and ownership/budget gates.

W02 receipt deadline amendment: Node20 garbage collection can lose transient AbortSignal.timeout sources combined through AbortSignal.any. A forced-GC subprocess reproduced the missing deadline; the adapter now owns a timer/controller and removes timer/caller listeners on settlement. Deadline/response bounds and unavailable classification are unchanged. Failures remain preserved; mock transport controls do not prove actual receipt reconciliation.

## ADR032 — lease authorization uses wall-clock time after lock waits

W02 mutation-basis review reproduced stale semantic/terminal writes and provider issuance when PostgreSQL now() remained pinned to a transaction start before lock waits. Fenced writes now compare clock_timestamp after locks and immediately before/after the callback, rolling back expired callbacks. Issuance revalidates after shared legacy/key/project budget locks and again after intent/action insertion; delayed INSERT expiry rolls the transaction back without changing the original allowance. Renewal locks run then lease before checking current expiry; a single UPDATE predicate can be evaluated before its row-lock wait. Replacement leases are stamped from database wall-clock time, not transaction start or the Node clock. Run/account/brief/consent/fence conditions and financial unknown holds remain strict. No dependency, public contract, schema, provider route, prompt or spending authority changes.

Real PostgreSQL delayed-lock regressions reproduce the failures before repair and verify positive current-lease behavior. A test harness initially deadlocked its own expiry setup and was terminated; corrected setup retains the same assertion. Standard test database retained unbound synthetic financial liabilities, so financial affected-suite proof uses a newly created isolated local database; existing data/guards were not weakened or deleted. Exact commands, failures and environment in verification/v6/lease-clock/. Rollback must retain wall-clock authority checks, renewal lock ordering and post-callback veto; reverting feature scheduling cannot restore expired mutation authority. This is local issuance/mutation correctness, not a claim of external exactly-once effects.

Bounded audit inspected admission/correction/deletion, model/search/read gateways and receipt settlement. Provider/network calls observed outside DB callbacks; content re-enters fenced writes. Financial receipt settlement deliberately survives lease/account deletion under financial locks without granting semantic-write authority. Remaining unverified candidate: cancel API invokes cancellation and event publication separately; Pool-scoped transaction advisory locking around emitEvent may not cover its separate INSERT. Next regression must exercise concurrent cancellation/event sequence and deletion races before declaring the full mutation audit complete.

## ADR033 — Atomic owned cancellation and event sequencing (2026-09-17)
W02/W03: real PostgreSQL barriers reproduced concurrent cancellation event collisions, event resurrection after account deletion and partial cancellation when event insertion fails. The owning run operation now locks the active account, locks/rechecks the owned run, and commits cancellation with its event in one transaction. Direct Pool event emission also keeps the sequence advisory lock and insertion in one transaction. Public response/schema, dependencies and migrations are unchanged; financial settlement remains separate. Five new regressions plus affected existing cancellation/privacy suites pass81/81; backend types and boundaries pass. Evidence: verification/v6/cancellation-atomicity/RESULTS.json, including failures. Rollback retains atomicity, ownership, deletion and publication veto; disable an affected mutation rather than restore partial cancellation. No live/provider/native claim.

## ADR034 — Reading restoration follows owned mounted layout (2026-09-17)
W03/W07: restored anchors previously depended on matching the original block callback and could be replayed on later layout after a user drag. A reader-generation tracker now scopes measurements to account/session, immutable report, visible block set and mounted reader. It waits for card/block/viewport/content measurements, clamps to the actual scroll range, consumes restoration once, and cancels automatic restoration on user drag. Source return remounts the report reader; protected snapshots keep the existing report/block/signed-offset shape. Hidden concise sections and removed sections have distinct explanations. Account/logout and old callbacks cannot restore another reader. No dependencies, migrations, public schema or spend changes. Eleven focused behavior controls include protected-store recreation and account clearing. Existing static lifecycle assertion now requires the new restoration entry point and dimension/user-drag wiring instead of the removed helper name; scroll and continuity assertions remain. Native evidence is separately scoped in verification/v6/reading-position/. Rollback retains owned protected anchors and stale-view rejection; disable automatic restoration if native regressions occur rather than expose another account's content.

## ADR035 — Source deletion retains opaque admission replay vetoes (2026-09-17)
W02/W03/W06: a real PostgreSQL regression showed that clearing a source-invalidated run's idempotency key allowed a delayed no-attachment admission to create a fresh run. Deletion now captures the original exact key hash into existing admission_withdrawals before scrubbing run metadata, in the same account/run-locked transaction. No new schema, dependency, service or route. Eleven combined source-deletion/admission-recovery controls pass, including case-sensitive key identity, unaffected other accounts, fresh requests, current purge and unknown financial holds. Backend types pass. Evidence verification/v6/source-deletion-replay/RESULTS.json retains the reproduced failure. Historical keys scrubbed before this protection are unrecoverable; no retrospective repair is claimed. Rollback must keep the hash veto and deletion tombstones.

## ADR036 — Source deletion acknowledges local redaction before remote effect (2026-09-17)
W03/W07: a confirmed source deletion first durably stores a cleared account snapshot plus an opaque source-ID retry handle. Current/previous reports, run, source, correction, reading anchor, events and selected attachment bytes/text are cleared; the unrelated composer draft is retained. The request cannot execute if durable storage acknowledgement fails. Network loss or malformed acknowledgement retains the handle and blocks reopening/research until retry or account cleanup. Account-generation guards reject late receipts; hydration forcibly redacts dependent content whenever a handle exists and fails closed on a malformed handle. An acknowledged server response is durably stored before clearing the local obligation. The SourceSheet requires a two-step confirmation bound to source/passage/version, explains local selection/cache clearing, and distinguishes server deletion from pending file cleanup. The existing DELETE API is unchanged; no dependency, migration, new processor or spend. Required writes supersede earlier coalesced snapshots. Unknown admission must be resolved before deletion, never silently dropped. Tests cover storage failure, lost reply/restart, acknowledgement write failure, foreign/stale receipts, account switch, malformed handle, replay with empty invalidation list, and selected attachment purge. Native evidence is separately recorded under verification/v6/mobile-source-deletion/. Rollback may disable new deletion scheduling but keeps pending readers/redaction/retry and account cleanup; it cannot silently restore deleted content.

## ADR037 — Requested claim checks preserve an immutable evidence obligation (2026-09-17)

W01/W02/W03/W05/W06: ordinary report feedback remains storage-only. An explicit controlled follow-up creates a child for one owned report/version/claim using the strict `requested-verification.v1` contract. The target captures the original assertion, semantic scope, claim revision, task, source versions, passage digests and actual extraction/support provenance. Re-extraction cannot omit or replace it. Legacy claims without restorable scoped provenance reject explicitly. Positive verification output can be checked again; cycles and a ninth consecutive verification child are rejected before recursive proof restoration or reservation.

`reuse_snapshot` reassesses the selected immutable passages. `refresh_sources` rereads the selected known URLs or re-ingests owned attachment bytes using existing durable tools; identical attachment extraction may reuse its measured cache. Neither policy discovers additional sources. The original question and captured assertion/scope enter the existing support gateway. The optional private note is saved feedback, **not assessed** and never used as a public query. Outcomes describe support, contradiction, qualification or unresolved status within inspected evidence, not universal factual truth. Negative report blocks link independently detected contrary/qualifying passages even when the model omits those citations; positive claim bindings remain restricted to supporting evidence. Blocked or unknown processing cannot publish a substitute conclusion.

Admission reuses active-account/conversation locks, consent, attachment ownership, allowance, exact-byte UUID idempotency and durable outbox primitives. Optional bound recovery on `/v1/run-requests/resolve` matches the saved parent/request digest and independently restores target proof before returning an accepted child; ordinary-run key collisions cannot be adopted as verification. Missing admissions are atomically withdrawn. Recovery stays available when scheduling is disabled. Account/source deletion purges private targets, notes and results before dependent provenance; late issued-cost settlement may persist only the existing minimal financial record, never resurrect content.

Impact checklist: additive idempotent migration036 introduces `requested_verifications` and `runs.verification_required_revision`; contracts own strict public inputs, research-core owns pure outcome classification, backend domain modules own admission/proof and the worker composes existing adapters. No dependency, service, processor, provider route, prompt version or allowance increase. Single-target context composition changes model behavior and uses existing pinned `assess_support`; known-URL refresh can also require the existing brief operation. All issuance retains normal consent/fence/reservation/receipt limits. No paid evaluation is authorized by this change.

Publication independently restores the captured target, exact model manifest, real durable receipt, current evidence basis and scoped result. A missing required marker, target or result cannot select generic writing; both marker and existing-row detection remain readers on rollback. Disable scheduling on regression while retaining bound resolve/withdrawal, immutable proof readers, publication vetoes, deletion, receipt settlement and unknown holds. Never erase admitted proof obligations or restore the old regex-generated verification narrative. Historical fixture follow-up remains explicitly labeled diagnostic research, with reopened discovery and no targeted-verification claim.

Evidence: `verification/v6/requested-verification/RESULTS.json` preserves 169/169 affected real-PostgreSQL controls, 28/28 recovery controls, 73/73 requested/P3 controls, initial failures and the reproduced negative-citation defect followed by four focused passing controls. These sets overlap. Final backend typecheck, governance and boundaries pass. Documents are synthetic; model and web-read transports are fabricated. They establish executed application behavior, not live-model quality, human adjudication, native acceptance or superiority. Some malformed proof paths fail closed through retry rather than a terminal typed blocked result. Real-model applicability and independent held-out evaluation remain required. Reverse this design only with equivalent immutable target/recovery/publication proof, measured need and privacy/cost review.

## ADR038 — Scope the Expo PostCSS repair to its existing dependency edge (2026-09-17)

Requirement: W09 dependency security without an unrelated framework upgrade. The installed Expo Metro config54.0.17 resolves PostCSS8.4.49 while8.5.28 is already in the lock. Decision: pin only `@expo/metro-config>postcss` to8.5.28 through a pnpm override and locked resolution. No runtime provider, processor, schema, migration, native module or user authority changes. The transitive CSS build parser keeps the same major; this is not a general Expo/Metro upgrade.

Impact checklist: exact isolated9d95ad5 checkout plus override passes offline frozen install,123mobile tests, types and Android JSexport; audit drops5moderate/4high to3moderate/2high but still exits1. Root integration requires its own install/resolution/audit and affected mobile check receipts. Existing image-size/Metro, uuid/xcode and Vitest findings remain; static reachability observations are not clean security proof. Expo Go and JavaScript export do not resolve the standalone native dependency graph. Evidence: verification/v6/dependency-postcss/RESULTS.json; root receipts will be separately labeled.

Rollback restores the prior locked build dependency only if this repair breaks builds, retains the advisory as unresolved, and cannot waive a release security gate. No audit exclusion, suppressed command exit or unrelated package upgrade is accepted as repair.

## ADR040 — Accept an exact question-span edit without implying selective research (2026-09-17)

W06: add strict `replace_question_span` beside the existing `replace_question` correction input. The client identifies the original question with its SHA256, UTF-16 start/end offsets, exact quote, explicit replacement and evidence policy. Under the existing active-account, owned-parent and expected-revision locks, the server recomputes SHA256 from the stored parent question and derives the child question itself. A caller-supplied digest cannot substitute for that computation. Reject mismatched digest/quote, reversed/out-of-range offsets, surrogate-pair splits, malformed Unicode and blank or over-20000-code-unit results. Equal start/end plus an empty quote permits explicit insertion; empty replacement permits removal when the result remains valid. Errors contain bounded reason codes, not private questions, quotes or digests. Existing whole-question acceptance remains unchanged.

This is granular accepted-question input through the API, not a semantic criterion editor or a mobile span-editing UI. It does not infer criterion equivalence, apply a model-generated patch, append the correction note to the question, or claim selective recomputation savings. Existing change-set JSON retains the full accepted typed patch, original correction text and expected revision; their complete identity determines replay. Snapshot policy reuses authorized immutable source membership, refresh policy does not inherit it, and both retain unknown dependency completeness, complete child support recomputation and conservative reopened discovery. Parent questions/reports remain intact, subject to privacy deletion.

Governance impact: one additive public discriminated-union member and a pure core helper; backend correction admission computes the digest with existing node:crypto and owns the transaction. Core receives the trusted computed digest without importing Node or a provider SDK. No migration, dependency, service, prompt, model route, processor or allowance change. Reuse the existing consent, attachment, reservation and outbox gates. No new paid authority is granted. Requirement/test linkage: `question-patch.test.ts` covers strict schema, exact offsets/quote/hash, Unicode, explicit insertion/removal, result bounds and whole-question compatibility; `question-patch.integration.test.ts` covers owned/revision/consent admission, exact stored patch, concurrent replay, parent preservation and evidence-policy behavior. `verification/v6/question-patch/RESULTS.json` records155core controls and8combined PostgreSQL controls, including the existing rediscovery regressions, plus types/boundaries. These are local synthetic controls, not live-model semantic quality or mobile acceptance.

Rollback disables new span acceptance while retaining accepted patch/change-set readers, existing whole-question correction, immutable evidence/provenance, conservative recomputation and ownership/deletion/publication/financial gates. Never reinterpret a saved span against a changed original question. Broader semantic criterion editing and selective dependency traversal require separate applicability evidence; reverse this API design only with equivalent exact-input and replay guarantees.

ADR038 root integration receipt: exact Metro config54.0.17 resolvesPostCSS8.5.28; offline frozen install0downloads,136mobile/types and648module2.18MB JSexport pass. Root audit remains exit1/3moderate/2high. No native or paid execution.

## ADR039 — Required correction rediscovery fails before model preparation when unavailable (2026-09-17)

Requirement W06/V2-04/G05: relaxed constraints can reveal sources absent from the parent. Existing unknown-completeness replacements record reopen_discovery=true; disabling discovery must not silently publish from the old evidence alone. Reproduced an old-only child report under disabled discovery. Decision: fail a non-document correction with this obligation and disabled discovery, before task/model preparation. Documents retain their existing private-query boundary. Do not infer equivalent tasks from identical model-extracted criteria.

Impact checklist: one worker capability guard and production-path regressions; no dependency, schema, migration, prompt, provider or allowance change. Existing whole-question/snapshot/refresh semantics, immutable membership, claim recomputation and publication gates remain. Four earlier gateway correction controls now explicitly enable bounded discovery; all original report/version/arithmetic assertions remain, with confirmed synthetic search receipts and no-reread checks. Fresh evidence control uses the actual durable read path. Existing136gateway tests pass before moving the guard earlier; final8span/rediscovery controls pass after that move. Initial false-publication and unnecessary brief-operation failures are preserved.

Corrected and fresh full runs produce the same two explicit synthetic platform facts. Correction reuses1passage and reads1new source; full rerun reads2 and inherits0. Both make8provider attempts and record10synthetic micro-units, so no model-cost saving is claimed. Fabricated transports prove orchestration, not live semantic correctness or independent human adjudication. Evidence verification/v6/correction-rediscovery/ and question-patch/.

Rollback disables new correction scheduling when required discovery is unavailable; retain recorded obligations, immutable membership, deletion and financial holds. Never restore old-only publication by ignoring reopen_discovery.

## ADR041 — Upgrade the test runner with unchanged behavioral controls (2026-09-17)

W09/V6-F19: pin the three existing Vitest declarations to4.1.11, which includes the advisory repair unavailable in the old3.x line. Retain all existing assertions, tests and runner configuration. The locked Vite/mocker graph changes only development tooling; no application processor, native module, public schema, migration, prompt or provider route changes. Node20.20.2 satisfies the resolved tooling requirements. No new services or spend.

Impact and verification: isolated experiment followed by root frozen offline install, verify155/116/136/6, mobile types, full393 local PostgreSQL tests and17 actual parser/API controls pass. New synthetic database avoids erasing retained accounting evidence. Current audit exits1 with1moderate/2high; this is not a clean security claim. Evidence and earlier cache-miss failure: verification/v6/dependency-vitest-experiment/ and dependency-vitest-integrated/. JavaScript/runtime checks are not native or hosted CI evidence.

Rollback restores the prior runner declarations and lock only if incompatibility is found, records its advisories as unresolved and retains every behavioral assertion and runtime gate. No test suppression, broad error acceptance or release waiver.

## ADR042 — Classify deterministic stored verification failures without swallowing operational errors (2026-09-17)

W05/W02: malformed captured target schemas, row records, changed digests and original-target identity previously escaped as retries despite permanent invalidity. Introduce a closed VerificationTargetError class only at explicit deterministic validation sites. The worker catches that class, stores a bounded blocked reason, emits an unresolved event and terminates under the existing fence. Database, lease, provider and untyped lookalike exceptions retain existing retry/fencing behavior; no generic message-prefix classification.

No dependency, public schema, migration, prompt, provider or budget change. Required proof markers and publication vetoes remain. Existing settlement derives confirmed cost from intents and retains unknown holds; failure cannot erase original financial rows. The stored error contains no private target or note. Six failing pre-fix regressions are retained; isolated35 requested-verification tests, backend types and boundaries pass, including confirmed7micro settlement, unknown hold retention and transient/lease/lookalike controls. Primary integrated receipts are separate in verification/v6/verification-corruption-integrated/.

Rollback can disable new scheduling while retaining deterministic target rejection, immutable readers, required markers, deletion and financial settlement. Do not convert corruption into publication or release unknown reservations. This is local synthetic correctness, not semantic model evidence.

## ADR043 — Isolate the xcode UUID compatibility override (2026-09-17)

W09: pin only xcode>uuid to11.1.1, preserving CommonJS and the existing v4 API used by xcode3.0.1. This exceeds xcode's declared^7 range; it is an explicit compatibility exception supported by actual API-path tests, not vendor endorsement. Do not adopt coordinated Metro overrides based on JavaScript export alone. No application dependency import, native module, schema, migration, prompt, processor or budget change.

Impact: isolated candidate on the actual PostCSS/Vitest graph passes CJS resolution, synthetic Xcode parse/edit/write/reparse,1000 generated identifier checks, malformed-buffer rejection and actual Expo iOS prebuild --no-install. Root integration independently passes frozen offline install, actual roundtrip and mobile types. Metro remains0.83.3. Audit exits1 with zero moderate/two high image-size advisories; no exclusions or clean security claim. Evidence verification/v6/dependency-uuid-root-candidate/ and dependency-uuid-integrated/. Earlier failed offline install and malformed synthetic project are preserved in dependency-uuid-experiment/. Prebuild is not a compiler, native build or device check.

Rollback removes only this override and matching lock edge, retains PostCSS/Vitest repairs and records the reintroduced advisory. Native dependency resolution/release gates remain unfulfilled. No paid or deployment authority.

## ADR044 — Preserve omitted HTML lists as explicitly partial supplements (2026-09-17)

W04: actual Trafilatura XML omitted entire ordered and unordered restriction lists in generic documents with following prose. The retained development SQLite bytes expose the same omission. Preserve missing top-level lists from the already sanitized DOM with immediate lead-in, nested item order, original numbering metadata and struck-through qualifiers. Exclude navigation/aside/hidden/menu-class supplements; avoid repeating a whole plain list already retained. Cap100lists,1000items,8nested levels and250000supplemental characters, retaining explicit omission warnings. Supplemental ordering and potential overlap with main text are disclosed; no full article-order reconstruction or layout/OCR promise.

Impact: new HTML extractor identity trafilatura-2.2.0/structure-v2; accept historical structure-v1 in the existing boundary parser. No new dependency, provider, schema table, service, prompt, network authority or budget. Plain text/PDF behavior and source-read operation identity remain unchanged. New reads recordv2; completed immutable source receipts are never silently invalidated/reread. Ownership, source bytes/digests, locators and partial access remain preserved. Root parser/API verification is separately recorded in html-list-integrated.

Evidence: generic pre-fix4fail/1pass; final8 actual HTML controls and4 actual PDF controls/types pass in isolated clone. Same development SQLite SHA f3467b530b883d4a00574fe1a898b3d121ed72764ae28cf66941080ac0badb9e now preserves the missing limitation at list:3,72→75blocks. Frozen gold and held-out cases unchanged. Retain earlier failing reference and first limit-fixture failure; do not infer model selection, support, live quality or adaptive superiority. Subsequent source review confirms ordinary production selects all passages then rejects counts above24 before extraction; the75block document therefore remains unavailable to that model path until a separate capacity fix. Evidence verification/v6/html-list-fidelity/.

Rollback disables new supplemental extraction or restoresv1 scheduling while retainingv2 receipt readers and immutable evidence, ownership/deletion/publication gates. Never reinterpret old receipts under new extraction semantics or erase access/omission warnings.

## ADR045 — Admit more whole passages within unchanged request and financial ceilings (2026-09-17)

W01/W02/W05/W06/W08: the ordinary worker selects every readable authorized passage, but the old24-count reader bound rejected useful25-block documents before extraction. It did not silently truncate. Raise the shared backend model-port count to128 across selection, model context, support manifests and captured-verification proof readers. Keep entire passage text, IDs, digests and versions; do not rank, batch or drop evidence to make the request fit. Canonical UUID ordering is identity stability, not relevance ranking.

Governance impact: one backend capacity constant and five existing reader boundaries; no public request/output schema, prompt, provider route, dependency, migration or service change. Per-passage24000characters, ordinary40sources, verification24sources, serialized context240000bytes, complete request<=128000bytes, output4096tokens and full-context21658micro reservation remain. Count expansion does not waive byte/token-policy, ownership, duplicate-ID, provenance, consent or budget checks. Smaller requests/fingerprints remain unchanged; admitted larger contexts have complete new request digests. Increasing useful capacity can increase actual usage within an already authorized run; these tests authorize no live spend.

Regression requirements: reproduce25 rejection, carry a late exact qualifier through extraction/support/writer, retain all input identity/text, terminal replay without issuance, snapshot verification/correction reuse,128 success,129 rejection and unchanged serialized-byte rejection. A late contrary passage must reach support and prevent positive publication. Model/extraction responses are fabricated on real PostgreSQL; no semantic quality or live latency claim. Agent failures and final receipts belong to verification/v6/passage-capacity/; primary final integration is separate and must pass before checkpoint acceptance.

Rollback may disable new scheduling or higher-count acceptance while retaining128-capable immutable proof readers for already admitted obligations. Do not revert shared readers so that saved evidence/proof becomes unreadable. Keep required markers, publication vetoes, deletion, financial settlement and unknown holds. Larger documents may still fail byte/source/count bounds; this is bounded capacity, not general long-document selection or guaranteed model attention.

## ADR046 — Preserve struck-through qualification in every extracted representation (2026-09-17)

W04/W01: actual generic HTML probes showed the same withdrawn statement retained with a marker in thev2 list supplement but unqualified in main prose/list copies and table cells. Marker-aware normalization now preserves source s/del/strike and Trafilatura overstrike markup in every emitted main/table representation. Do not erase old wording; label it explicitly as struck-through beside the current wording. Supplemental inline normalization excludes comment content while preserving the visible tail; a primary review reproduced that separatev2 comment leak. Source comments are not documentary assertions.

Impact: HTML extractor identity advances to structure-v3, withv1/v2 readers retained. Existing list-version expectation changes only for newly executed extraction; historical-version readability gains explicit coverage. No dependency, schema table, prompt, provider, service, access or budget change. New derived passage text/digests recordv3; old immutable receipts are never rewritten or automatically reread. Partial extraction, ordering/overlap and layout limitations remain. This does not claim CSS rendering, exhaustive formatting fidelity or model understanding of deletion semantics.

Evidence: nine generic formatting controls fail before repair; after marker handling, the supplemental-list comment regression separately fails. Final23 actual HTML/list/formatting/PDF controls pass in the isolated clone, including all copies, cells, comments/tails andv1/v2 readers; backendtypes/boundaries pass. Preserve intermediate21/22 passes and failures. Primary integrated checks follow separately in capacity-formatting-integrated; no current native or live semantic claim. Artifacts verification/v6/html-qualifier-review/IMPLEMENTATION-RESULTS.json.

Rollback can disable new extraction scheduling while retainingv3 readers, original bytes and all immutable proof/ownership/deletion/publication gates. Never silently reinterpret saved marked text as current unqualified prose, or relabel historical parser output with the new version.

## ADR047 — Check authenticated route configuration before document upload (2026-09-17)

W02/W03/W07: admission previously uploaded private documents before learning that the selected server route was disabled. The existing admission helper now requires a fresh authenticated GET /v1/settings and validates the selected route flag as strictly boolean true before saving a new journal, uploading or admitting. False, absent, malformed, failed or superseded responses stop those effects. Existing pending identities and confirmed upload IDs survive; Check/Withdraw remains available independently to recover accepted requests while scheduling is disabled. Account/view generation checks fence late responses.

Impact: mobile-only helper port and App wiring; no new endpoint, public schema, dependency, service, prompt, model route or migration. Server consent/ownership/budget/admission gates remain authoritative; configuration can change after preflight and does not certify model readiness, budget or health. Existing reconnect behavior is unchanged. Regression linkage: admission-preflight.test.ts covers both modes, malformed flags, ordering, durable retry identity, stale account/view and accepted recovery; existing admission tests retain assertions. Root148 mobile tests/types/648-module2.18MB JS export pass. Actual local API/PostgreSQL probe checks settings authentication, disabled zero-effects, enabled uploads/admissions and recovery with zero provider intents; no worker or paid model.

Review failure retained: the initial task premise named /v1/session, which exposes no route flags. Mocked passes on that wrong endpoint were rejected before integration. Corrected source and actual API proof use /v1/settings; old patch and receipts remain in verification/v6/admission-preflight/. This is not native journey proof. Rollback can revert client preflight while retaining all server authorization, consent, financial, retry and deletion gates; it would restore the known unnecessary-upload behavior.

## ADR048 — Add documents to an owned correction with durable recovery (2026-09-17)

W02/W03/W04/W06/W07: a corrected question could reuse or refresh existing sources but could not introduce a new owned document. The additive strict `append_attachments` patch accepts one to three distinct owned attachment IDs with `reuse_snapshot`, retaining the original question, prior attachments and immutable evidence. The combined attachment limit remains three. It inherits no claim approval: the child recomputes extraction/support/coverage/writing under current consent and normal financial gates. Private filenames and the fixed correction note do not become public queries. Document-only changes record no public rediscovery; whole-question/span corrections retain their existing rediscovery obligation.

Account/parent/conversation locks serialize ownership, consent, revision, admission, allowance and outbox. Case-insensitive duplicate, old, foreign, deleted and excessive attachment IDs are rejected. The content-derived request identity retains existing whole/span key bytes. A bound authenticated `/v1/runs/:id/corrections/resolve` reads the exact accepted child or atomically withdraws the identity without scheduling, spending or requiring renewed processing consent. It restores the saved accepted patch and parent binding; missing/tampered proof fails closed. Admission honors withdrawals, including source-deletion tombstones. `/v1/settings` adds boolean `appendDocumentsAllowed`; it reports configured capability, not budget or model-health assurance.

A reproduced defect in the first append implementation published the old answer when the new PDF was unreadable. The worker now derives added attachment IDs from owned immutable parent/child briefs, so removing optional change-set metadata cannot erase that obligation. Missing parent basis fails before model preparation; an appended document without a digest-bound readable authorized passage fails explicitly before assertion extraction/support. Earlier reports remain historical; no new successful report is invented. Partial extraction remains partial and does not establish complete PDF/layout fidelity.

Mobile uses the existing native picker, binary endpoint and SHA/idempotency upload pipeline, with a dedicated account-bound protected journal and separate transient bytes. Fresh route and append capability checks precede new effects. Confirmed upload IDs survive ordinary snapshot coalescing; retry and withdrawal reread durable state. Actual authenticated child GET is durably stored before clearing the journal. Branch revisions may exceed parent+1; exact parent and a later safe integer revision are required. Account/view/consent/picker guards reject late responses; consent invalidates old view leases without losing selected-run identity. UI exposes exact-file retry and Check/Withdraw, preserving the question and old evidence. Orphan uploaded copies are honestly disclosed as retained until deletion.

Impact: additive patch/resolve/settings contracts and mobile protected-journal version; no dependency, service, migration, prompt, model route or paid authority. Requirements link to append-document core/integration/actual-PDF tests and correction-documents mobile/flow tests. Agent evidence records19 affected PostgreSQL,157 core and173 mobile controls/types, actual parser runs and retained failures; primary combined integration is separately required. No native journey or real-model quality follows from those receipts. Canonical behavior belongs to ENGINE_CONTRACTS/MOBILE_SCREEN_STATES/SECURITY_PRIVACY_COST, not a parallel specification.

Rollback disables new append controls/admission while retaining old and appended attachment/evidence readers, required readability checks, protected journal/recovery/withdrawal, ownership/deletion fences, publication proof, receipt settlement and unknown holds. Do not erase admitted children or restore the old-answer success on unreadable new input.

## ADR049 — Execute registered evaluations through production under explicit authorization (2026-09-17)

W05/W06/W08/W09: the old `eval:live` command always exited and incorrectly described credential presence as authorization. Replace it with evaluator-only authorization/planning, journal and production-driver modules. The driver uses the existing authenticated API, admitted A1/B strategies, worker, real extraction adapters, tools, writer and publication checker. Production API/worker imports may not reach evaluator modules or gold. Generation receives only registered original/corrected questions; reference criteria, decisive source lists and split labels remain outside generator context.

Initially execute only `live_discovery`. Frozen supplied-document mode is explicitly unavailable and records all selected slots as unrun before admission; HTML is not injected through a fake attachment path. Keep registered repetitions and alternating arm order, B snapshot correction and fresh A1 corrected rerun. Record failed parents, invalid/unknown outcomes and budget-unrun slots rather than dropping denominators. Semantic scores and adjudicator identity stay null until actual adjudication; synthetic controls are not a benchmark win.

Execution is default-deny: explicit execute/operator-confirmation flags, matching approval identity and authorization-file SHA256, short validity, registered scope/hash and positive total budget are required. The file is an operator attestation, not independent proof of user permission. An existing dedicated database/queue and consented account/session must be provisioned separately; no automatic account/allowance/consent grant or migration. The runner holds an advisory lock; the operator must keep unrelated workers/dispatchers out of the dedicated database. Current task authorizes no paid execution.

Schedule each run only with room for its full existing reserve, counting prior account/project/same-key and unattributed liabilities. Existing gateway account/project/key caps remain authoritative for issuance. Unknown outcomes retain reservations and stop later steps. Stable approval/slot admission keys survive a new journal; no blind new-identity resend or automatic hold settlement. Application caps and observed receipts do not certify provider-side invoice enforcement.

Fsynced serialized JSONL records pin commit/source tree, registration, effective configuration, prompt/contract/model policy, every slot/attempt/run, owned source artifacts/extraction/access/locators, support/report/reuse identities, returned receipts, confirmed cost, unresolved exposure and measured attempt time. Existing output directories are refused. Tokens/keys remain environment-only and are redacted from string values as defense in depth. Operational errors are bounded and recorded, including queue failures; failed durable writes are not reported as completed results. Evaluator artifacts have their own operator retention/deletion responsibility; account deletion in PostgreSQL does not claim to delete external exported files.

Impact: replace stub plus evaluator-specific modules and local tests; strengthen governance import rejection. No dependency, service, migration, production schema, model prompt/route or paid allowance change. Unit/governance, default-deny CLI and real local API/worker/actual-parser controls use fabricated transports. A new-journal replay must issue no additional provider/parser calls; prior same-key liabilities and opaque outcomes must retain holds. Review reproduced expiry during asynchronous exposure/journal writes; a final check directly before admission now refuses expired approval, while already admitted work retains completion receipts. Primary combined checks and exact source receipts remain separate. Rollback disables the command while retaining admitted identities, outbox/proof/readers, journals, failures, deletion, receipt settlement and unknown holds.

## ADR050 — Propagate remote source deletion into protected mobile content (2026-09-17)

W03/W07: source deletion already purged backend evidence and dependent reports, but a same-run terminal snapshot could leave the previously cached report visible. GET run now exposes an owned boolean `contentInvalidated` from the existing source-deletion tombstone and forces `reportId:null`. Ordinary cancellation is distinct. Deleted accounts are rejected even if a credential row remains. Real local API/PostgreSQL regressions reproduce the missing signal, retained redacted report ID and deleted-account read, then pass4/4; initial erroneous cancellation timing expectation is retained and corrected to existing cancelling semantics.

Mobile invalidation redacts dependent current/previous report, source, correction content and reading position before ancillary fetches, preserving independent composer content and exact pending recovery identities. An account/run-bound noncontent protected marker precedes snapshot cleanup; interruption leaves a retry hold, while a clean tombstoned snapshot can reopen offline without a false hold. Session-local invalidated identities sanitize late queued snapshots after marker removal. Storage failure stays explicit and cannot be called proven disk erasure. Current account/view/run fences reject late updates; explicit opening state handles responses arriving before React commits the selected run. Correction and verification adoption reject invalidated children.

Impact: additive response boolean and mobile protected marker, no database migration, dependency, service, prompt, provider route or spending authority. Link API remote-source-invalidation integration and mobile invalidation controls; primary combined receipts follow separately. No current native, hosted-account or external backup-retention proof. Rollback may disable new scheduling while retaining invalidation signals, durable cleanup markers, ownership/deletion denials and immutable proof/financial readers. Never restore deleted cached research as a rollback convenience.

ADR049 integration follow-up: the combined extraction suite reproduced the runner refusing an earlier synthetic unattributed hold (3failed/36passed). The evaluator test now creates and retains its own random local database, using existing test-role CREATE DATABASE permission. No production filter, accounting settlement, deletion, assertion or accepted-error change. Other suites' liabilities remain intact; the default fixture-free runner still counts unattributed exposure.

2026-09-18 native graph follow-up on exactce2f889: isolated Android prebuild and actual Gradle releaseRuntimeClasspath resolution pass (Gradle8.14.3,12m25s). Selected CommonsIO2.6,Codec1.10,OkHttp4.9.2,Okio2.9.0,annotation1.8.1,biometric1.1.0,React/Hermes0.81.4. Apache CVE-2024-47554 affects selectedIO2.6 through XmlStreamReader; reachability is not established and no clean native-security claim follows. Official source https://commons.apache.org/proper/commons-io/security.html checked2026-09-18. Prior ADR024 unresolved-resolution statement is historical; actual graph is now inspected. No app compilation/APK/device install/release. Root source/dependencies unchanged. Local Gradle and NDK27.1.12297006 were downloaded under the existing SDK license. Evidence verification/v6/native-graph-current/RESULTS.json. Native compatibility/remediation and current device journey remain open.

ADR024/ADR050 native follow-up (2026-09-18): actual current-dependency x86_64 debug build and Android14 emulator journey are recorded in native-current-emulator. Device testing exposed two transport/presentation defects: bodyless source DELETE advertised JSON and was rejected; invalidated terminal Library snapshots retained progress status and displayed WRITING. Add body-aware request headers, authoritative lifecycle presentation and cancelled tombstone status. Requirements W03/W07 link api-request and invalidated-progress regressions, actual server retry/redaction and native cold-restart observations. No new dependency, migration, schema, provider, prompt, service or paid authority. Test-only controller extension binds its optional FAT32 fact to exact actual parsed passage/digest; it is not a production model fallback or semantic evaluator. Rollback may disable deletion entry/new scheduling but must preserve pending deletion, content tombstones, protected cleanup, ownership, evidence readers and financial settlement; never restore deleted content or fabricated progress. Build uses a default test debug key and Metro; physical standalone/iOS/release and dependency remediation remain separate.

## ADR051 — Retain the full admitted document artifact (2026-09-18)

W04/W09: the actual registered TMP117 PDF (1718295 bytes) passed the existing 8 MiB upload/parser boundary, then failed SQL23514 because migration008 limited evidence artifacts to1500000 bytes. The worker transaction rolled back; its unconfirmed execution and retained upload are failed evaluation evidence, not successful reading. Add idempotent migration037 changing only the named artifact-body constraint to8388608. Keep the independent public-fetch1500000-byte gate unchanged. This aligns durable private-document evidence with existing admission, without changing model context, provider limits or the public network boundary.

Impact: one additive constraint migration, no old migration edits, new dependency/service, public API, model route, processor, prompt or spending authority. Link `artifact-size.integration.test.ts`: exact8MiB byte/digest/owner roundtrip, all migrations replayed twice preserving admitted bytes,8MiB+1 atomic rejection and foreign source denial. Pre-fix23514 and a test-process OOM remain; native Buffer.equals replaces generic deep comparison with the same complete-byte assertion. Official actual-PDF worker rerun is separately recorded by the evaluator packet; storage tests alone do not prove extraction or semantics. Keep8MiB storage/read capacity on rollback for admitted artifacts; disable new uploads if needed rather than shrinking a constraint over stored evidence or discarding bytes. Account/source deletion still overrides retention.

## ADR052 — Execute registered frozen PDFs through owned production ingestion (2026-09-18)

W04/W08/W09: extend the default-deny evaluator beyond live discovery to the existing `frozen_supplied_document` mode for actual PDFs. Project source IDs, filenames, MIME and frozen SHA256 only; keep rubric/reference answers out of generation. Validate realpath containment, PDF bytes,8MiB maximum and at most3distinct documents before effects. Each original/fresh-rerun slot uploads through the existing authenticated binary endpoint with approval/slot/source identity; B correction inherits its parent's owned evidence. Recheck expiry before every upload and run admission. A successful upload receives a durable identity receipt before proceeding; expiration or journaling failure retains the object for explicit operator cleanup and does not authorize a run.

Disable public discovery/retrieval/new challenge scheduling equally in frozen arms; use the same ordinary model/parser/support/writer/publication and budget gates. The result measures supplied-document behavior, not source-discovery superiority. HTML remains unsupported as a supplied attachment and is recorded individually unrun, never renamed to text or inserted as evaluator-made passages. Mixed selections keep all registered slots; incomplete execution exits2. All-unsupported execution ends before database/session/provider setup. Current user authority still permits no paid execution.

Trace attempted owned attachments/source versions even when extraction yields zero passages. Verify source-version, transport, extraction and raw artifact digests independently; failed reads do not become support. Include strictly owned, numerically ordered run events to distinguish actual failure causes. Corpus/source/config hashes include migration source changes in the dirty-tree signal. Original attempt, unknown-hold and financial settlement controls remain unchanged.

Impact: evaluator-only projection/loader/driver/trace changes plus tests; no dependency, new service, production API/schema, model prompt/route or processor change. Migration037 is separately owned by ADR051. Unit and actual PostgreSQL/parser regressions cover same-path four-run pairing/replay, missing/tampered/unsupported files, unreadable source digest tamper, expiry during upload, changed bytes under one upload identity, gold isolation and preserved prior accounting/unknown controls. Official PDF ingestion is not successful synthesis: two fabricated-controller failures lack assertions, and ESP32 hits the existing context policy. Semantic scores/adjudicator remain null. Rollback disables frozen scheduling while retaining uploads/journals, original run identities, immutable evidence/proof readers, deletion, required obligations and all financial holds/settlement.

ADR052 review follow-up: owned trace restoration includes failed reads that generated no passages. Legitimate bodyless transport failures must have null artifact/source/transport digests, zero bytes, no passages and exact unavailable extraction shape. Retained non-success bodies still require matching raw/source/transport digest and byte count; absent extractor output is allowed only for the typed HTTP/extraction failure. Successful reads cannot use either exception. Before-fix negative-read controls fail with `evaluation_receipt_identity_unconfirmed`; repaired controls preserve failures and reject forged success/digests without another provider call. These are failed-read observations, never supporting evidence.

## ADR053 — Restore sign-in after acknowledged expiry cleanup and clear stale offline state (2026-09-18)

W03/W07: physical Android testing reproduced two client lifecycle defects. An expired saved session was successfully cleared during startup, but the early hydration return left `storageReady:false`; Profile sign-in rejected outside its visible error handler. Separately, reconnecting and opening an authoritatively invalidated run correctly hid deleted content but returned before clearing the old offline banner.

Keep sign-in blocked while expired-session cleanup is pending or fails. Restore readiness only after `clearAccountLocal` succeeds and its captured account generation remains current. A superseded cleanup cannot enable another session or overwrite its error. Move hydration/readiness denials into the existing sign-in error handler so the user sees the recovery action instead of an inert control. Successful owned invalidation refresh clears `offline` in the existing guarded redacted state update; retain the deleted-source notice, storage acknowledgment requirement and account/view/run guards.

Impact checklist: narrow App lifecycle changes and `apps/mobile/test/auth-recovery.test.ts`; no dependency, service, migration, public schema, prompt, model route, processor, paid allowance or new persistence representation. Tests execute actual App closure bodies against controlled boundaries; they cover pending/successful/failed/superseded expiry cleanup, visible readiness denial and successful invalidation reconnect without ancillary deleted-report fetching. Five expiry regressions failed before the fix; the additional reconnect regression failed with five passing before its fix. Final focused six tests and mobile typecheck pass. Physical reproductions are distinct from post-fix native acceptance; final device results belong to `verification/v6/physical-unlocked/RESULTS.json` when recorded, not to an inferred pass here.

Rollback may disable automatic recovery or new sign-in entry while retaining fail-closed cleanup, durable denial markers, account generations and visible recovery errors. Never unlock sign-in after unacknowledged storage deletion, restore deleted evidence, discard pending identities or weaken ownership, publication, settlement or unknown-hold controls.

## ADR054 — Admit saved HTML through the existing owned document path (2026-09-18)

W04/W08, V6-F06/F16: the registered corpus contains saved HTML, but evaluator projection and binary upload admitted only PDFs (plus explicit text notes). This prevented the same production parser/worker from exercising those frozen tasks. Accept UTF-8 `text/html` bytes through the authenticated binary endpoint, limited to the existing HTML parser/public-fetch bound of 1,500,000 bytes. HTML never enters the pasted-note shortcut and upload acknowledgment remains `stored`/`not-read`. Parsing stays in the existing networkless subprocess with Trafilatura structure-v3; scripts and remote resources do not execute. Malformed/unreadable pages produce existing partial/unavailable outcomes.

The evaluator accepts digest-pinned HTML as owned supplied documents, with `attachment://` provenance, no inferred live HTTP success or original-site authority, and disabled discovery. It uses the same upload, worker, support and writer paths as PDFs. Corrections reuse exact authorized source membership; fresh reruns upload separate owned originals. Parser limits and model context limits remain distinct. The official corpus and reference criteria are unchanged; enabling an input does not score it correct.

Impact checklist: additive MIME capability in contracts/backend; no dependency, model asset, service, database migration, prompt, route, budget or processor change. Native picker support is unchanged (PDF/text/Markdown); this change establishes API/evaluator saved-HTML behavior only. Existing privacy/consent, byte validation, idempotency, owner/deletion/fence and publication gates apply. Tests cover admission bounds, invalid encoding/MIME, exact bytes, unparsed upload state, ownership/replay, script removal, randomized-entity original/correction/full rerun, and official SQLite restriction extraction. The former unsupported-HTML CLI control now tests absent-session denial; unsupported XML still has explicit projection/loader and runner-denominator controls. No assertion is removed to disguise a failure.

Rollback rejects new HTML uploads/evaluation scheduling while retaining parser/readers, admitted bytes and immutable receipts, deletion, proof validation and unknown financial holds. Never downgrade HTML into ready pasted notes or relabel saved bytes as a successful public fetch. Evidence is recorded in `verification/v6/current-continuation/`; fabricated model controls are not a real-model benchmark or independent adjudication.

ADR054 encoding regression: real parser testing then found that unlabelled valid UTF-8 HTML corrupted Österreich/Café/漢字 before extraction. Structure-v4 explicitly uses UTF-8 when the original bytes strictly decode, otherwise retains declared/inferred legacy decoding with `non_utf8_encoding_inferred`. The digest always covers unchanged original bytes. This normalization rule applies identically to fetched and supplied HTML. Structure-v1/v2/v3 receipt readers remain; old immutable reports are not rewritten. New ingestion does not reuse an older cached HTML extraction. Explicit snapshot reuse still describes the original extraction version. No new parser dependency or encoding detection service. Existing fidelity assertions remain; only expected current extractor identities advance to v4. The failure artifact is `encoding-before.log`.

## ADR055 — Bound whole-passage inspection with immutable omission proof (2026-09-18)

W01/W02/W03/W05/W06/W08: all-readable evidence previously exceeded the executable context policy (including the saved ESP32 document), preventing substantive extraction. New runs use `whole-passage-selection.v1`: preserve every passage when it fits; otherwise use deterministic Unicode token overlap weighted by inverse passage frequency to rank anchors, preserving the whole adjacent passage on each side within the same source version. Required counterevidence target quotes and their neighbors are mandatory. This is retrieval, never entailment or a claim that absent evidence satisfies criteria. The policy bounds candidates4096/16MB text, chosen passages128/sources40/double-serialized passage+source bytes48000. Oversized required bundles and inventory exhaustion fail explicitly. The final gateway still enforces its original byte/token/output/financial ceilings independently.

Migration038 records immutable candidate metadata (short block positions plus hashes of full locators, including PDF geometry), selected IDs, required quotes, revision/question/policy hashes and recomputable proof without duplicating text. An opaque required-slot identity in runs prevents proof deletion from creating a new paid logical action. All mutations use existing fenced transactions; original inventories survive later evidence additions but cannot restore altered/deleted/foreign passages. Strict model context carries the selection ID/digest/counts; input-manifest.v5 retains those fields through extraction/support/writer/calculation. Publication restores every current proof and requires exact omission limitations even for a limited caller outcome. An omitted inventory forces completed_with_limitations. Corrupt/missing selection proof terminalizes without more model issuance; database/lease errors are not relabeled as content failures.

Impact checklist: one pure core policy and backend-owned metadata table; no library/service/public action API/output schema/prompt text/provider route/processor or allowance change. Internal context is versioned by selection and manifest; request hashes bind the actual changed input. Existing runs are backfilled legacy-all.v1 while new runs default to selection; legacy in-flight/unknown request identities and proof restoration remain unchanged. Source/account deletion purges the new inventory. Snapshot correction recomputes selection over actual authorized reused versions, never a reuse flag. The feature packet links requirements and tests; execution evidence distinguishes fabricated model controls from actual saved-document parsing and from unrun paid quality evaluation.

Rollback disables new scheduling/admissions while retaining migration/policy/proof readers and obligations, deletion, fences, receipt settlement and unknown holds. Do not switch admitted runs to the old policy or regenerate missing proof. Limitations: lexical ranking is not semantic recall, neighboring passages may not contain distant qualifiers, all selected evidence can still exceed later writer/support context limits, and partial inspection cannot establish full-document coverage. Held-out semantic performance and independent adjudication remain open.

ADR055 verification follow-up: initial full suite420pass/22fail exposes21manual test-teardown FK failures and one actual requested-verification state mismatch. Extend teardown inventory, retaining every assertion. During saved target restoration, convert only the typed selection-proof error to the existing original-target-changed verification error; the existing worker then records blocked state/result, terminal failure, zero new issuance and preserved financial obligations. Transient/lease/untyped lookalike errors still propagate normally. The existing source-title-corruption regression and an added missing-selection case protect this integration.

## ADR056 — Check the authorized inventory before granting selected claims support (2026-09-18)

W01/W03/W05/W06: ADR055's bounded context omitted a known contradiction when its neighboring passages could not fit. The real PostgreSQL/production-worker probe published the positive limited report; its small-context control correctly withheld it. Both outcomes remain recorded. Omission warnings are insufficient publication protection.

The backend now additionally executes the existing deterministic scoped checker over every authorized passage in the immutable selection inventory. This server-only input never expands model context or authority. A separate `selection-inventory.v1` receipt binds selection proof, claim revision, scope, original validated assessment, checker version and support intent. This additional gate only downgrades eligibility; it never upgrades an uncertain selected-context result. Writer, calculations, coverage, publication and requested-verification target admission consume the effective result. Existing scoped-support.v3 receipts and admitted provider identities are unchanged. Missing old additive receipts trigger recomputation without unfenced writes; conflicting receipts fail closed.

Impact checklist: migration039 adds one backend-owned derived-result table, with deletion cascades and explicit account/source cleanup. No dependency, service, public schema, model asset, prompt, processor, route or financial allowance changes. Evaluation traces include the new typed results. Known literal contradictions/qualifications receive a deterministic check; this is not semantic comprehension of every omitted passage. Model-omission warnings remain required and say exactly what was excluded. Tests retain positive controls, require an actually omitted contradictory passage, exact proof/revision binding, no writer issuance for disputed claims, replay, tamper refusal and deletion. There is no independent human adjudication.

Rollback may stop new admissions, but must retain the additive gate for all selection-aware admitted results, immutable proofs, deletion, fences and unknown holds. Never restore the known unsafe behavior by reverting to selected-context-only eligibility. Original failure and before/after probe are separate artifacts in verification/v6/evidence-selection/; broader recall and real-model quality remain unfinished.

## ADR057 — Recover empty bounded extraction without expanding authority (2026-09-18)

W02/W05/W06/W08: the actual production worker stopped after an empty first extraction even when its immutable inventory contained uninspected passages. A fresh synthetic document regression fails on the old runtime despite a decisive fact outside the first context. New admissions use `empty-selection-recovery.v1`; migration040 preserves `none.v1` for existing runs. An unknown policy fails before task-model cost. Successful nonempty extraction retains the existing criterion/support workflow.

After an executed empty extraction, a pure deterministic policy packs uninspected whole anchor/neighbor bundles under the existing passage/source/byte bounds, then uses unchanged whole-passage-selection.v1 with mandatory anchors. Every next context contains previously uninspected evidence. The same durable selection slots, proof obligations, model manifests and provider identities support replay; no new model operation or role is introduced. The existing four-iteration research limit also caps this recovery. Exhausted inventory, oversized indivisible evidence and remaining evidence at the limit are explicit unresolved outcomes. The model does not grant authority, and selected passage presence never establishes an answer.

Impact checklist: migration040 adds only an admission policy field; no new dependency, service, public action/schema, model asset, prompt, processor, provider route or monetary allowance. The pure selector factors existing ordering/bundle/cost expressions without changing v1 proof output. Unknown external attempts retain reservations and cannot be resent. Source/account deletion and fencing use existing immutable membership and selection obligations. Claims still pass ADR056's full-inventory veto. Correction runs select against their actual authorized versions and changed question; this does not promise semantic agreement with an unexecuted live full rerun.

Tests require a truly omitted decisive passage, a new exact context, successful constrained publication and replay without extraction reissuance; they also cover inventory exhaustion, indivisible oversized bundles, foreign inspection IDs, policy migration/replay, unknown policy denial before cost, four-iteration exhaustion and held unknown outcomes. Existing negative publication, parser fidelity, byte bounds and legacy paths remain. Registered official document controls must expose all attempted contexts and unresolved outcomes; fabricated models remain nonsemantic controls. No tuning to previously exposed reference answers.

Rollback disables new recovery admission by restoring the new-run default to none.v1 while retaining the versioned recovery reader/selector for admitted runs, immutable proof slots, inventory veto, ownership/deletion/fences and unknown holds. Do not overwrite an admitted recovery policy or recreate prior paid action identities. Next quality evidence requires real-model execution with current scoped authority; this local recovery mechanism alone is not benchmark success.

## ADR058 — Provider capacity and historical completed estimates are different accounting facts (2026-09-18)

W02/W08: the operator incorrectly treated $5.545 of pre-receipt-schema estimates as new unpaid charges blocking a newly authorized $0.40 comparison. Read-only inspection establishes all70 old OpenRouter intents have historical state confirmed and their runs are terminal; individual actual costs and key attribution remain unknown. The current provider key reports usage$0.2140772 and remaining$4.7859228. These aggregates do not reconcile individual old rows. The user explicitly requested this repair after authorizing spending.

A new explicit evaluation period uses a separate local ledger/account and preserves the historical database without migration, rewriting, releasing holds or resending actions. Setup fingerprints original financial rows, requires the pre-action/key-attribution schema and terminal/confirmed states, and refuses modern or unresolved external-outcome ledgers. This is an operator-authorized forward-period setup, not a general production escape hatch. Every new request keeps existing atomic key/scope/account reservations, actual usage receipts and unknown-outcome stop/reconciliation rules. ProductionDriver's same-key/unattributed liability gate is unchanged.

The actual evaluator additionally reads bounded, redirect-denied GET /api/v1/key before setup and each step. Remaining provider capacity is already net of historical provider-reported usage; it must not be debited again by old estimated or confirmed lifetime totals. Initial capacity must cover the full authorized grant, subsequent checks at least one admitted-run cap. The independent application grant still bounds all new spend. This does not prove account credits, exactly-once delivery or absent concurrent external key usage. HTTP errors/malformed quota fail closed. No management credential or private customer content is needed.

Impact checklist: no dependency, migration, public schema, model route, prompt, processor or service change. New internal quota response parsing and evaluator journal events; read-only fixed provider endpoint. Thirteen deterministic quota controls cover rounding, insufficient/unlimited capacity, historical usage, malformed/error/oversized metadata and redacted failures; existing unknown-liability evaluation tests retain their assertions. The selected paid run must retain actual failures rather than claim quality from a quota response. Rollback disables new paid evaluation; retain original ledgers, the new ledger and every receipt/unknown hold.
