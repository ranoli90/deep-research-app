# Builder handoff

The authorized V6 W01–W09 goal remains active and incomplete. Preserve the existing app, user changes and both untracked ZIPs. No push/deployment/hosted migration/public exposure or paid provider calls. Current paid allowance is unknown. STATUS.md owns the current requirement map; EXECUTION_LEDGER.md and versioned evidence preserve history.

## Current checkpoint

Branch `codex/v6-evidence-milestone`; latest reviewed implementation `5fb44af682a58c7b417fae64e6f8a4a40e2d3f2c` (UUID repair), following0734558 typed verification-target failure, ece80f4 Vitest4,5fbec3a correction rediscovery/span and PostCSS,6490546 profile extraction,f88091a requested verification. Check git status before editing. Original review pin03fab6b9d6a04ce9fdaeb48636383757213f7242; staged kit /tmp/deep-v6-staging. Current separate agent work concerns actual HTML list fidelity and bounded read-only lifecycle/mutation reviews; do not overwrite it.

Requested verification binds an owned report/version/claim and independently restores extraction/support provenance. Snapshot or selected-source refresh feeds queued support assessment without discovery. Private feedback is saved but not assessed. Required proof survives scheduling disable/result loss; deletion purges private content while preserving obligations. Malformed stored schemas/records/digests/original identity now terminate as blocked with bounded reasons; database/lease/provider failures retain recovery. Confirmed spend settles from original intents, unknown holds remain. Mobile stores exact request identity before POST and clears it only after durable child adoption/withdrawal. Full native flow remains unverified.

Corrections accept whole-question or exact hash/UTF16 span patches through the API, preserving owned revision/idempotency/immutable membership. Unknown dependency completeness uses full conservative recomputation and required discovery; disabled required public discovery fails before model preparation. Synthetic correction/full-rerun facts agree:1new read+1reused passage versus2new reads, both8provider attempts/10synthetic micro-units. No live semantic agreement, model savings, semantic criterion UI or selective traversal claim.

## Evidence and environment

Node20.20.2,pnpm9.15.9,Python3.12.3,TypeScript5.9.3,Vitest4.1.11. Local PostgreSQL127.0.0.1:55432. Truncating suites run serially. Latest fresh synthetic database deep_research_vitest4_v6_20260917; no prior data dropped. Preserve retained synthetic liability in deep_research_test. Native database deep_research_native_v6 is separate. Actual parser runtime /tmp/deep-v6-extraction-runtime.

- dependency-vitest-integrated: verify155core/116backend/136mobile/6governance, configured unsuppressed types/boundaries; full393/393 PostgreSQL tests in397.62s;17/17 actual extraction in79.03s; mobiletypes/frozen offline install pass. Real local DB/parsers, fabricated providers, paid0. This is reviewed working-tree evidence, not a final exact clean checkout.
- verification-corruption: pre-fix6fail/3pass retained, isolated35/35; verification-corruption-integrated:35/35 in107.82s/backendtypes pass. Nine added deterministic/financial/operational regressions preserve existing controls.
- dependency-uuid-integrated: actual xcode3→CJS uuid11.1.1 roundtrip, frozen install/mobiletypes pass. Candidate actual iOS prebuild --no-install passes separately, not a native build. Metro remains0.83.3. Audit exit1 with0moderate/2high image-size advisories. Experimental Metro is unadopted because native runtime compatibility remains unproved.
- Latest exact detached clean proof is5fb44af: offline frozen install, verify155/116/136/6,mobiletypes,648module2.18MB AndroidJS export and clean end state. Subsequent HTML extraction change is separate until verified. Profile/correction graph separately exports648modules/2.18MB. Hosted manual CI has not executed. Current design typecheck is unsuppressed since5f10a84.

Evidence paths are under verification/v6/, with RESULTS/FILES and failure receipts. Do not sum overlapping suites. Document validators pass only structural metadata; they are not app/native/semantic tests. W08 fixed12task/13document protocol retains SQLite extraction omission and slower adaptive HTML controls. No live superiority or independent human adjudication.

## Android state

Last authorized device10.0.0.167:43417, REDMI Note15 5G, Android16, ExpoGo54.0.8. Standalone app.deepresearch.mobile is old and untouched. Device remains locked; native source-delete, new verification and full authenticated journey are unpassed. Latest synthetic server account/content deleted; API/Metro/reverse mappings stopped. Protected synthetic device cache/session remains and must be cleared after unlock using exact account identity1617411b-d6bd-4aeb-98b4-9f0ebb5197e3 before further acceptance. User was asked once to unlock. Do not clear unrelated Expo data or publish OS dumps.

Earlier bounded ExpoGo proof covers picker/consent/upload digest/deletion, correction recovery/refreshed PDF, unknown admission restart/one run/withdrawal, source-reader/share preview and synthetic cached section7 reading-anchor cold restart. These are component checks, not a complete authenticated journey, recipient delivery, signed native build, iOS or hosted identity proof. Latest experimental Metro launch found no runtime target while locked; no account read/cleanup occurred and temporary processes/reverses stopped.

## Next executable work and rollback

1. Review actual HTML list omission and general regression/fix, preserving frozen gold and original failure. Review pending bounded account/mutation audits. Keep shared DB suites serial.
2. After source/dependency freeze, execute exact current clean-checkout proof and reconcile remaining findings/command/evidence registry. Keep2high advisories open; Metro exceeds Expo pins and lacks native compatibility proof.
3. Complete unlocked current native research→report→source→reopen→correction→share, account lifecycle/reconnect/accessibility and source deletion after guarded cleanup. Continue safe local work while locked.
4. Real-model held-out repetitions and independent semantic adjudication require current allowance/authority. Hosted identity/retention/backups, signed current Android/iOS and release gates remain W10/external; purchases/push unavailable.

Rollback disables new scheduling/acceptance while retaining immutable readers, ownership/tombstones, required challenge/verification/discovery markers, publication vetoes, deletion, receipt settlement and unknown holds. Never restore fixture fallback or erase admitted obligations. No background continuation is promised after execution stops.

W04 HTML list repair over5fb44af: root22/22 actual extraction controls in70.97s/backendtypes pass; isolated8HTML+4PDF pass, generic pre-fix failures retained. Same SQLite saved bytes preserve missing limitation at list:3 understructure-v2; v1 readers unchanged, no frozen gold/heldout edits. Model path still rejects>24passages; separate capacity work is not yet passed. Artifacts verification/v6/html-list-fidelity/ and html-list-integrated/; ADR044; paid0/native0.
