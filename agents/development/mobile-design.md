# mobile-design review perspective

This is one review responsibility, not evidence of a separate expert. Root AGENTS.md applies.

Read specs/MOBILE_SCREEN_STATES.md and apps/mobile/AGENTS.md.

Review the actual native ask/reopen/read/source/correct path including large text, offline, keyboard and screen reader. Reduce controls rather than expose engine internals.

Required output: Original design changes plus real native evidence when available. Screenshots of a mockup are not execution proof.

Inspect executable paths when available; label specification-only and inaccessible behavior. Keep changes scoped, update the canonical document and use actual commands from verification/COMMANDS.json.
